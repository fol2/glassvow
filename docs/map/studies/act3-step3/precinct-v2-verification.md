# Act III Step 3 — corrected candidate qualification

Candidate: `0315abe3f07f7ad7b8885abb737ce10a1a23bd5d`.
Comparison base: `774cb7375b38ca8bf81f7478aac4b6b8d1faedbd`.

The native review study replaces fragmented stair approaches with shared transverse flights and low retaining walls. Broad dark facets, selected cool glints and concentrated magenta glazing/seams restore the intact obsidian precinct direction. Court elevation, generation and encounter topology remain separate: three normal compiler exports preserve all original node records and edge IDs.

## Evidence

- Nine native runs: seeds 717, 4 and 2026 at 844×390, 1180×820 and 1458×820. All image hashes verified independently. 555 focus/click checks and 30,927 sampled route-camera checks pass. Per-seed full-width support, foundation and covered-passage checks pass.
- Uncut native tour: 57.6 seconds; 1,675 travel frames over 14 edges; zero sampled occlusions. Played to completion through the tailnet browser without media error.
- Host Metal tracing: 632 complete frames; active GPU median 1.024 ms, P95 1.468 ms, maximum 6.289 ms. Concurrent GPU intervals are unioned, not added. Initial recorder crash rejected. This measures host activity, not frame latency or physical-device qualification.
- Native master frame-interval P95 8.49–8.564 ms; renderer allocation peak 131.78 MiB. Act I/II native visual and input regressions pass.
- Imports and 424-script parser sweep pass; all 11 scope-selected checks, map asset validation, store exclusion checks and focused regressions pass.
- Independent semantic review: APPROVE at the candidate SHA above, no blocking findings. That review retained full-core completion as a delivery gate; the subsequent complete run and log comparison above satisfy assertion validation without concealing the retained headless diagnostics.

The first core run printed PASS despite an aborted pre-existing Dawn fixture. The fixture now supplies the Vigil quest profile used by application boot; its original assertions pass in a focused run. The complete rerun finished with PASS (101 tests), exit 0, and no SCRIPT ERROR. See `precinct-v2-core.log`. The earlier printed count is not treated as a complete pass. Remaining headless diagnostics match the prior run: 31 null-material and 50 null-scene-tree messages, deliberate negative-test errors, and shutdown resource/RID diagnostics. This is complete assertion success, not a zero-diagnostic engine run.

## Limits and next boundary

This is a Step 3 native study, not campaign integration or device certification. Desktop pointer checks do not establish physical touch behaviour. Three generated seeds do not prove every seed. Six diagnostic-only source hashes in the capture receipt refer to retained local experiments outside the committed runtime; all candidate runtime/compiler hashes and all capture image hashes match. No claim relies on those diagnostic scripts.

Owner approval is required for Act III before Act IV Step 3. All chapters must clear Step 3 before Step 4. Goal remains off. Task-level token use and cache savings are unavailable and are not inferred from account quota.
