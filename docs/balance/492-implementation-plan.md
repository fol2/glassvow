# #492 implementation plan after the pre-registration size checkpoint

The pre-registration/tooling commit `82176e2` crossed the Glassvow 400-line
review checkpoint before any simulator row was run. Work stopped there. The
two-axis review found one process finding and six specification blockers; this
plan is the bounded continuation. No Tier-1 F1, mini-CEM or audit seed had been
used when this plan was written.

## Wave A — make the frozen protocol executable

1. Bind every evaluator bundle to the committed #491 candidate identities.
2. Rebuild point metrics, strict guardrails and every bootstrap from raw shards.
3. Use CRN paired candidate-to-candidate C1a/C1b intervals for dominance.
4. Freeze the mini-CEM representative selection and paired-bootstrap algorithm.
5. Bind the sealed audit to one committed finalist set, one fixed output path and
   an exclusive invocation receipt.
6. Re-run the Standards and Spec reviewers. Simulator work remains stopped until
   both axes clear.

## Wave B — progressive racing

Run Layer 1 on exactly `t1-c000` plus the four #491 candidates. Record decisions,
then run only the baseline plus exact promotions in Layers 2 and 3, inheriting
the prior rectangle each time. Every layer is raw-reanalysed before promotion.

## Wave C — development optimisation only if admitted

If Layer 3 has strong-breadth survivors, freeze the finalist set and run mini-CEM
only on those survivors plus `t1-c000`. Apply the pre-registered development C3,
C4-proxy, Vow-5 and paired-uncertainty rules. If no survivor exists, publish the
Tier-1 insufficiency result and skip both mini-CEM and audit.

## Wave D — one sealed audit and handoff

Only strong finalists enter the fixed `/private/tmp/glassvow-492-audit`
invocation. A second invocation fails closed. Publish at most two complete
numeric/hydrated packets to #421, or publish no handoff and name one bounded
Tier-2 mechanism decision under #488.

## Completion review

Run all repository gates, the two-axis code review, fix every blocker and repeat
until both axes clear. Commit the evidence; do not merge.
