# Owner-supplied TestFlight/device evidence

No owner-supplied device image was available in the issue or its parent at packet creation time, so none is copied or represented as verified here.

Any later owner image belongs in this directory with provenance recorded beside it: original filename, device model, OS version, TestFlight build, orientation, capture time if known, and the closest corpus frame dimensions (act, seed if known, shape, zoom, pose, locale). Do not replace or relabel the desktop/Xvfb PNGs as device proof.
