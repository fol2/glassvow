# Issue #295 — map payload and active-act runtime closeout

Evidence in this directory is generated from the exact commit named in
`manifest.json`. Source/package bytes, imported-cache bytes, signed IPA identity,
floor-device memory, active-act unloading and the iPad 8 map-pan workload are
kept as separate measurements; none is converted into another unit.
