# Control identity: correction to the conversational description

This note corrects the earlier conversational description of the 44/64 and
46/64 Vow-0 controls as "RandomBuild". It does not change any frozen protocol,
primary row, readout, candidate, model, threshold or historical terminal.

At evidence checkpoint 39181dc2e4309d64effef4d8e6f0044bf8bcc57e,
`natural-contribution-20260907/replication/PREREGISTRATION.json` explicitly defines
the controls as the same balanced research controller, not the signed original
arm2. Its frozen model is
2cfe1d8ff00d5ff695664c597be3a93e3960f39732fa049eac82b9397184bab7.
The corresponding continuation-20260908/PUBLISHED-VALIDATION.json records the
balanced Dusk V0 cell at 44/64 wins and balanced Ash V0 at 46/64 wins.

For those fixed observed denominators only, even 64/64 treatment wins give
maximum empirical gaps of 20/64 = 0.3125 and 18/64 = 0.28125. They cannot reach
a 0.35 empirical gap while those same comparator counts are held fixed.

This arithmetic is NOT a population confidence bound, a signed C2 RandomBuild
result, a rejection of the exact candidate under the release protocol, or
permission to change a comparator or weaken acceptance. It does not establish
what a content change would do to the balanced controls either. Such a change
would create a different candidate and require correctly scoped evidence.

Keep the signed original-arm evidence and the fixed research-controller
validation separate. Do not use this arithmetic to skip a package obligation,
reopen or refit the validation, or declare any package/P9 admitted. Continue the
already selected Ash hand-size source-specific causal and observer preflight
under its actual prerequisites; the six-package disposition remains the work
map, not six complete certificates.

New native runs: 0. New independent samples: 0. P9 admission: none.
