# DD1-X implementation-ready promotion packet

This is output 5 of DUSK-DESIGN-1, read with DESIGN.md, DECLARATION.json and the actual Python evidence. It specifies a future implementation; **none of the product changes or native tests below were executed or are authorised by this author handoff**. The only present executable is the bounded non-native projection. The next stage is one fresh exact-artifact review, not native execution.

M = `07b5aa9dec8436132a524511d5438c510e322070` (source and current-main instructions read). H = `5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6` (bound #547 artifact). Prefix H:`research/p9-six-route/duskblade-first-proof-20260912/` is immutable. The current `ADAPTER_RECORDS.md` there, blob `ba53bce69de7667ac62038026eb29474c5567460`, is the adapter contract, NOT the archived count-first obligations map. Binding `D547-PC1-20260914-BIND-01` and receipt #547/5683984558 remain unchanged.

## 1. Proposed source edits and exact ordering

No general status framework, target-history engine, new simulator, detector rewrite or new public command is required. Keep rules/state separate from presentation; preserve Game.apply and its ordinary legal command surface.

| Seam in M | Smallest proposed change | Must remain unchanged |
|---|---|---|
| `content/full-content.json` | Add proposed `setTheAngle` and `crosscut`, uncommon/cost 1/target enemy, with the only values in DECLARATION.json. P effects: ordinary block 4/6, then special `crosscutAnchor`. C effects: one special `crosscutReturn` with n=5/7; do NOT also add an ordinary dmg effect. Add both to the ordinary uncommon pool. | All current IDs, existing cards, authored Stun/Shatter coupling, starters, deeds, relics and Vow rules. P and C are not already shipping IDs. |
| `domain/rules/combat.gd:SPECIAL_IDS`, `_apply_effect`, `_apply_special` | Register exactly the two new special effect names. Existing effect dispatch already passes target and damage_mult. Anchor special only records a valid live member after P's normal Block. Return special captures/clears the anchor and calls `hit_enemy(run, cb, B, n, true, damage_mult)`; after terminal/player/member/different-target checks, call the same helper once on A. | can_play, eff_cost, card payment/hand removal, first-card discounts, one card/Attack count, ordinary hit implementation and native callbacks. Do not multiply Strength twice, bypass Thorns or use raw-hit false for the return. |
| `domain/state/combat_state.gd` | One nullable `EnemyCombatant` reference, proposed name `crosscut_anchor`, default null. No event queue scan or run-global identity cache. | Existing state representation and stable enemy indices. The actual guard is current object membership plus alive, not idx equality alone. |
| `domain/rules/combat.gd` lifecycle paths | Clear before enemy phase; clear when anchored enemy dies and on terminal/handoff/abandon. New CombatState defaults null. Capture/clear before either consumer hit; revalidate the captured object after primary. | Existing terminal choice, ordinary death events, Ember caps, quest/phase/handoff logic, reward timing and defeated-enemy cleanup. |
| `domain/rules/rewards.gd:card_pool` | A two-ID Dusk-only eligibility check in BOTH base-pool and unlocked append paths. Preserve normal reveal eligibility, rarity, ordering and existing pools. | Actual offer sampling and shop_inventory/shop_buy costs/validation. No seed changes, synthetic offers, free grant or new unlock deed. |
| `domain/rules/combat.gd:preview_play` and `presentation/combat/combat_screen.gd` / card presentation | Public marker, clear/replace display, two-target preview when eligible, primary-only preview otherwise, description explaining same-target consumption and this-turn expiry. Derive display from current state; no queue consumption as truth. | Existing command payloads, targeting legality, UI/rules separation, localisation conventions and no hidden future information. |
| `domain/state/run_state.gd`, `application/save_service.gd`, `application/main.gd:_resume_pending_combat` | No new serialized field, save version or migration. Verify existing pending encounter reconstruction creates a fresh state and marker-free view; if it reuses any combat object, explicitly clear at that boundary. | Save v2, stable card IDs/next UID/deck/upgrades, pending encounter identity, class progression and no silent unknown-card replacement. |
| Existing native event/export reader | Observe the new state transitions and ordinary native hit/settlement sequence with typed raw source pointers. Add only the narrow decoding for this proposal to the existing reader path. | Observer must not choose actions, pay rewards, reconstruct missing native outcomes, or import the Python model as an oracle. The bound adapter remains unchanged. |

The content entries are prospective effect payloads, not an instruction to create a production patch now. The new special names are an implementation encoding of the SINGLE declared law, not additional mechanics. P's Block is executed exactly once through its ordinary effect before the anchor special. C's primary is executed exactly once INSIDE its special; combining that special with an extra dmg effect would be a defect.

### Native sequence that must refine the declared law

1. `play_card` resolves actual hand UID; `can_play` and `eff_cost` validate current state, target and resources. Illegal commands do not clear an anchor or mutate zones/counters.
2. The existing path pays, removes the card, increments play count, emits PLAY/ENERGY, and increments Attack count only for C. Iron Talisman and Executioner's Seal processing remain exactly once. The resulting `damage_mult` applies through the native helper to both C hits.
3. Set `pending_chips_active` through the existing path. For P, ordinary Block then anchor. For C, capture and clear, primary hit, validate remaining live context, at most one return hit. No recursive call to play_card.
4. Let the existing per-card pending-chip loop process insertion-ordered connected targets, once. Existing positive-hit chip conditions, Beacon, printed chips, overflow, annealed panes, Adamant, Stun AND Cracked, Ember/Prism, Bell and Smolder behaviour stay native.
5. Retain post-card Venomous on selected B only (`target=enemy`), normal Silk Fan processing once, and normal power/exhaust/discard lifecycle. C is not a Power and neither proposed card exhausts by its declaration.
6. Clear/replace the public marker coherently at every lifecycle boundary. Terminal or handoff after the primary suppresses return. Native terminal callbacks must not be revived by a stale reference.

The Python projection does not certify all these callbacks. In particular it records a Reaper draw obligation but does not draw, and it does not execute Prism/Smolder RNG or native save/UI paths. Preserve those limitations in any review, not merely in a footnote to a PASS.

## 2. Save, pending-combat and seeded-RNG obligations

M's RunState save is version 2 and holds the pending encounter/run state rather than the proposed anchor. Design choice: anchor is a live combat reference only, never in RunState or the save JSON. A newly constructed or reconstructed combat starts unanchored. No cross-fight target ID, arbitrary enemy name or card UID may be used to restore it.

Required native acceptance before claiming compatibility: load a pre-change v2 run and profile; preserve deck/UID/upgrades/reveals/currency/aspect/Vow/RNG/pending encounter; enter its pending combat without a marker; save/reload with the new binary after ordinary acquisition of new cards; resume using current documented combat reconstruction semantics without persisting a half-played anchor. Abandon/re-enter and terminal-to-next-fight must clear it. If actual resume semantics preserve a live combat, the clear must happen on that exact boundary and be tested; do not infer compatibility solely from end-of-fight reset.

No backward loading of a new-ID save by an old binary is promised. No deletion/replacement of unknown IDs or breaking migration is permitted. The future new binary must still contain the new IDs when loading its own saves. If migration or a changed public interface is actually necessary, stop as outside this design authority rather than improvising it.

The mechanic itself consumes no RNG. Existing collateral such as Smolder can consume native RNG as usual. Adding Dusk-eligible content changes the relevant pool and can change offers under the same seed; this is a prospective product change, not an identity failure to hide. On an existing combat with no new cards/state, null-path action, state and RNG traces must match. On Ashwarden, the explicit pool filter must restore the old candidate list and ordering before sampling; merely disabling the return effect is insufficient acquisition isolation.

## 3. Smallest native qualification matrix (NOT run)

Build narrow focused tests under the repository's existing native test runner once separately authorised; do not create a generic balance simulator. Start with deterministic component/refinement tests and stop early on a decisive failed law. Use constructed native states only under recorded native authority/account handling; never turn their fixtures into natural acquisition or policy evidence.

| Group | Decisive cases | Required observation / failure disposition |
|---|---|---|
| Legal commands/resources | P/C order, P absent, C absent, disabled new writer/reader, other aspect, missing UID, dead/null/out-of-range target, insufficient sequential Energy, real Duskmirror/omen first discount | Whole legal command effects/zones/costs retained. Unavailable continuation = UNKNOWN, not zero. A failed law is a design/refinement failure, not a request for new values. |
| Identity and reset | P(A), P(C), C(B); C(A) then another C(B); two consumer UIDs; enemy death between plays; terminal/handoff/abandon; new combat with same idx/type; pending-save resume | Sole live current-object anchor; same-target spends; overwrite/no stacking; no alias or persistent marker; no callback after terminal. |
| Native damage/cost collateral | Base/up independent; Strength/Weak/Cracked/Seal floors; full Block; 1-HP/overkill; primary and return Thorns; Iron Talisman; native first-card discount; printed/chip effects | Compare full player/enemy resources, card zones and ordinary hit order, not only return HP. Physical HP change cannot be replaced with reported overkill. |
| Coupled facets/death | Near-threshold both targets, zero-chip/high-threshold, Adamant hold, Bell, Prism, Smolder transfer/RNG, primary/return nonfinal deaths, Reaper draw/energy, quest/finale handoff | One card-level settlement in native order; complete Stun/Shatter coupling; no suppressed collateral, free callback, recursion, additional Attack count or illegal extra effect after terminal. |
| Full command interventions | Complete P/mediator/C chain, every required proper subset, producer/consumer/mediator disabled views, lawful background alternatives and signed comparator | Prove the bound canonical immediate-label and MEC obligations with real native snapshots. The Python component 00/01/10/11 results are insufficient; R3 COMMAND_CONTRACT_FAIL stays closed history. |
| Closure and three-way peers | Exact Scoreline/Afterimage/switch/cross-enemy/one-bit/threshold/Honing read/write comparisons plus K1/K2/DD1 mixed play on SAME product | Reject exact covered laws or a failed independent slot; no new observer label, UID alias, card renaming, hidden policy information or exclusive credit for generic background. |
| Acquisition/profiles | Ordinary Dusk common/uncommon/rare access, both pool insertion paths, reveal/unlock eligibility, actual shop funds/sold state, excluded-aspect pools, old saves | No hand grant or profile forgery. Pool presence is not acquisition, acquisition is not enactment, and constructed hands are not natural support. |
| UI/policy preview | Public anchor create/replace/consume/expiry; primary-only versus eligible two-target preview; blocked hit, same target and lethal primary; aspect exclusion | Presentation derived from rules, legal public choices and no future draw/seed access. Native execution remains authoritative over preview. |

Tests of mechanism-off behaviour must leave P's Block and C's primary hit, paid cost and card lifecycle in place. Removing an entire command requires the full bound intervention treatment; do not smuggle a free Block or Energy refund into a disabled mechanism. Require a native lawful-continuation proof where the protocol requires one. Report irreducible unavailability honestly.

## 4. Legitimate predecessor profiles and same-product policies

K1/K2 remain the exact outstanding source qualifications in DESIGN.md, not free slots. Before a candidate is admitted, resolve their exact closed-law/union/peer dependencies and define the three surviving MECs on one identical product. If any slot is covered or otherwise fails, this proposed triple is not admitted. Do not spend to learn what an already decisive source rejection says, and do not refill DD1's design nomination.

For each required vow, construct the bound legitimate Dusk-earned profile from real legal predecessor progress with its ledger and exact hashes. Use the specification's first-valid selection rule, not a convenient mature save. K1's Echo Lance/PaneBreaker dependency must be actually satisfied by the selected legal Dusk profile; ordinary unlock/reveal prerequisites for every role must be declared before selection/freeze. K2 and DD1's ordinary uncommons must be eligible through the real pool. There is no Ashwarden-to-Dusk relabelling, force-granted deck or new quest bypass.

Within a vow, all R/B/K1/K2/K3 arms use the same selected predecessor profile and exact product. Required strategy acquisition/enactment happens naturally in the later trajectories. Shared background Strength, Cracked, Facet chips, enemy death and target counts is not owned by whichever policy happens to run. All three chain memberships must be observed within EACH trajectory. A legitimate multi-route trajectory is multi-label; one policy arm must not manufacture the label.

### Public-policy scaffold, not fitted/frozen policies

Existing `tools/balance_policy.gd` / `tools/balance_pilot.gd` legal-action and bounded-lookahead seams should host the later declared policy roster; no new policy engine. R is competent ordinary play, B the signed random-build/competent-play comparator, and each K deliberately selects the corresponding public trade-off. R/B definitions and all bound guards remain as in H. No configuration is fitted or selected now.

Two genuinely different public decision panels are still required for the K policies under H, with actual legal-action preflight. For DD1, candidate panel emphases are (a) immediate bounded return plus survival/focus-fire alternatives and (b) target coordination under projected threshold/intent pressure. Both use the same public state and full legal alternatives. A renamed vector or a route-label feature is not a second policy. Record exact source/hash/roster and tie-breaking BEFORE development; do not choose a panel based on confirmation behaviour.

A proposed small canonical feature scaffold has at most 24 coordinates: public turn; player HP fraction, Block, Energy, Weak, Strength; living-enemy count; affordable attack count, affordable defence count; selected-target HP fraction, Block, Cracked, staggered, Facet deficit and visible intent damage; counts of other living/low-Block enemies; then anchor-present, anchor HP fraction, anchor Block, anchor Cracked, anchor staggered, anchor Facet deficit and selected-is-anchor. Values must be produced by a source-checked canonical public-state reader with explicit finite normalisation and missing-value rules before development. These are design suggestions, not a frozen estimator or acceptance change. Full/blind feature semantics must follow H; remove EVERY direct and derived anchor coordinate in the DD1 blind view, not just the marker flag. If other chosen features leak the removed semantics, remove those too before the legal freeze.

No raw card/enemy UID, root/seed, arm label, intervention name, future intent/draw, eventual win/enactment or receipt flag may enter the classifier. Identity/epoch/idx needed for proof provenance stays outside the feature vector. Policies must not read observer annotations as privileged game state. Use the unchanged bound cost envelope and actual per-decision native forward-evaluation accounting; the specified 128-forward-evaluation budget is not multiplied to give one route free search. Measure CPU and actual work rather than claiming equality from configuration names. Adequate available cost caps are a pre-development external requirement, not a new budget granted here.

## 5. Narrow exporter and adapter field map

Use **D547-NATIVE-EXPORT-2** and the existing entry `prospective_admission.evaluate_packet(packet, context)`. Do not create a second validator, alter H, import a historical count-first adapter or translate CHECKS.json into native records. The model's convenient `trace`/`return_hp` fields are NOT adapter authority.

| Native fact / proposed narrow observation | Existing adapter location and required meaning |
|---|---|
| Exact product/producer/profile/reader and five policy digests, panel/vow/root identities | Each digest-bound native export stratum; required manifest/freeze/exposure namespaces. Raw IDs remain proof context, not model features. |
| Genuine terminal win/loss | Ordinary terminal record in each trajectory; no synthetic model terminal or capped/unfinished run relabelled a win. |
| Real card acquisitions and prerequisite resources | Ordered acquisition snapshots for actual MECs; complete enactment references an earlier qualifying snapshot in the SAME trajectory. P/C in a constructed hand is not natural acquisition. |
| P creates/replaces anchor, actual live owner, C consumes before primary, order/expiry/death | Ordered native decision records and registered native views, with raw public pre/post snapshots and typed pointers. Proposed local observer fields may encode combat epoch, event sequence, producer play reference, anchor idx, selected idx, consumed/invalidated reason and hit phase; they must map through the authenticated native reader rather than add unrecognised top-level adapter keys. |
| Native legality, first preconsumer eligibility, complete P/mediator/C chain and physical payoff | Native reader/decision observations under H; eligibility and independent enabled/disabled labels derived from actual views. Whole proper-subset and canonical obligations remain. Telemetry booleans do not suffice. |
| HP/Block/death and collateral event order | Source-backed native hit and after-state references. Separate return HP from primary, overkill, Bell, poison and other causal paths. Full Block/illegal tails cannot be forced into positive/zero labels. |
| K1/K2/K3 simultaneous memberships | Complete per-trajectory decision-membership map; factual reductions preserve multi-labels. Exclusivity is enact_K AND NOT enact_L in a K-policy trajectory, not a cross-policy contrast. |
| Public canonical feature vector and observed cost | Per-decision registered public views/fingerprint and trajectory `forward_evaluations`/`cpu_seconds`; exact bound full/blind/peer reconstruction and cost validation. No supplied prediction overrides observations. |

Pointers use the exact existing shape, for example `{"role":"native_export/A/v0","path":["arms","K1",0]}`, resolved only inside the out-of-band digest-bound store. Do not fetch arbitrary URLs as evidence during record resolution. A reference to this design, a valid hash, `approved:true`, `legal:true` or an author's PASS is not an extraction or guardrail receipt.

The host must authenticate the manifest and real issuer/receipt identities BEFORE constructing `TrustedContext`. The extraction receipt must bind exact source and supported meanings for ordinary wins, complete MEC acquisition/enactment, first preconsumer eligibility/payoff, public features and legal policy actions. The separate guardrail receipt must close MEC source provenance, legitimate profiles, signed B, policy information/cost and unchanged invariants. Neither is created by this batch. Missing authority/receipts must block; malformed contradictions reject; numeric FAIL or INCONCLUSIVE remains possible after integrity succeeds.

Preserve the existing `D547-FACTUAL-2`, `D547-MEASUREMENT-2` and `D547-PEER-2` mappings: five-leg CRN, complete assigned effective-root order, first 256 eligible and first 256 natural-negative selections from the pre-output fixed permutation, first qualifying decisions, and complete 2,048 per-class peer trajectories including losses/acquisition failures. Fewer than required eligible/negative roots is INCONCLUSIVE, never refill/top-up. Optional bits/predictions/summaries remain cross-checks, not ground truth. Confirmation never trains or selects a model; ties abstain under the existing adverse rules.

## 6. Stage order, accounts and final handoff contract

| Stage | Required evidence before entry | Permitted endpoint, NOT permission supplied by this document |
|---|---|---|
| D0 — current design | Owner design-only exception and declaration | This complete design/model/results/readback. Author batch complete; no native population or candidate attempt consumed. |
| D1 — next | One fresh isolated read-only review of the WHOLE exact artifact and canonical sources, without importing author conversation or asking the author to review itself | Exact-head verdict and readback; scrutinise close Scoreline/switch comparisons, projection limits, full-command claims and all-three dependencies. A design APPROVE alone is not empirical authority. |
| N0 — conditional implementation/qualification | Separately recorded applicable native authority, account/attempt eligibility and adequate available resources; prospective source decisions including reused-slot blockers; reviewed law | Small source implementation and native component/intervention/refinement tests, legitimate profiles, authenticated reader and guardrails. No closed result rerun, hidden top-up or candidate parameter tuning. A decisive covered/failed slot stops this proposed triple. |
| N1 — bounded development | Qualified three-route candidate/product/profiles, signed comparator, predeclared policies/features/blind removal/cost caps/legal preflight and authorised ledger event | Use the existing 64-root per configuration development design, at most eight configurations and required K support; select by unconditional win count with the bound hash tie-break, derive models exactly. No unseen variant or successful-enactment-only selection. |
| N2 — confirmation admission/freeze | Completed development selection, exact input manifest/reader/profile/policy/model/frame/exposure/preflight hashes, genuine receipts and valid account state | Freeze under H, then only the authorised fixed two-panel/two-vow/five-arm, 2,048-root confirmation design. No new roots, candidate, cost increase or attempt on failure/UNKNOWN. |
| N3 — downstream decisions | Full native record-to-decision checks and independent external acceptance through the established process | Numerical/statistical decisions with all hard guards; certificates only through the actual external process. Production/TestFlight and #549 remain separate later gates; do not merge from a symbolic PASS. |

This table does not recreate the accounts. Read their actual valid state before the FIRST native invocation, including failed/retried invocations. Exact event readback is idempotent; an actual retry consumes again. The existing zero-used allocation template is not a reset of historical balances or evidence that empirical authority is free. Keep the canonical allocation, 204 occupied intervals/52 sealed slots, 152 predicates and .025/.025 split unchanged; never borrow #548, refund alpha or turn UNKNOWN into credit.

All five requested outputs are complete as a design-only author artifact. The unresolved obligations are scientific/native promotion gates explicitly carried forward, not missing files silently implied to exist. DD1-X is not a solved triple: K1/Q1 and K2/Q2 source qualification, native full-command/collateral refinement, genuine predecessor profiles, natural support and policy/balance evidence remain. None of these is delegated to a competing writer by this handoff.

The final issue handoff must point to one exact source/proof commit, the subsequent readback receipt and current capsule, and state zero native/spend/production changes. Retain the earlier incorrect duplicate-check publication only as Git history; the delivered checker must match the tested blob `f68b9c1e12ae914367325d6be07de1d5c8b924a0`, not interim commit 6ecf53f6c2bc260df34633db8263e4207c5bbe98. The restored checked artifact contains 40 unique named checks. This publication correction did not change the declared law or tested parameters.
