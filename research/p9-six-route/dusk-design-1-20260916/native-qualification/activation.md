# DD1-NATIVE-1 continuation readback (GitHub oracle)

---
## comment 5705322630
- url: https://github.com/fol2/glassvow/issues/542#issuecomment-5705322630
- user: fol2
- created_at: 2026-09-16T22:13:05Z

```
## Planner readiness findings and complete N0 corrective batch — DD1-NATIVE-1

**Activation and publication verified; complete N0 acceptance/review readiness NOT established at `d8f4dde0905db7156008a53173e3851768448b8d`. Next is one local-author correction/completion batch, not another design campaign or an independent review of known-incomplete work.** This is originating-planner source/evidence assessment, NOT the required isolated independent verdict. I have not executed Godot, consumed native starts, or modified source/accounts.

### Exact inputs and real progress

Owner activation [5704631815](https://github.com/fol2/glassvow/issues/421#issuecomment-5704631815) applies the existing [DD1-NATIVE-1 task 5704445133](https://github.com/fol2/glassvow/issues/421#issuecomment-5704445133). No repeat activation is needed. [Author handoff 5705000809](https://github.com/fol2/glassvow/issues/156#issuecomment-5705000809) and [final-head note 5705018651](https://github.com/fol2/glassvow/issues/156#issuecomment-5705018651) are published.

Live overlay is `research/p9-six-route/dd1-native-1-overlay` at **O=`d8f4dde0905db7156008a53173e3851768448b8d`**, tree `f116c8fd625a3469b6d6c80c6cb335161dc6b927`; implementation `b569bf7e58347e4c2090e4907f63f0a1521d2533`. Main remains **M=`07b5aa9dec8436132a524511d5438c510e322070`**. Retained research/capsule branch resolves to `20802e38713fd8bd9e425e807a89df482fcf4e89`. M→O is a clean three-commit descendant, not a merge of the research branch's older runtime. Preserve these branches and this single scientific candidate.

The actual new code includes the declared cards, nullable combat enemy reference, two-hit command, lifecycle hooks, both pool filters, localisation, presentation and focused native tests. Published `native-tests-1.log` is an actual author log ending `native_starts=30` and `PASS (1 tests)`, blob `cd79278a111ae043d61b00114b3c121adf194344`. This is useful native progress, not paper-only work. The issues below limit what it proves.

Use original D=`746388323b8e886967e0e13fe109490a70206a66` PROMOTION.md and accepted C=`cf27285a3103d8867a179f700c5ebf32661453d5` source-qualification/NATIVE-HANDOFF.md as the existing task specification. Bound H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6` stays immutable. These findings do not change the accepted law, cutoffs, source-role verdicts or closed families.

### R0 — reconcile the actual execution account before further native calls

At O, `research/p9-six-route/dusk-design-1-20260916/native-qualification/EVENT_LEDGER.json` (blob `18158491551a03b4a18de61ad013a6aee1a66132`) lists three 30-start test invocations: **90 recorded starts, attempt 1 consumed**. Preserve all three, including the initial fixture failure. Its CPU and active elapsed values are explicitly estimates; raw emissions are recorded as zero despite published native stdout. The headed attempt in `preview-venue.log` (exit 124, Metal renderer) is absent from those three events. Thus the listed 8102 starts is arithmetic on the recorded tests, not proof that all cumulative resource/execution debits are reconciled.

`CANDIDATE_PREFLIGHT.json` (blob `cb6e41897d08755fe0d63fe043a786c19912589f`) names M, design roles and a freeze timestamp, but does not bind exact executed overlay/test/content digests or a concrete seed/capture manifest. `preflight-ledger.txt` identifies HEAD M while the overlay was dirty. Those records alone do not establish exact pre-invocation source identity. This is not an allegation that no contemporaneous local records exist.

Recover any retained launch/process/time/source records, bind the actual invocations and count all applicable work, including retries and the headed launch. Separate actual measurements, defensible conservative bounds and UNKNOWN. Do not invent precision, report emitted bytes as zero, reconstruct missing history as a prospective freeze, or silently discard the first failed run. Reconcile against H's actual event/account semantics. If a required previous usage or namespace cannot be bounded adequately to establish remaining authority, stop affected native execution and report that exact gap; continue independent code/evidence preservation where possible. No second candidate, refund, top-up or fresh elapsed clock. Establish exact source/test/engine/command/input identities and enforced measurement/containment before the next permitted invocation. This is the same epoch and unchanged-law implementation repair, not a renewed scientific attempt.

### R1 — implement the bound interventions, not substitute cards labelled component-off

CANDIDATE_PREFLIGHT currently calls Strike versus Chisel `K1_P_off` and an ordinary attack/Eclipse Slash versus Resonant Lance `K1_C_off`. The focused `_k1_chisel_chip_and_subsets` test checks those different cards, not the stipulated controlled chain. Strike changes the base attack as well as the printed chip; Eclipse changes cost/status/law as well as the echo read. These are useful lawful substitution cases, NOT component masks preserving the same command.

Keep them as substitutions. Add the existing C:NATIVE-HANDOFF controls through a narrowly scoped research test seam: same paid Chisel with only its printed extra chip disabled; same paid `resonantLance` with only its special echo multiplier disabled, preserving ordinary Cracked scaling, hit, cost and lifecycle. Verify joint mediator and required whole-command/proper-subset cases under the already-bound definitions. For DD1 off-controls, preserve Set the Angle's Block and Crosscut's primary hit; merely omitting the producer/consumer is not the same intervention. Do not implement a new gameplay law or an arbitrary zero-payoff acceptance rule. R3 COMMAND_CONTRACT_FAIL remains closed.

`_add()` presently refills energy to at least 3 on each insertion. That is a fixture convenience, not proof of sequential affordability. Build the required hand and resources before a cost/control trace; preserve fixed initial resources during it, and test legal exhaustion/discount/denial explicitly. Constructed hands remain constructed evidence, never acquisitions. Qualify both required vows and independent upgrades where the original matrix requires them; `_fight()` currently fixes vow 0. No new thresholds or forced natural-run wins.

### R2 — repair state/null and preview correspondence to the accepted law

O `domain/rules/combat.gd`, blob `8110f15efe5119c5a0d82947b8ddd695ba17d66b`, `_apply_special`'s `crosscutAnchor` writes the marker for any aspect. The return function filters Ash only after the primary hit. Therefore in the explicitly required constructed other-aspect domain, Set the Angle can install a live public marker even though return is disabled. The current Ash test checks one hit only, not producer state. Preserve ordinary Block/hit on the null path, but require the new marker law to be absent on Ash; verify state and rendered marker, not just attack count. Pool exclusion is valuable but does not prove this separate off-domain requirement.

`_preview_crosscut_return()` computes the return from the initial live anchor/selected target without accounting for primary-hit consequences. Source-level counterexample to verify natively: live marked A and distinct living B; player HP 2/Block 0; B has Thorns 3 and survives the primary. Preview supplies a return, but the ordinary primary hit kills the player through Thorns, making `cb.over` true and suppressing that return. This is a constructed refinement counterexample, NOT a new natural-run result observed by this planner. Also cover the declared finale/death/collateral cases affecting return eligibility. Use the existing native command/preview architecture and public information; do not build another simulator or silently weaken the preview contract. Keep primary versus return/ordinary collateral distinct.

### R3 — make evidence checks falsify the claimed ordering and resolve real pointers

O `tests/support/dusk_native_export_reader.gd` (blob `799af6eb1b8e0df6437f097619649cf4ac262954`) defines `first_hit_index_before_chips` using ONLY the first hit and first chip. Source evaluation of `[hitEnemy(B), chip(B), hitEnemy(A), chip(A)]` returns 1. That is exactly the bad interleaving the Crosscut test claims to reject. The same weakness affects the three-hit Flurry assertion. I did not execute this as a native trace; it is a direct counterexample to the predicate.

Test the actual direct primary/return (or three Flurry hits) against the first card-level chip settlement, with explicit counts/order and a negative interleaving. Do NOT require every HIT_ENEMY to precede chips: legitimate Bell/other collateral can follow settlement. Add the already-required B-index-1/A-index-0, Bell-kills-anchor-before-its-chip-settlement case, terminal/Thorns, identity replacement and relevant independent lifecycle controls. Expected absence/size must fail explicitly; an `if hits.size() >= 2` body alone cannot establish that two hits occurred. Use narrow control/mutation failures and restore the actual implementation; no global runner rewrite or repetitions solely to enlarge test counts.

The reader also hardcodes `native_export/A/v0`, resets per-type ordinals on each call, and returns `['events','hitEnemy',seq]` / `['events','play',seq]` without publishing/resolving the corresponding byte-bound object. The test checks only that the pointer is a Dictionary with that role string. This is not yet H's content-bound trajectory/decision provenance. Its HP fallback can synthesize a predecessor from `hpAfter + amount`, and missing hpAfter defaults to zero.

Complete the narrow native capture/reader promised in the original task: preserve actual queues and necessary pre/post snapshots with exact source/candidate/invocation identities; retain true event ordering and unique trajectory/command paths; resolve each generated pointer against those captured bytes; reject missing/contradictory required HP fields instead of manufacturing physical loss. Exercise ordinary overkill (7 reported / 5 overkill / 2 HP), handoff and collateral distinctions at their actual native paths. Parameterise real context rather than asserting panel A/vow 0. This does NOT require fabrication of 2048-run arm arrays or a confirmation export; label N0 fixture capture as N0, and do not claim full D547-NATIVE-EXPORT-2 qualification or an authenticated receipt before it exists. Reuse H's boundary; do not edit H or create a new evidence framework.

### R4 — finish executable N0 obligations; distinguish genuine blockers from work not attempted

First make R0–R3 decisive; then complete unblocked parts of the existing PROMOTION/NATIVE-HANDOFF matrix in this same batch. Map every required row to actual evidence, a supported failure, or the smallest genuine prerequisite gap. The current v2 test creates its 'old' save with the overlay itself and manually restarts combat: retain that structural check, but do not call it a recovered pre-change save or the full application resume/abandon seam. Use an actual preserved M fixture and the relevant application boundary where the claim requires it. Run only changed-surface import/parse/content/locale/save regressions justified by this overlay, not the unrelated long layout suite.

**Visual venue clue already resolved from current-main source:** `docs/dev-tools.md` (blob `d8455fcfa29391313bec4cd918d2dbc7351794ed`) explicitly distinguishes Funplay editor MCP on port 8765 from the file-backed Native Proof runtime bridge. Native Proof does NOT depend on that editor port. `preview-venue.log` shows Metal started and timed out; it does not establish that all native capture routes are unavailable. The Godot skill directs the existing `tools/live.sh`/`tools/shot.sh` fallback. Use that existing native runtime route and authorised constructed scenario after account entry; inspect create/replace/consume/expire and eligible/suppressed-return presentation at relevant shapes. Do not install a new viewer, expose a network service, alter platform security, or treat source-string checks as visual proof. If this route actually fails, preserve its exact invocation/log and capability gap, rather than repeating the port-8765 probe. I have not inspected the present local machine or promised that the fallback will succeed.

Legitimate first-valid Dusk profiles for vows {0,5} are in the activated conditional preflight scope, not a new owner permission question. They may follow surviving law/control checks within the reconciled account, using the existing legitimate-source/exposure/first-valid prerequisites. 'Not run' by itself is not an unavailable prerequisite. Complete them if authorised inputs/capacity allow, otherwise name the exact missing input/authority and retain the work. Do NOT start natural acquisition-rate estimation, policy development, confirmation or a broad whole-run study to fill this gap. Independently authenticated extraction/guardrail receipts must still come from genuine qualifying evidence/issuers; the author must not self-approve them. Their absence does not excuse missing component captures or wrong interventions.

### Batch endpoint, ownership and preservation

Resume the existing sole LOCAL AUTHOR on the existing overlay; source Pro and reviewer remain parked. Do not recreate the accepted source proofs or Crosscut topology. No new owner activation is needed for these within-law repairs. Record corrections and input invalidation honestly before their dependent executions. If a genuine failed declared law/admission predicate is established, stop affected promotion instead of tuning or renaming it; an incorrect implementation/test is not automatically scientific futility.

Finish all unblocked repairs and required N0 evidence in one batch, then publish exact source, complete relevant outputs/captures, source-bound invocation/debit records, unresolved prerequisites and readback; update the one mutable capsule and give one consolidated #156 handoff. Preserve the original O submission and logs in history. Only the completed new exact candidate, with honest residual scope, goes to the required isolated reviewer. No formal independent verdict is issued in this planner pass.

D/C/H, original accepted source and numerical guards, real closures including Honing/R3/R14, UNKNOWN historical accounts, protected/exposed roots, M's campaign/save/ID guards and the ALLOCATION template stay unchanged. Do not merge the research tree into M or start a production PR, development, confirmation, policy fitting, #548 spending, #549 recovery, release or replacement topology. Source repairs and reviewer time do not reset the consumed attempt or cumulative epoch limits. Certificates remain 0/3. The immediate purpose is completing the already-authorised native implementation and trustworthy evidence, not adding another policy layer or asking the owner to coordinate the fixes.
```

---
## comment 5705518998
- url: https://github.com/fol2/glassvow/issues/542#issuecomment-5705518998
- user: fol2
- created_at: 2026-09-16T22:33:36Z

```
## DD1-NATIVE-1 — local-author continuation of 5705322630 (no new activation)

The sole local author continues the already-activated DD1-NATIVE-1 batch under current-main AI-SDLC and the four rules. This records [planner corrective batch 5705322630](https://github.com/fol2/glassvow/issues/542#issuecomment-5705322630) (R0–R4). It is not a new owner activation, not a new design, and not an independent review.

**Preserved:** D=`746388323b8e886967e0e13fe109490a70206a66`; C=`cf27285a3103d8867a179f700c5ebf32661453d5`; H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`; ALLOCATION template `824ff18a…`; consumed candidate attempt 1/1 (not reset). Original O=`d8f4dde0905db7156008a53173e3851768448b8d` stays in history.

**Sequence:** R0 account reconciliation before any further native invocation; then R1–R3 fixed-law repairs on the same candidate; then unblocked N0 evidence; then one #156 exact-head handoff with GitHub readback.

**Forbidden:** new topology; development (N1); confirmation (N2); self-approval; production PR; main mutation; merge; release; #548 spend; resetting the attempt or epoch clock.

```

---
## comment 5705519115
- url: https://github.com/fol2/glassvow/issues/421#issuecomment-5705519115
- user: fol2
- created_at: 2026-09-16T22:33:36Z

```
## DD1-NATIVE-1 — continuation recorded (5705322630; no new activation)

Owner activation remains [5704631815](https://github.com/fol2/glassvow/issues/421#issuecomment-5704631815) of [5704445133](https://github.com/fol2/glassvow/issues/421#issuecomment-5704445133). The sole local author now executes [planner R0–R4 batch 5705322630](https://github.com/fol2/glassvow/issues/542#issuecomment-5705322630) on the existing overlay. R0 account reconciliation precedes further native calls. D/C/H and the consumed attempt are preserved. No development, confirmation, self-approval, merge or release.

```

---
## comment 5704631815
- url: https://github.com/fol2/glassvow/issues/421#issuecomment-5704631815
- user: fol2
- created_at: 2026-09-16T21:16:37Z

```
## DD1-NATIVE-1 — owner scope authorisation recorded (2026-09-16)

The owner has explicitly authorised the prepared native implementation/refinement batch in [5704445133](https://github.com/fol2/glassvow/issues/421#issuecomment-5704445133) in the current direct instruction:

> I authorise DD1-NATIVE-1 in #421 comment 5704445133, including only its existing preflight allocation and conditional scope.

**ACTIVE for this bounded author batch only:** one sole local author under current-main AI-SDLC (`AGENTS.md`, `docs/agents/ai-sdlc.md`, `tools/ci_scope.py` from live `origin/main`) and the four rules (AI-native SDLC DNA; minimum wall time / maximum effectiveness; minimum model-token and compute; no compromise). Scope is the comment’s existing H preflight allocation and conditional N0 implementation/refinement only. Existing research branch `research/p9-six-route-local-20260905` and one current task capsule are retained. Other research/reviewer writers remain parked. Checkpoints are saves, not stopping points.

**Fixed inputs:** scientific base **M=`07b5aa9dec8436132a524511d5438c510e322070`**; accepted design **D=`746388323b8e886967e0e13fe109490a70206a66`**; accepted source **C=`cf27285a3103d8867a179f700c5ebf32661453d5`**; bound **H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`** / #547 prefix. Roles remain shipping `chisel` → coupled target threshold/Stun/Cracked → shipping `resonantLance`; shipping `empower` → additive persistent Strength → THREE-hit `flurry`; unchanged declared `setTheAngle`/`crosscut` law. The research branch’s older runtime is not merged into main.

**Unchanged:** three independently usable Duskblade strategies into the production/TestFlight line before Ashwarden; D547-PC1 acceptance; binding `D547-PC1-20260914-BIND-01`; immutable zero-spend ALLOCATION template; historical UNKNOWN/spent balances; A1/A2 remain 0/3 Duskblade certificates. Do not repeat K1 correction, accepted source comparisons, DD1 design review, or #547.

**Existing account only:** preflight cap 8,192 native starts, less every actual applicable debit; one candidate attempt consumed at the first new candidate-native invocation. No new allocation, reset, or transfer. Epoch ceilings remain 64 CPU-hours, 48 cumulative active elapsed hours, 64 GiB raw, at most two native executors, 300 CPU-seconds per invocation.

**Forbidden in this batch:** tuning; replacement topology; extra ordinary damage beside Crosscut special; development (N1); confirmation freeze (N2); policy fitting; certificates; alpha commitment; protected-cohort exposure; production PR/CI; main mutation; merge; release; self-approval; #548 spend; #549 reconstruction. A handoff ready for later isolated review is not a native certificate.

The sole local author is proceeding through the prepared native implementation and refinement sequence and will publish complete overlay/source/evidence plus one #156 handoff with GitHub readback. This comment records scope activation, not completion, not a scientific PASS, and not a native certificate.

```

---
## comment 5704445133
- url: https://github.com/fol2/glassvow/issues/421#issuecomment-5704445133
- user: fol2
- created_at: 2026-09-16T21:01:04Z

```
## DD1-NATIVE-1 — prepared native implementation/refinement batch (PROPOSED, NOT ACTIVATED)

The corrected source gate is complete: [independent APPROVE 5703689482](https://github.com/fol2/glassvow/issues/542#issuecomment-5703689482) and [planner acceptance 5704414261](https://github.com/fol2/glassvow/issues/542#issuecomment-5704414261) at **C=`cf27285a3103d8867a179f700c5ebf32661453d5`**. Do not repeat K1 correction, accepted source comparisons, DD1 design review or #547.

**Recommended next outcome:** one reproducible, unmerged native implementation of the fixed three-role proposal, with the decisive native refinement evidence and an honest preflight disposition. Not another paper model, strategy search or production release. This task is prepared now; it is not an inference that an APPROVE grants native authority.

### Activation and sole worker

The prior owner activation 5693811899 expressly limited the work to design/source and forbade native calls. **An explicit owner instruction authorising DD1-NATIVE-1 activates this proposal, including only the applicable existing preflight account and the conditional transitions below.** The local author records that instruction before execution and proceeds without requesting a second planner receipt. Merely sharing an old review, this proposed task, or a generic status check does not activate it.

Use the existing local AUTHOR environment that has demonstrated Git and supported Godot execution. Verify current readiness rather than assume it. The Pro source author and completed reviewer stay parked. The author finishes this entire author-side batch; the subsequent required independent review occupies the same worker slot sequentially, not mid-run or as author self-approval.

### Fixed inputs, not new design choices

- Scientific base **M=`07b5aa9dec8436132a524511d5438c510e322070`**. Refresh operating instructions from current main; reconcile any material later dependency change explicitly without rebasing frozen science.
- Accepted design **D=`746388323b8e886967e0e13fe109490a70206a66`**, `research/p9-six-route/dusk-design-1-20260916/DECLARATION.json`, `DESIGN.md`, **`PROMOTION.md`** (blob `ba9fc8004f385958b064598b6fa54dc9634cf577`).
- Accepted source correction **C**, `research/p9-six-route/dusk-design-1-20260916/source-qualification/PROOF.md` and **`NATIVE-HANDOFF.md`** (blob `df007ee4a1032e57f3bcd90de98a6c0c1dca8fe2`). The latter is the already-reviewed implementation/qualification delta; do not write another competing specification.
- Bound **H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`**, `research/p9-six-route/duskblade-first-proof-20260912/`: CONTRACT, SPECIFICATION, ADAPTER_RECORDS, kernel and allocation remain immutable. External binding 5683984558 / D547-PC1-20260914-BIND-01 stands.
- Roles are fixed: shipping `chisel` -> coupled target threshold/Stun/Cracked -> `resonantLance`; shipping `empower` -> additive persistent player Strength -> THREE-hit `flurry`; unchanged declared `setTheAngle`/`crosscut` law. No new coefficients, role replacement or extra topology.

The native implementation is an experimental overlay on a clean M-based worktree, **not a merge of the research branch's old runtime**. Keep the existing research writer/branch and one capsule. Publish the complete overlay/patch or actual candidate source, exact base and reconstructed tree identity, tests and evidence through the permitted existing research channel. No replacement programme/ticket, production PR/CI, main mutation, merge or release belongs here. Selected integration and its full production gates remain downstream.

### Complete finite author sequence

**1. Establish the entry, once.** Confirm provisioned stable Godot, Git, capture/publication and resource-account surfaces. Recover existing valid source/exposure manifests; do not repeat unchanged failed capability probes or access protected raw. Before any candidate-native invocation, establish H's CANDIDATE_PREFLIGHT manifest: exact product overlay/source and three role definitions; profile prerequisite/first-valid witness plan; relevant fixed roster/command policy; intervention and null table; explicit required assertions with their existing authority; allowed input/exposure namespace; debit events and finite stopping graph. Freeze the applicable inputs before their outcomes. The actual post-development confirmation freeze is NOT a prerequisite for building its own preflight evidence.

Missing authority, unknown applicable exposure namespace or unreconciled current-account usage blocks the affected native action. Do not turn a named file, boolean or this proposed task into a trusted receipt. Complete independent code/publication work where possible, but never label a missing entry prerequisite as passed.

**2. Implement the selected native law, not another interpreter.** Apply PROMOTION §1 exactly: the two declared cards/effects; one nullable current-combat enemy reference; consume before primary and revalidate the identical living object before return; both ordinary hits before native insertion-ordered one-card chip settlement; all expiry/death/terminal/abandon/resume boundaries; Dusk-only pool filtering on BOTH insertion paths; state-derived marker/preview and existing localisation conventions. Preserve current costs, counters, native callbacks, Stun/Shatter coupling, v2 save/ID validation and M's campaign repairs. K1/K2's shipping card laws remain unchanged. No extra ordinary damage effect alongside the Crosscut special.

**3. Execute the cheapest decisive native refinement first.** Use the existing Game.apply/test seams and the exact matrices in PROMOTION §3 plus C:NATIVE-HANDOFF, not a new balance simulator. Before large profile or export work, test native ordering, costs, legality, masks and the material whole-command/proper-subset obligations. Keep K1's disjunctive guard `staggered OR vulnerable>0` explicit, including separately observable cases where available; do not generalise the symbolic grid's Cracked proxy. Keep direct-Cracked/ordinary-chip substitution and nonzero disabled-echo damage visible. Do not rerun R3's known-false zero proposition or select new acceptance after native outputs. A discovered mismatch cannot be repaired by changing the declared gameplay law or threshold.

Where mandatory applicability or an existing predicate cannot be instantiated unambiguously, report the exact clause and missing decision before the affected run; a favourable deterministic cell is not a new native causal-admission standard. A decisive genuine failed-law/guard ends dependent promotion; retain independently useful evidence, not an unconditional demand to build the rest of a doomed candidate.

**4. Finish the surviving native surfaces in the same batch.** Complete the relevant three-role mixed/substitution/target/reset/collateral checks; absent/off and excluded-aspect comparisons; both pool paths; legacy/new-ID save and pending-combat reconstruction; and actual marker/preview runtime captures. Visual source presence is not visual acceptance. Complete the first-valid legitimate Dusk profile witnesses for vows {0,5} only where entry authority and remaining preflight capacity allow; constructed test hands are not those witnesses or natural acquisition rates. Preserve the signed B behaviour and the distinction from balance_sim.gd's all-revealed three-act fixture. No policy fitting or development panel is opened.

Extend only the necessary native snapshot/event reader seams toward H's **D547-NATIVE-EXPORT-2**. Bind actual ordered source pointers, terminal/legality/intervention context, target/copy/epoch identities, acquisitions and work records; distinguish ordinary HIT_ENEMY.amount, overkill, physical HP, return and generic collateral. Do not populate an empirical bundle with the Python grid, claimed booleans, invented trajectories or synthetic model labels. Capture issuer/reader qualification evidence for review; the author does not manufacture an independent guardrail receipt. Missing external authentication remains a downstream gate, not a reason to rewrite the bound adapter.

**5. Deliver one complete native author package or one evidenced terminal disposition.** Preserve actual source, deterministic reproduction, full relevant stdout/stderr/exit statuses, native traces/captures, record identities, account debits, profile witnesses or their exact gaps, and one #156 handoff with GitHub readback. Keep native fixture evidence, real predecessor runs and future statistical rows distinctly labelled. Report implemented-and-verified obligations versus unresolved/failed obligations. No source-only PASS is reused as native proof. A handoff ready for review is not a native certificate; source/extractor tests do not prove policy quality or sufficient acquisition.

The author endpoint is the coherent exact candidate plus all executable preflight obligations and a prepared neutral review input. No second reviewer can be simulated inside the author context. The next isolated review evaluates this new native artifact and evidence, not unchanged source history. Checkpoints are saves; continue all unblocked, decision-relevant sections without an arbitrary time stop or a routine request for continuation.

### Existing account and hard stop boundaries

This proposal grants **no new allocation, reset or transfer**. Upon activation, H SPECIFICATION §6 governs: preflight cap **8,192 native starts**, less every actual applicable debit; one fixed candidate attempt consumed at its **first new candidate-native invocation**. Preserve the immutable zero-spend template; use its external binding and a reconciled runtime event ledger for this same epoch, not a new budget account. Historical UNKNOWN balances remain UNKNOWN. An actual retry costs another start and CPU, never another independent sample. Delivery repairs satisfying the unchanged law follow current-main rules with exact input invalidation and charged reruns; they do not authorise a second scientific candidate or a budget reset.

The epoch-wide caps remain **64 CPU-hours, 48 cumulative active elapsed hours, 64 GiB raw emissions, at most two native executors, 300 CPU-seconds per invocation**, and all stricter existing per-decision/gameplay limits. These are ceilings, not targets or promised runtime. This batch cannot borrow development/confirmation/counterfactual subaccounts or #548 reserve. **No development, policy fitting, reference/route confirmation, alpha commitment, protected cohort exposure, production integration or release** is activated. Stop before an action cannot fit its remaining authorised cap; publish the actual residual, not a top-up request disguised as a new task.

Research uses the focused native evidence needed for these claims; it does not replay the long unrelated full Godot suite to fill time. The complete local core and selected CI/visual gates required for eventual production delivery are not waived. Preserve all source/closure decisions, numerical guards, R3/R14 and R11/R12 scope, current-main campaign/ID/save invariants and platform refusals.

**Present state:** proposed task only; native account unactivated by this comment; no new source, native result, spend or certificate. The product goal is still three Duskblade strategies into production/TestFlight before Ashwarden.
```
