# DD1-NATIVE-1 N0 after 5705322630 (R0–R4)

Same candidate. Attempt 1/1 not reset. D/C/H/ALLOCATION unchanged.

## R0
Reconciled before further native calls, then this batch added catalog tests and Native Proof. Used 231/8192 starts (remaining 7961). CPU/elapsed UNKNOWN with conservative bounds under epoch caps. Headed Metal debit included. Attempt 1/1 not reset.

## Implemented and verified (`GlassvowGame.apply`)

R1 same-command `research_mask_printed_chip` / `research_mask_echo` / anchor / return; Strike/Eclipse labelled substitutions; sequential energy without refill; vow 5.

R2 Ash does not install the marker; Block retained; Thorns-lethal primary suppresses return in preview and apply; finale handoff suppresses return.

R3 ordering predicate fails on `[hit,chip,hit,chip]`; Crosscut two hits before first chip; B idx 1 / A idx 0; Bell kills A before A's chip; pointers resolve against captured bytes; missing HP rejected; overkill 7/5/2 physical 2; M fixture via SaveService.load_run.

Two identical PASS runs `native_starts=44`. Catalog wiring test PASS twice (identical blob `90c42859…`). Native Proof `tools/shot.sh` / `tools/live.sh` inspected:

- Card lab after catalog repair: UNCOMMON — 2, Crosscut and Set the Angle (bare-pane art; names/text/cost visible). Prior 0-card PNG retained as SHOT-1/3.
- Constructed `combat-normal` scenario with `add_cards`: opening hand includes both overlay cards.
- live.sh create: Set the Angle targeting preview; after play, **MARKED** on the aimed sporeling, energy 3→2, Ward 4, producer discarded.
- live.sh consume: Crosscut aimed at the unmarked sporeling; both at 9/14 (primary 5 + return 5); MARKED gone; energy 1; discard 2.

Replace/expiry remain unit/preview (not restaged on the live host). Marker source still exists in EnemyView/combat_screen.

## Residuals (UNKNOWN / not passed, not certificates)

- First-valid legitimate Dusk P_v for vows {0,5}: no recoverable overlay-bound witnessed ledger; not constructed as a whole-run study. Constructed scenario hands are not those witnesses.
- Natural acquisition rates: not run (forbidden as rate study).
- Extraction/guardrail receipts: absent, not manufactured.
- Overlay card art files `assets/art/cards/{setTheAngle,crosscut}.jpg` are absent; CardView documented bare-pane fallback. Display catalogue count 63.
- N1/N2 / 2048-run D547-NATIVE-EXPORT-2: not opened.

**0/3 certificates.** Handoff is for isolated review, not self-approval.
