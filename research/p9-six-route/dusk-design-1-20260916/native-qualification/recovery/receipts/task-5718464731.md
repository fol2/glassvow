## DD1-N0-RECOVERY-1 — transition 2 sole-author batch

**Commissioned now by external binding [5718448223](https://github.com/fol2/glassvow/issues/421#issuecomment-5718448223), DD1-N0-RECOVERY-1-BIND-01.** Resume the existing local AUTHOR. Keep the completed amendment reviewer, source Pro and other writers parked. No additional owner activation, repeated amendment review or N0 acceptance review is needed before this authorised author work.

The task contract is exactly [option A / 5717964158](https://github.com/fol2/glassvow/issues/421#issuecomment-5717964158), SHA-256 `92b78c7a93ddf5f46cc4ff1fe77f42729223a9814fc59241f8cccddade06b7df`, selected in 5718178514 and independently approved in 5718202450. The binding supplies their complete verified identities. This comment schedules its existing requirements; it adds no new law, cohort, threshold or resource allowance.

### Start and preserve

Resume overlay `research/p9-six-route/dd1-native-1-overlay` from actual live G=`84cd143f44923294e614def62de74e720598648a` (tree `bc9850d151c20a4395ce8138fb381b1d4ad9604d` when checked). Scientific M remains `07b5aa9dec8436132a524511d5438c510e322070`. Refresh current-main AGENTS.md / AI-SDLC / relevant Godot skill without rebasing frozen science or importing the research branch's older runtime. Keep the existing research branch and capsule; record the binding/task there as part of normal author delivery.

Preserve accepted D/C/H, H's ALLOCATION template, current fixed gameplay law and controls, original INPUTS/SESSION-STATE, all previous reviews and the old account/exposures. G's reader changes and Main scaffold are starting source, not already-tested recovery code. Old F summaries and constructed pending bytes remain inadmissible history for ordinary-path proof.

### Complete the connected batch, not just the launch switch

**1. Receipt-bound launch and real containment.** Initialise a distinctly named recovery event account referring to the owner, reviewed method and external binding, alongside—not in place of—the old ledger. Validate the exact receipt bodies, operation, source/driver/input identities, reservations and expiry. File existence or an environment variable alone cannot authorise a launch. Keep fetched receipts/exact manifests recoverable without credentials.

Implement the smallest existing-tool-based wrapper needed to count starts, account complete process-tree CPU, capture cumulative raw output and enforce the reviewed limits. Before any game call, prove the wrapper with inert non-game controls: exhausted reservation, wrong/stale identity, child-process CPU limit, output limit, and interruption cannot proceed as successful execution or silently reset accounting. Preserve actual control outputs. Count engine process overhead and contained combat/forward-simulation starts exactly as the protocol requires; store pre-launch inputs before outputs and retain failures/retries. Do not call an unmetered Godot parser/import/fixture a free preflight.

The old UNKNOWN CPU/elapsed/raw record no longer blocks this separately authorised operation by itself. Do not spend another round trying to reconstruct it as a prerequisite for the new allowance. Conversely, missing NEW metering/containment/protection/venue evidence still prevents the affected launch. No estimate or post-hoc declaration replaces that proof.

**2. Finish the existing reader, application driver and evidence verifier.** Retain mandatory digest/no-drop/exact-integer/null-versus-empty reader fixes; verify their actual entry-point mutation/restoration behaviour and typed callers. Complete once-only combat-result dispatch, pending rewards and route advancement through Main, with real scene lifecycle and legal choices. Use the unmodified Pilot selector and bind the complete public-choice mapping before acquisition; no fitting or outcome-dependent policy improvement. Capture actual commands/events, RNG/map/UID state, saves and terminal receipts instead of returning only status/counts. Incomplete execution remains INCOMPLETE.

Separate explicit acquisition from ordinary regression. Negative fixtures can establish that missing evidence is rejected, but the N0 acceptance path must accept a valid retained witness when supplied and reject/block its absence. It must not assert that genuine witness files must remain absent, automatically replay campaigns or overwrite canonical evidence. The reviewed protocol's five source-completion items are the exact worklist; do not substitute another partial campaign implementation or general evidence platform.

After the relevant source and launch conditions hold, execute the narrow metered reader/driver/route tests before profile campaigns. Preserve meaningful state/command evidence and real failure propagation, not just a test-count banner. A source correction to satisfy the unchanged law may continue within this recovery's finite allowance with truthful invalidation and charged verification.

**3. Ordinary-M pending capture and actual overlay resume.** Use unchanged M's map-selection/encounter-arm/save route and retained capture-driver source. The permitted supporting fixture seed is 5420099. Archive the exact emitted run/Vigil bytes before normal application cleanup; load the same bytes on the overlay through Main's actual continuation/resume path. Check pre-resume identity and the legitimate post-resume changes, including marker absence. Do not relabel the old injected pending file as this witness. Establish isolated storage before application load; do not overwrite the owner's saves.

**4. Fixed first-valid predecessor recovery.** Execute only exposed roots 5421600–5421615 in ascending order from the genuine blank Dusk ledger, at `min(vow_unlocked,4)`, under the protocol's complete frozen prerequisite and public-choice definitions. Preserve initial and ordered transition/save/commit bytes. Archive P_0 at its first durable valid ledger; continue only as authorised to first valid P_5, fixed-sequence end or a hard stop. Do not mutate archived P_0 with later unlocks. Do not append seeds, force wins/deaths, inject cards/rewards, retune the pilot, or optimise against remembered outcomes. These are reconstruction inputs, not fresh statistical data. Preserve all actual unsuccessful/incomplete attempts and debits.

### Limits and completion

The approved recovery ceilings are 2048 conservatively counted starts, 4 process-tree CPU-hours, 1 GiB cumulative raw before compression, one native executor, 300 CPU-seconds per invocation plus stricter applicable limits. The seven-day clock begins at the first recovery ENGINE launch; record that actual timestamp and deadline, and do not reset on a session or reviewer change. Bounds are not work targets. Old listed 1277 starts / attempt 1 of 1 / operational UNKNOWNs stay visible; the 6915 arithmetic remainder is not this operation's funding.

Checkpoints are saves. Complete the reader, metering, driver, pending-compatibility and profile work together wherever their prerequisites hold. Do not return merely after making a launch-permitted file, passing a meter self-test, obtaining P_0, or saving one section while independent authorised work remains. Never continue past a real cap, protection conflict, unavailable required evidence channel or failed fixed-law obligation.

Publish actual source and focused verification, immutable pre-launch inputs, process/output/debit evidence, pending-save and P_v transition bytes, their validation/results, dependency-scoped reuse/invalidation, and one nine-bullet response map to review 5712464645. Preserve complete required artifacts on GitHub through permitted channels and read back their exact identities; local-only archives and hashes without content are not delivery. Update the existing capsule and return one consolidated #156 handoff with final head/tree, tested-code identity, complete remaining-account state and clear disposition.

Only a genuinely complete candidate proceeds to the normal fresh exact-head N0 review. An incomplete fixed replay or exhausted/unavailable prerequisite gets a precise terminal/BLOCKED handoff after independent work is preserved, not an advertised N0 completion or a request for automatic top-up. The author does not self-approve or invoke another reviewer mid-batch.

**N0 remains unaccepted until that later review. No N1/N2, statistical alpha, #548, rates/quality study, replacement topology, production PR/CI, merge or release.** Recovery profiles remain in their separate provenance class; later statistical use needs the protocol's actual authority mapping. Existing accepted design/source/#547 work and downstream art/live-restaging/extraction-receipt boundaries are unchanged.