## DD1-N0-RECOVERY-1 — proposed owner decision and bounded recovery protocol

**PROPOSED / NOT ACTIVATED. No new native execution, spending, cohort permission or N0 acceptance is granted by publication.** The planner recommends option A below. This is an explicit operational/provenance exception requiring owner selection and prospective independent semantic review, not an inherited allowance, a retrospective approval of bad evidence, or a rewrite of H's scientific tests.

### Verified state and why another unchanged continuation is not the next task

Author handoffs [#542/5717533180](https://github.com/fol2/glassvow/issues/542#issuecomment-5717533180) and [#156/5717533373](https://github.com/fol2/glassvow/issues/156#issuecomment-5717533373) identify G=`84cd143f44923294e614def62de74e720598648a`, tree `bc9850d151c20a4395ce8138fb381b1d4ad9604d`, on retained `research/p9-six-route/dd1-native-1-overlay`, parent F=`3993b3f0a98ef436ed144ba800e7e031583baa0a`. Live refs match. Scientific main remains M=`07b5aa9dec8436132a524511d5438c510e322070`.

G delivers reader changes, tests and a Main-route scaffold without native execution. It explicitly retains N0 BLOCKED. This is correctly limited delivery, not a runtime-tested repair or complete provenance producer. Review 5712464645 remains the last independent native verdict, for F; do not apply it as a new verdict on G.

H SPECIFICATION §6 binds one epoch, cumulative limits and no automatic new post-outcome cohort. The earlier 8-to-16 procedure is not validly authorised by the old task. The listed historical count is 1277 starts, attempt 1/1 consumed; cumulative CPU/elapsed/raw remain UNKNOWN. No supplied evidence establishes a spendable remaining account, an actual overrun, or universal impossibility of the strategies. Another instruction to recover the same absent records or rerun the same acceptance review will not itself resolve those boundaries.

### Owner decision

**A — recommended:** authorise this one separately accounted, prospective N0 recovery operation for the same accepted gameplay design and same three roles. It permits new engineering/provenance execution under the explicit limits and fixed replay procedure below, despite the old account's unresolved operational remainder. Prior records remain charged, exposed and uncertain where uncertain. It is not a refund, retrospective clock correction or a claim that the old H epoch complied.

**B — keep existing authority unchanged:** retain the native pause. Existing source work is recoverable. Native continuation then needs actual evidence clearing the original account/procedure, not a generic relaunch. This option is not a scientific rejection or closure of #542.

The user need not choose numerical thresholds, scripts or variants. The decision is whether to permit the defined recovery exception. No owner selection is inferred from merely reporting G.

### Exact scope of option A

Keep D=`746388323b8e886967e0e13fe109490a70206a66`, C=`cf27285a3103d8867a179f700c5ebf32661453d5` and bound H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6` immutable, including H ALLOCATION blob `824ff18acbca2f776533beed83a43b21d9a50f2c`. Record the exception externally with the exact owner and review receipts; do not change H's status/header, thresholds, kernel, sample sizes, statistical error account or native-evidence validity rules. Candidate attempt remains recorded as 1/1; the new recovery operation is separately identified, not a second strategy nomination concealed by a new hash.

The exception covers only: reader/regression completion; correct application-driver implementation and its focused tests; one genuine unchanged-M pending-save capture plus overlay application resume; and first-valid ordinary Dusk P_0/P_5 provenance under the fixed replay below. It does not cover game-law tuning, alternate packages, rates/quality estimates, policy optimisation, N1 development, N2 confirmation, #548, production PR/CI, integration, merge or release.

A recovered P_v would establish the ordinary-path witness required by H §2, not frequency or independent confirmation. Recovery outputs remain in their own identified provenance class. They cannot be injected into the frozen H statistical packet by bypassing its account/authority checks. N1/N2 retain separate, unresolved entry/account authority; this exception does not revive the original statistical epoch or claim it can still fit. Any later use needs its actual authority mapping and required review before outcomes.

### New recovery allowance — ceilings, not estimates or work targets

| Resource | New recovery maximum |
|---|---:|
| Conservatively counted native/engine starts | 2048 |
| CPU, summed over the complete native/controller process tree | 4 CPU-hours |
| Emitted raw data, before compression and including retries/diagnostics | 1 GiB cumulative |
| Calendar window | 7 continuous days from first recovery engine launch; no restart or review-pause reset |
| Concurrent native executors | 1 |
| Per-invocation CPU | 300 CPU-seconds, with any stricter existing action/turn limits retained |

This is **additional explicitly authorised recovery capacity**, not 6915 presumed unspent H starts. Keep both accounts visible: historical listed starts 1277 and UNKNOWN operational usage; recovery starts/CPU/raw/time beginning at zero only for this new operation. Never describe their sum as proof the original epoch satisfied its ceilings.

For this recovery, debit conservatively before each engine process and each native combat/forward-simulation start; counting both process overhead and contained starts is deliberate. No hidden rollout or capture is free. Reserve the impending unit before executing it and stop before exceeding the cap. Time/output containment covers children, stderr and intermediate raw, not just saved final logs. Prove containment with inert non-game controls before the first game call; use existing process tools plus the smallest necessary wrapper, not a new platform. Failure to measure, retain or enforce a required bound prevents the launch or stops the operation. A final summary asserting a bound is insufficient. All failures and retries remain charged.

These finite ceilings are planner-proposed resource choices, not demonstrated workload predictions. They do not guarantee that P_5 or N0 will succeed. At exhaustion, preserve the terminal result and stop; no automatic second recovery or quota top-up.

### Fixed predecessor replay — intentionally exposed inputs, no fresh-sample claim

The sole permitted candidate-profile root order is the already exposed sequence **5421600 through 5421615 inclusive, ascending**. This is a newly authorised reconstruction procedure, declared before its corrected Main-route outputs, not retroactive ratification of the old enlarged search. Do not append roots, reorder towards known wins, change the controller after results, or start from one of the invalid old summary profiles.

Start from the actual ordinary blank Dusk Vigil and retain its bytes. Before acquisition, bind the complete required unlock/access set from unchanged K1/K2/DD1, exact controller and public-choice mapping, product/driver/engine identities and output schema. Use the existing unmodified Pilot policy as a legal action/choice selector with its exact identity and no fitting; establish its applicable eligibility and information boundary. Act only through legal production choices, including real offers/prices and terminal handling. The driver cannot grant cards, resources, outcomes or unlocks. No R14 closed stock-family capacity trial, closed-family experiment or signed B-control substitution is authorised.

For progression, use the highest genuinely unlocked vow up to 4 (`min(vow_unlocked,4)`) to earn access to 5; do not inject unlocked access. After each genuine application terminal commit, retain the full run/Vigil transition and evaluate the frozen prerequisite set. Archive P_0 at its first durable qualifying ledger; continue only until the first durable P_5 ledger or the end of the fixed sequence/any hard stop. Later progression must not mutate the archived P_0. Required incidental unlocks earned by normal play remain; no optional further unlock hunting once both requirements hold.

An unfinished fight, unknown route or containment stop is INCOMPLETE, never a death/win. No profile credit from an incomplete run. A genuine death follows the application’s normal commit and may advance to the next fixed root. A controller/driver bug may be corrected within the unchanged law and resource cap, with exact invalidation and correctly charged deterministic replay of the same assignments; no new roots or outcome-conditioned policy improvement. Preserve every actual attempt and do not count replays as independent evidence.

All 16 inputs are explicitly **exposed preflight reconstruction inputs** and remain excluded from any fresh development/confirmation claims. Check their effective 32-bit identities and the applicable protected/reopening restrictions; this exception does not authorise protected 3000–5399 or unknown protected namespaces. If an actual protection conflict cannot be resolved, stop rather than substitute another seed. The separate supporting unchanged-M pending-save capture may reuse exposed fixture seed 5420099 through a real map route; it is not a new candidate or a P_v witness. Existing fixed component-test inputs may be reused for code verification only with their scope and costs recorded.

### Source-completion instructions; G is not yet an executable recovery producer

The planner inspected G's `tests/support/dd1_native_main_route.gd` (blob `39b4c84894ace0ec9fc4842b228ace0956d8f8f6`) and `tests/test_dd1_native_matrix.gd` (blob `a36bf882642e3a5319741b8fe9df638aaf29e768`). These observations are source-derived, not independent native findings:

1. `launch_permitted()` currently tests only FileAccess.file_exists. A present empty/stale file is not permission. Parse and validate the actual owner/recovery-method-review receipts, exact operation/source/input identities, expiry and remaining reservations before spawning or accepting acquisition. Missing/mismatched state rejects. Environment variables are opt-in, not authority. Keep internal integrity distinct from external receipts; do not manufacture approvals.
2. `drive_ordinary` checks `cb.over` before pending rewards and calls `_on_combat_over` on every visit. M's handler creates/persists rewards and is not a generic idempotent polling method. Dispatch each completed combat once, then service the actual application state/choice route; verify no repeated reward generation or RNG advancement and that the next map node is reached. Do not fix this by merely reordering two tests while leaving stale combat dispatch elsewhere. Correct SceneTree lifecycle, readiness and result signal handling are required; wrappers may not replace game/quest/save truth.
3. `_pv_acquire_via_main` currently attempts one hardcoded root and then fails unconditionally; the route helper returns only status/counts. Complete the explicit acquisition entry and durable trace producer. It must retain initial state, legal input commands and native observations, source identities, RNG/map/UID state, actual save bytes and commit receipts, and the frozen first-valid P_0/P_5 files before cleanup. No outcome labels alone or fabricated post-terminal save.
4. Current regression predicates expect the ordinary pending witness and durable profiles to be absent. Keep negative fixtures explicitly negative, but make the N0 acceptance path verify valid retained artifacts when supplied. Missing or unsupported evidence yields BLOCKED/REJECT, not success and not an unconditional assertion that prevents a future genuine witness from passing. Routine regression must remain read-only with respect to acquisition artifacts.
5. Produce the pending witness with unchanged M's real map selection/_prepare_encounter/SaveService path and retained driver source. Load identical emitted bytes on the overlay through Main._continue_run/_resume_pending_combat; compare the correct pre-resume identities and legitimate post-resume effects. Do not reuse the old constructed pending as ordinary-route proof. Use an isolated store established before application load; never overwrite the owner's saves.

Preserve the reader's required digest/no-drop/exact-integer/null-versus-empty changes and finish its actual runtime verification with single-fault/restoration controls, including malformed caller input. Preserve already useful K1/collateral/source/live-capture findings by exact dependency scope. No generic re-review of D/C/H or repetition of unchanged historical suites. Final art, live replace/expiry restaging and separate external extraction/guardrail receipts retain their existing downstream status. Crosscut text clipping remains open.

### Explicit transitions and one worker at a time

0. **Now:** no owner activation; no new native call. Preserve G and the blocked author handoff. No N0 acceptance reviewer is commissioned.
1. **Owner selects A:** record the exact instruction on #421. A fresh local reviewer then reviews this recovery amendment only, in the repository's isolated/read-only arrangement, against H §2/§6 and the accepted D/C boundaries. This is prospective review of a changed procedure/account, not a predictable acceptance review of incomplete N0. No Godot or native outputs. Return one scoped verdict and concrete findings on #421, identifying the exact proposal comment/body and owner receipt. Authors remain parked.
2. **Recovery amendment approved and bound externally:** the same sole local author completes the source fixes, implements/validates metering, records immutable pre-launch inputs and starts only the allowed recovery work. Acceptance of this method does not pre-approve G's code. Narrow engine/component tests precede campaign acquisition; then complete all lawful independent/conditional N0 evidence in one author batch. Checkpoints are saves. No routine new owner decision is needed inside this finite graph.
3. **Verified complete recovery candidate:** preserve all source, execution/capture/save bytes, accounting, disposition and one #156 exact-head handoff with GitHub readback. One normal fresh exact-head N0 review follows. A still-missing mandatory condition produces a precise blocked/terminal handoff, not another advertised N0 completion. No merge, certificate increment or N1/N2 follows automatically.

If prospective review requests a substantive change to this exception, prepare the exact amendment before executing it; do not silently reinterpret the owner’s permission. If any scientifically decisive fixed-law obligation actually fails, retain that failure and stop its dependent path rather than use recovery to tune it away.

### Decision boundary and preservation

This proposal gives the owner a genuine resource/procedure decision, not routine coordination. It explicitly breaks the deadlock of needing irrecoverable old operational measurements before any correctly metered recovery while preserving their uncertainty as history. It also avoids pretending that repeated profile summaries are legal gameplay provenance. The limits and source work above are not a promise of N0 success.

No changes to source branches, runtime accounts, H/D/C, thresholds, protected records, original reviews, main, production PRs or certificates were made by this proposal. #547 remains complete; #421/#542 remain open; Duskblade remains 0/3. The three-strategy Duskblade-first production/TestFlight goal, Ashwarden order, #548/#549/#543/#541/#108 responsibilities and all non-amended invariants remain.