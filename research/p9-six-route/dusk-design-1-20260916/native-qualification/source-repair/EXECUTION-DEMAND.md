# Dormant execution-demand map — not authority or funding

Applies to implementation I=`b2e2399c71b101db1e740db6d43672d84d3cd661`, tree `574fa8a6f2ec766a8fe88046fe90585cab67d555`, scientific/operating M=`07b5aa9dec8436132a524511d5438c510e322070` and the unchanged bound D/C/H/ALLOCATION. **Every engine command below is UNEXECUTED and currently BLOCKED.** No new procedure, cohort, launch receipt or allowance is frozen by this map.

## Prerequisite: finish B1 without the game

Before even parse/import, implement the missing enclosing native backend named in `EVIDENCE.md`. Use temporary synthetic accounts and inert children to falsify CPU overrun across threads/descendants, stdout/stderr plus file/log/trace/overwrite emission, output/source escapes, unsupported channels, concurrent starts, exhausted cumulative caps, and interrupt/parent-death cleanup. Keep conservative reservations charged after incomplete reporting. Test no-spawn for each failed precondition. Do not run the native game to debug basic containment.

The existing 23 checks prove reservation and record-verifier properties, not this backend. A Linux namespace's existence is not aggregate accounting. A host differing from the historical macOS runtime needs its own enforcing implementation and qualified launch/source identity; cross-host equivalence is not implied.

## Minimum integrated falsifiers after B1 and an explicit execution disposition

### 1. Two-combat identity, save, serialization and terminal control

Exact child argv from an isolated copy of I with a complete source manifest and separately prepared/import-qualified Godot environment:

```text
godot --headless --path <isolated-I> -s res://tests/run_all.gd -- --tests=res://tests/test_dd1_source_repair.gd
```

The `--tests=` selection is supported by the unchanged `tests/run_all.gd`, blob `df036e9667825b1b39e3ee666fb3664f31c7885f`. Do not run the whole suite as a substitute.

Inputs are embedded in `tests/test_dd1_source_repair.gd`: seed **5420099**, Dusk aspect 0, Vow 0; two real combat creations separated by a completed reward and reachable rest/shop/treasure entries. The test explicitly constructs node kinds, an act transition and win flags. It therefore exercises native driver/save/terminal plumbing but is NOT an earned victory or P0/P5 witness. It verifies the first combat is never dispatched again after reward/safe nodes, the next combat gets a new identity and one debit, the final boss stages a terminal, the native commit persists Dawn, and actual Dawn completion clears the save. Separate bounded controls cover journal refusal and ENOTDIR save failure preventing another start.

Demand mode `focused_fixture`; reserve **1 engine + 2 contained starts** before child creation. Proposed upper limits: **300 CPU seconds aggregate**, **300 wall seconds**, **67108864 raw bytes for the whole unit**, including metadata, stdout/stderr, native temporary saves, trace/control files and cleanup. These are proposed caps, not measurements or newly authorised capacity. Deny before spawn if the complete unit does not fit the then-bound cumulative account or source/executable isolation cannot be established. A tight limit produces INCOMPLETE, never a passing truncated trace.

The backend must create the process-wide `DD1_RESERVED_UNIT_PATH` only after the durable reservation, with source bindings including the exact test and runner as well as all required driver/native/content dependencies. A hand-written grant is not a substitute. No fixture-local grant reset is allowed.

### 2. One genuine ordinary root, not a fresh 16-root acquisition

Exact dormant child argv:

```text
godot --headless --path <isolated-I> -s res://tools/dd1_recovery_acquire.gd
```

Demand mode `fixed_ordinary`, diagnostic `max_roots=1`; therefore the sole root is **5421600**, a real blank native Vigil, Dusk aspect 0 and Vow 0. The driver fixes Pilot to **p8-d0-v1**, unchanged default snapshot, random-build=false and random-play=false. No forced outcome, replacement path or target-unlock injection is permitted. All outputs are new files under the host-reserved artifact root. The test must retain initial Vigil/run bytes, every actual command and native event, before/after run/Vigil/RNG/map/UID identities, preterminal run bytes, successful saves, terminal receipt and actual clear. Death is an ordinary terminal; route/write/capture/limit failure is INCOMPLETE, not a death or balance observation.

Use a proposed **contained-start cap of 240** (the driver's step cap), plus **1 engine start**; CPU cap **300 seconds aggregate**, wall cap **300 seconds**, and whole-unit raw cap **268435456 bytes**. This is a conservative stoppable diagnostic envelope, NOT a measured cost or a claim that 240 is the necessary minimum or guarantees a terminal. Every actual start still consumes the enclosing grant before creation, so the bound is enforced even if a source-derived estimate is wrong. If the path cannot finish within it, retain the exact prefix and stop. The 241-start proposed envelope exceeds the presently reported arithmetic remainder of 8 and must be denied. No funding for it is requested or granted by this document; the planner may derive a tighter sufficient envelope only from source/evidence, not speculative refunds.

Feed the resulting `linked-capture.json` to `dd1_provenance.inspect_sequence` with its exact host-pinned source/Pilot manifest. A structurally valid one-root prefix remains non-native-semantic proof. For an empirical provenance claim, `evaluate_witness` additionally requires H's real external role bindings, source/extraction/guardrail receipts and a genuinely complete first-P0/P5 chain; none exist here. A diagnostic prefix is not an alternative scientific cohort and cannot be accepted as the 16-root protocol.

### 3. Stop validation before scale-up

The above controls must establish one-route/one-terminal capture and actual cap/interrupt enforcement before a whole-sequence decision. Any source mismatch, missing source closure, unsupported route, missing/changed initial bytes, save failure, stale combat identity, missing terminal clear or unaccounted output blocks promotion. Separately meter any required engine version/import/parse invocation; do not hide it in 'setup'. GDScript check-only exit zero is not proof of parsing; follow the current-main script-check contract when execution is later authorised.

No automatic corrected full replay, second recovery or top-up follows a green control. Both previous account histories and the old recovery clock remain intact.

## What a corrected replay would and would not establish

Repairing stale-object dispatch, command observation, native reward-field plumbing, durable-save capture and pre-start metering is an **unchanged-game-law verification repair**: no production law, accepted design, Pilot algorithm/threshold, root order or topology was edited here. Driver-dependent trajectories may nevertheless change, so old route/progression and P-profile claims cannot validate the corrected implementation. Existing exact-source M pending-save, reader/collateral/control and visual evidence keeps only its original scope.

The source currently fails INCOMPLETE on unmapped special public-choice routes instead of inventing policy. Giving such a route a new choice policy, changing Pilot weights or acceptance thresholds, reordering/replacing roots, choosing another starting profile, expanding the scientific cohort or claiming new capacity would require a separate planner/owner authority decision under the existing contract. Do not disguise that as logging repair. A diagnostic root prefix does not freeze a new scientific procedure.

No prediction of P5 availability or scientific success is made. The reported previous win/death sequence is historical, not a proof that another run will or will not supply P5. The next disposition belongs to the planner after resolving B1 and inspecting this package, not to a human relaying another status-only restart.
