# PREP-1 evidence completion — whole-artifact author handoff

**AUTHOR EVIDENCE COMPLETION: ready for one fresh whole-PREP source/inert review. NOT APPROVED, NOT NATIVE-ADMITTED.** Authority #542/5779715600 and the owner's atomic-commit continuation, within #542/5757861057 and selected contract #421/5753240365 / selection5753317824. Sole Pro evidence worker. No other writer or reviewer was invoked.

## Exact artifact and task completion

Preserved implementation I=`d4e91e4fc70d57c81922cc1883b339877d835a53`, tree `f635cace1f659b236590cd44ed502841233d2d93`. Whole amendment comparison base B=`36c607355bb7add7968ae51bf7b738c2c078d7d8`. Current-main instructions independently refreshed and rechecked at `07b5aa9dec8436132a524511d5438c510e322070`; no rebase or implementation edit. Publication branch remains `research/p9-six-route/dd1-native-1-overlay`.

The interrupted continuations had already added nine useful evidence files after rescue head e3ad0548747865af914c8c8b4a5fb48c6e3274db. Completion started at b07747d18041a232fd789c0267f4d2e1b0d37e43. Instead of replaying controls, this worker completed their case-level coverage, reused the intact source-admission analysis, supplied its missing pricing artifact, and preserved new static-reader proof. The four progressive commits before this terminal report are 3fda1e8d7b42f473e5310ea96621504f15829a94, 5c39364770c340a2d517b1c5017c143da8d58cca, ef6176bd142b684611e32f4b8eb9bff16ef0a5a1 and d22d87440d78b71eba2b08141de9ca21bedaad5d. Each was atomically committed on GitHub and its complete contents read back before proceeding.

This report, the existing CAPSULE and READBACK.json are one terminal evidence/capsule commit. The #156 terminal handoff records its actual final head/tree after publication and readback; no self-referential or prospective hash is invented here.

## 1. Matrix coverage is complete at the material-claim level

[COVERAGE.md](COVERAGE.md), blob ae362c40f24091861652e05e9ebb1ceae8bfc336, supplies all **45** case names, restored raw lengths/SHA256s, exact bundle locators, expected guards, command/source/build identities, source/config/seal/account joins and cleanup scope. The four independently complete raw bundles retain one actual execution identity: prep1-revalidation-20260921T211017Z, boot d0864e9f-3592-4f03-9800-01af9ddfb74b. Their 13 positive/sealed/replay, 16 semantic/mutation, 12 binding and four kill/exclusivity records are not spliced with another run.

The map includes the actual quality-9 generated resource followed by a restored quality-7 sidecar, failed seed history and no seal. It joins the positive preparation's exact original-to-derived-to-sealed config/resource bytes to the runtime that consumes the seal after the mutable workspace was changed. Full synthetic reservation charges are preserved, including deliberately killed cases. Sixty original boot/PID/start identities and 64 retained files are linked through the same-run postcheck, not a new /proc observation on this host.

**No demonstrably missing inert claim was found; zero additional connected inert runs were executed.** The final outer matrix observer duration/CPU record remains unavailable. That absence is recorded, not filled using the older19:47 run's aggregate and not mistaken for a missing per-case proof. The active continuation contract permits the complete attributed case units to establish their individual claims. Deliberately absent killed-controller final records are not fabricated.

Real-H source/public-entry/binding evidence remains the intact32-check command at its accepted H identities. The three predeclared font/icon/music metadata samples retain the complete input/checker/command evidence from their own NEW run; they are neither Godot-generated artifacts nor the missing original four. Their rescue verification and raw bundles are reused at their actual scope. No H numerical-suite, COMPAT-2, B1 or metadata reassurance replay was performed.

## 2. The conditional source-level admission map is complete; native entry remains blocked

Canonical analysis: [SOURCE-ADMISSION.md](../prep1-final-20260922T211833Z/SOURCE-ADMISSION.md), immutable blob12c6d2c3ad5b1f65b989e46bb95816a3819fbce2. It was produced by the interrupted prior continuation and read in full by this worker, retaining that authorship. It is not rewritten as new engine observation. Its finite inventory/conditional branches cover original/preparation/runtime configuration, explicit editor plugins and implicit extension/module initialization, command-stage effects, single/group/lazy import writers and callbacks, authored seed/options meaning, constructor/content/font dependencies, generated/temporary metadata, headless exclusions, lifetime thread births, exact H roles, and remaining concrete bound inputs.

The map distinguishes source-established facts (S), conditional incompatibility witnesses (X), specifically missing bound inputs (M), and genuine later host/native observations (H). It does not claim a Git tree or three sample sidecars are a launchable per-file runtime manifest. The source-level root/expansion rules and conditions are supplied; the future issuer still must bind the actual chosen files/build/features/outputs to a specific demand. That is a named native admission input, not a fabricated complete manifest or a generic request to repeat this analysis.

### Decisive incompatibilities and favourable exclusions

**X-THREAD:** Under the pinned ordinary threaded editor/preparation conditions, the initial workload, IP resolver, W worker-pool threads, dummy audio and first editor scan require at least W+4 lifetime thread births. When W>=1 that exceeds the fixed four-thread limit before additional backend/module/named-pool births. This is conditional exact-source reasoning, not a measured first syscall, an invented numerical host W, or proof about every possible build. A prior thread's exit does not refund the lifetime allowance. Non-editor W+3 plus other births is not a proved fit. The headless resource-preview worker remains correctly excluded.

The newly inspected pinned WorkerThreadPool source additionally exposes a conditional pump-task growth branch in _add_task: enough dedicated pump tasks can grow the worker array. This is an explicit contributor to the map's T_other, not a new claim it occurred on this workload, not one thread per ordinary import group and not a reason to weaken the existing lower-bound conflict. No additional engine experiment is required to record the source condition.

**X-SENTRY-MODE:** Removing Funplay's editor activation does not remove the Sentry GDExtension's preparation/scene initialization. For the documented debug-Unix handler-preparation condition, an existing handler copied as ordinary immutable source is non-executable, and the attempted executable-permission adjustment conflicts with that protected source mode. auto_init=false excludes automatic SDK initialization, not every earlier preparation effect. No actual chmod, socket, handler launch or binary feature selection was observed here. Paying its bytes does not solve its mode conflict; no source/policy adjustment is authorised in this evidence pass.

Favourable facts remain: the two export plugins register export hooks rather than the inspected resource importers; the project has no explicit autoload list; the filtered fixture's SceneTree script does not run the shipping main scene/custom main loop; its driver constructors still require Main/theme/HintGuide dependencies. The headless MusicBus/SfxBus branches exclude their dynamic audio sample loads, not editor imports of staged MP3 assets or the engine's dummy-audio thread. The font constructor route is not removed by headless mode.

### Remaining concrete native inputs and observations

M: exact staged transitive source/content/asset/plugin/extension manifest, selected engine/module/library/handler features and complete ELF/dlopen bindings; individually declared generated and temporary output inventories/maxima; authenticated closed-writer proof and PREP disposition tied to the exact source/producer/argv/runtime/seeds; actual per-unit/account/receipt sizes and cumulative applicable costs. H: point-of-use supported controller/host/private account/lease/reservation state and, only after actual admission, observed native imports, resource bytes/semantics, complete cleanup and remaining runtime demands. These are not claimed present. The positive inert producer and inotify ordering cannot authenticate native call origin or make all reachable writers qualified by assertion.

A future changed compatibility scope, unsafe effect or unaffordable/expired operation requires its actual authority. This handoff does not authorise a thread, syscall, UID, plugin, permission, clock or budget change. It neither promises native success nor reopens scientific strategy research.

## 3. Dormant pricing is now supplied, with decisive static proof

[DORMANT-PRICING.json](../prep1-final-20260922T211833Z/DORMANT-PRICING.json), blob0aea7f7bfc15d8c6d8ff8d8e7879bfb88a5217dc, is newly authored in this completion and placed beside the existing source report to resolve its missing relative reference. It derives the exact conservative per-unit requirement from the actual I snapshot/preparation/entry/reservation code:

`H + sum_F(bytes+16384) + P + L + 5*A + 4*U + 593920 + W <= reserved_raw`, retaining all earlier strict guards.

It separately expands promotion's complete source copy, seeded backing initialization, original archives, generated maxima, recipe/manifest/journal costs and final/temporary layout entries. Subsequent parse/fixture units still copy source/sealed/runtime inputs; no unobserved zero-copy credit is taken. Generated sizes, optional dependency costs, actual growing account/demand lengths and cumulative prior costs remain named nonnegative terms, not zeros.

For the illustrative m=2 path, five engine invocations and two contained fixture creations total seven starts. Core runtime/helper payload alone is753073440B; including seven runtime-file metadata allowances and fixed unit control/copy costs makes a **756616480B lower bound**. Published raw remainder1072848685 leaves316232205B before all other terms. If the distinct earlier commissioning static payload150811617B is applicable and unbooked, the remaining upper margin is165420588B before those terms. This is not full affordability or a new allowance. Cases m=0..3 and exact formulas are recorded, not selected as execution authority.

Fresh complete font-tree metadata and a deterministic reconstruction establish all22 names/modes/blob identities: nine font binaries, nine seeds and four licence texts. Binary bytes were not fetched or republished. Immediate constructor font pairs total768620B; the full22-file directory totals2374716B, or2735164B with one per-file metadata allowance. A full-directory staging must price the licences too; a nine-face upper set does not assert all faces are loaded.

[STATIC-CHECK-BUNDLE.json](STATIC-CHECK-BUNDLE.json), blob a5d188b72e8baeb0676581a9a8b4a8f4099755a0, contains the complete executed checker, exact metadata inputs, structured results and outer command. Its independent XZ/base64 payload decodes to22267B, SHA25618f66fe4004d75ffa82d1495803bb0f6290434022a23c593d2982f1b30542cee. Every embedded file has length/SHA256. No multipart tail is required. The fresh reader verifies the prior positive-result bundle and account joins, reconstructs the font tree, and computes the conditional pricing. This verifies the intact prior verifier result; it does not relabel the underlying case executions as fresh.

Actual corrected reader command2026-09-23T06:51:08.024096+00:00–06:51:08.082775+00:00 exited0, empty stderr, wall0.058406964s, child userCPU0.05289s/system0.005062s. The prior evidence-only reader syntax failure and brace correction are recorded before/after exact hashes; no implementation or predicate changed. The inner reader measured0.001725553s, a narrower scope. Retrieval, authoring, previous decodes, publication and full-session/static/token totals are not available and are not invented or credited to an account.

## 4. Preservation, finite limitations and next gate

Live B-to-I comparison shows three original commits, thirteen implementation/test paths and the earlier development CHECKPOINT. Live I-to-d22d874 comparison is ahead76/behind0 with only the existing capsule and PREP recovery/revalidation evidence paths changed. Recovery ACCOUNT was read at that head and still has blob8b7e4e6367ad4bc366c04b930ef4fc3810e2a900. Main was rechecked unchanged. The terminal commit contains only this report, readback index and existing capsule; the terminal handoff verifies that actual diff/ref rather than assuming it.

Original records are preserved: parts01–03, unavailable parts04–14, unmatched submissions, recovery reports, CHECKPOINT and old archive hash are untouched. The original missing284-record/four-sample run remains unverified, and its bounded retrieval remains closed. All later evidence keeps its NEW execution or reader identity. No compiler/backend failure is inferred from a chat interruption. The remaining outer-run measurement gap is explicit and does not renew the closed search or justify replaying all45covered cases.

Recovery stays published2040/2048; historical1277/8192, consumed attempt,6915unspendable and UNKNOWNs remain. Deadline remains2026-09-24T17:54:40Z. Published ledger facts are not private live-account/lock observations. No refund, top-up, cost exemption, new clock or native funding is created. Accepted B1/COMPAT-2/D/C/H/#547, H allocation, product/config/sidecars, official engine and protected evidence retain their own scope; old approval is not inherited by PREP changes.

**Next gate: one fresh isolated review of the WHOLE B-to-final PREP amendment and evidence, confirming implementation equivalence to I.** Not a review of this report alone, not N0 acceptance, and not another rescue/revalidation restart. This worker has not commissioned or performed that independent review or issued a native disposition. A reviewer may identify a concrete missing material proof; none is concealed by a fabricated success here.

Source/evidence completion is delivered with conditional native blockers, not a native go decision. No engine/loader/version/import/parser/game run, additional connected inert run, source/account change, ordinary acquisition, N1/N2, self-approval, production PR/CI, merge, issue closure or release occurred. **N0 remains BLOCKED; certificates0/3.**
