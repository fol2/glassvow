# PREP-1: complete source-level conditional admission map

Authority: #542/5779715600, continuing 5757861057 and selected #421/5753240365. Author: sole Pro evidence worker. This is NEW source analysis, not a recovered historical report, native execution, launch disposition or independent approval. The matrix coverage decision is in MATRIX-COVERAGE.json; no missing inert claim requiring execution was demonstrated.

Implementation I=`d4e91e4fc70d57c81922cc1883b339877d835a53`, tree `f635cace1f659b236590cd44ed502841233d2d93`. Whole PREP base B=`36c607355bb7add7968ae51bf7b738c2c078d7d8`. Godot source G=`ed1daf0bf001b61586d9930840f2f1394092c079`. Sentry source S=`d288ad983c30bf7a7d924fbceb8ed7cf6e64de9c` (the project's d288ad9 / 2.1.1 pin, resolved through GitHub). Current-main operating rules were read at `07b5aa9dec8436132a524511d5438c510e322070`; I was not rebased. Source names below are at I, G or S as explicitly marked, never current upstream master.

Disposition: **the source/inert analytical assignment is complete as a conditional map; native admission is BLOCKED.** In particular, the nominal threaded import path has a concrete four-thread-budget conflict when its default pool creates at least one worker. A separate Sentry permission-repair branch conflicts when a non-executable immutable handler is present. Neither is an observed engine failure. No request to relax a policy, change flags or edit the product is made here. Those conflicts do not invalidate a correctly rejecting PREP guard or replace the required whole-PREP independent review.

Classification: **S** = source-established fact/exclusion; **X** = source-established incompatibility under the stated reachable condition; **M** = specifically identified missing bound input; **H** = specifically identified future native/host observation. S does not mean native acceptance. A conditional X is not represented as a measured first syscall. M/H do not conceal a claim that all native writers were qualified by this author.

## 1. Exact stage and configuration boundaries

| Row | Source-established route and input | Disposition |
|---|---|---|
| Configuration | `dd1_prep_view.transform/describe/verify_seal` pins original project blob `319cdc9006d3579ff2abb567830e204a8ff86d9c`, removes only Funplay's exact enabled-plugin entry, binds the changed execution map, and restores original project bytes in the sealed runtime. It rejects unexpected original bytes, ambiguous edits and wrong runtime-view bindings. | S. This changes the preparation view, not the product. Positive/sealed evidence verifies the actual mapping; no engine-import-on-original claim. |
| Preparation | `dd1_compatibility.validate_argv` fixes `/workload --headless --path /source --import --quit` for the preparation stage. G's first filesystem scan initializes extensions/autoloads/editor plugins before ordinary import work. | S route. X/M conditions below must be resolved before any useful native sequence. Removing a plugin entry does not remove its files from the original inventory. |
| Parse | The fixed parser command is one `--headless --path /source --check-only -s <declared script>` process per script. G `Main::start` loads that script, then returns its validity for `check_only` before instantiating its MainLoop. | S. No fixture run or Main constructor is implied by a successful parser. Earlier core/server/extension setup still occurs; this is not a process-free parse. No parser was executed here. |
| Fixed fixture | The fixed command uses `-s res://tests/run_all.gd -- --tests=res://tests/test_dd1_source_repair.gd`. G's script branch instantiates the script's SceneTree base rather than the configured custom MainLoop; `run_all._select_scripts` loads only the named test and does not invoke `_discover` in this filtered branch. | S. No whole-suite replay, production main-scene boot, ordinary acquisition or P_v outcome is authorized. The fixture's explicit constructors and effects below remain real dependencies. |
| Identity | The previous identity/preparation/parse/fixture graph remains conditional on useful-endpoint admission. Version output alone cannot discharge preparation conflicts. | S/M. No new version call, native receipt or automatic transition. |

Actual inert projection, newly verified in POSITIVE-POST-RESULT: original project SHA256 `458619f45e53a0acde22facbe4727db0fd6d7f18b07e3bffd8f2f3d1307683c0`; preparation project SHA256 `fa4ee7e54d08224c0b9b8356e9fbfcd23c5fc470260de818d077d584123cbfac`; runtime restores the former. Seal SHA256 `7fbcc8ebc97552e020155613f3e0da154cb31586fd54974fb9f46d3d8337e65a` covers 22 payload files and two original archives. These are the retained harmless execution's bytes, not a native cache or a future native source manifest.

The current project has no explicit autoload list. It specifies GlassvowMainLoop and application/main.tscn, a splash/icon and default_bus_layout.tres. The script-selected parse/fixture exclusion above is why the production main-loop boot is not silently counted as fixture work. It does not exclude class registration, static script dependencies, extension initialization or the fixture's explicit `DriverMain.new()`.

## 2. Lifetime-thread and process map

The immutable helper's `dd1_notify` admits only the exact allowed thread-clone shape after the one workload exec. It increments its thread counter before allowing the clone; **attempted births are not refunded**. The configured maximum is four including the initial workload thread. This is not a peak-active-thread quota. Clone3's classified ENOSYS fallback and the narrowly classified denied desktop query do not grant another process or an unlimited thread retry. No workload child process is allowed.

Let W be the actual default WorkerThreadPool size selected by G on the admitted host/build. Let T_other be additional lifetime births from platform, renderer, navigation, custom modules, extensions, loaders, repeated scans or producer callbacks. It is nonnegative and is not assumed to be zero.

| Creation site | Condition and source conclusion | Classification |
|---|---|---|
| Initial workload | One initial thread after exec. | S: 1. |
| IP resolver | G `register_core_types` calls `IP::create`; `IP::IP` calls `resolver->thread.start`. | S for a threaded build: one attempted resolver birth, even if the workload makes no hostname request. |
| Worker pool | G `Main::setup` uses `init(-1, 0.75)` for editor/project-manager mode. `WorkerThreadPool::init` replaces a negative count with OS's default, then starts that many workers. `OS::get_default_thread_pool_size` delegates to processor count. | S: W births. A project worker setting alone does not bound the editor branch. `reserve(5)` in the pool is vector capacity, not five births or a minimum count. |
| Dummy audio | G setup2 calls AudioDriverManager initialization. AudioDriverDummy's `use_threads` defaults true and `init` starts its thread in that branch. Main disables it for the movie-writing branch; the selected argv has no movie-writing option. | S for the ordinary threaded dummy-audio branch: one attempted birth. Game MusicBus/SfxBus being silent does not remove this engine thread. |
| First editor scan | G EditorFileSystem constructor sets `use_threads=true` for native THREADS_ENABLED/non-Web builds. `scan()` performs first-scan initialization on the main thread and then starts its scan thread. | S: one further attempted birth on the successful ordinary fresh-import path. Headless is not the constructor exclusion. |
| Later scan_changes | The same filesystem has a separate thread_sources path, and subsequent scans can start new threads after previous workers are joined. | S conditional additions to lifetime usage; do not reclaim births after joining. Exact repetitions depend on the admitted inventory and callbacks. |
| Parallel import | `reimport_files` uses existing WorkerThreadPool group tasks for an eligible multi-file threadable group, subject to editor/import/use_multiple_threads and Web conditions. | S: do not count one new OS thread per asset. Importer begin/end hooks or separate named pools still require their own producer qualification. |
| Resource previews | Previously pinned `EditorResourcePreview::start` returns for headless before creating the worker. | S exclusion retained: zero preview-worker births on that branch. |
| Script documentation | G `_process_update_pending` skips script-documentation and scene-group updates in command-line mode. | S exclusion. Not a blanket exclusion of class-cache creation or every editor service. |
| Other platform/server/module sites | Main setup2 calls OS initialize_joypads, rendering-server initialization, navigation managers and module/extension initialization. Separate rendering, named worker pools and platform backends are conditional; their feature/build and callback selection must be bound. | S call sites and nonnegative T_other. They are not required to prove the lower-bound conflict below, and no complete native upper bound is claimed. H/M: actual selected build/backend and qualified demand, not a guessed zero. |

**X-THREAD:** for the ordinary threaded preparation path just enumerated, the demanded lifetime count is at least `1 + 1 + W + 1 + 1 = W + 4`. With even one default worker it is at least five, already outside the current four-thread contract before T_other. A single-CPU affinity restriction cannot establish a valid four-thread bound. If a build/host reports W=0, this particular inequality alone is not a proof of excess; that exceptional value and every remaining creation site still need genuine bound evidence. This analysis does not invent the binary's reported CPU count, compiled feature set or first denied syscall, and does not authorize changing the pool, dummy-audio behavior or helper.

For script-selected parse/fixture without an editor scan, the corresponding baseline is `W + 3 + T_other`, subject to the same actual build/driver conditions. W=1 therefore leaves no further lifetime allowance. It is not a proof that those processes fit. No separate named-pool, platform/plugin birth or retry may be hidden behind a peak count.

## 3. Retained plugins, Sentry and other service effects

PREP's native validator recomputes three original plugin trees: Funplay `e37a0d78a51a17aba550e6469bbf160290eae7b9`, iOS export `2073e862ab8ccf45782263c3c5ac5f8dc8f323e6`, web export `3c355e0ae02dda675439bdaebde9a1f58f5f2ec4`. Omitted plugin files cannot be used as proof that execution was disabled.

| Service | Exact route and effect | Disposition |
|---|---|---|
| Funplay | Its enabled editor activation is the sole removed entry. Previously commissioned source analysis identifies its settings/server startup as the unwanted network path. Sources remain in the fixed tree. | S suppression of this activation, not general network permission or removal of arbitrary tool scripts. Native writer closure must rule out a separate reachable call/importer registration; no such call is asserted by the fixed fixture. |
| Web export | I plugin `_enter_tree` registers an EditorExportPlugin. Its `_export_begin` adds six named data files only when web_dev export is requested. It registers no resource importer. | S: registration retained, export-data production not part of `--import --quit`. No need to pretend those export callbacks generate the fixture's font cache. |
| iOS export | I plugin registers its export hook and preloads ios_plist_privacy.gd. Plist/privacy processing is in export callbacks, not its `_enter_tree` import startup. | S: retain script/preload bytes; no iOS export, SDK, release or generated plist in this operation. |
| GDExtensions | G first scan finds extension descriptors and ensures they are loaded before editor plugins. I contains Sentry's descriptor even though Sentry is not an enabled-plugin-list entry. | S: Funplay removal does not suppress this path. M: exact selected library/build/ABI and transitive dependency manifest. |
| Sentry runtime initialization | At S, scene initialization registers classes/singletons and calls `prepare_and_auto_initialize`; editor initialization also registers SentryEditorPlugin by type. Auto-init false is tested only after preparation. | S. Do not equate `options/auto_init=false` with no extension code, editor registration, file access or callbacks. |
| Sentry automatic capture | S `_auto_initialize` returns without `init()` when auto-init is false, and also disables automatic initialization in editor hint mode. I fixes auto-init=false. | S favorable exclusion of that automatic SDK-init path; not proof against an explicit application init call or library constructors. The script-selected fixture is not the production custom MainLoop boot. |
| Sentry preparation | S sets SENTRY_GODOT_LIB_PATH, constructs its backend, reads user-data `sentry.dat`, schedules settings verification and lifecycle tracking, and in debug Unix builds checks handler execute permissions before auto-init. | S reachable preparation effects. Environment mutation is process-local, not an authorized host change. M/H: actual backend/library closure and contained user-data path/absence; no live-user-data import. |
| Sentry project saves | `_verify_project_settings` can save project settings when required tracking flags are absent. I already sets always_track_call_stacks=true, include_variables=false and attach_log=false. | S: those inspected corrective-save predicates are not reasons to widen project.godot writability. Unexpected runtime settings must fail the bound-view contract. |
| Sentry C# patch | S prepare_csharp_project calls `_patch_csproj`, which stops when the assembly name is empty or the named project is absent; otherwise it can write .tmp, backup and replace the csproj. I declares no dotnet assembly setting. | S conditional writer, not silently permissible. No C# project patch is admitted; any selected build/default or input activating it requires explicit closure and would conflict with immutable source. |
| Sentry lifecycle | S start_lifecycle_watch schedules a deferred SceneTree watcher and shutdown callbacks. | S: the inspected lifecycle implementation creates no separate worker thread. Do not count an invented lifecycle thread; its Node/callback effects remain represented. |

**X-SENTRY-MODE:** when the selected debug Linux Sentry library reaches permission repair and a crashpad handler is present as an ordinary source input, the backend copies that input with mode 0400 (`snapshot.prepare` marks source files non-executable; `Prepared.__call__` applies 0400). S's repair code then requests executable permission bits before checking auto-init. That branch is incompatible with pretending every source byte/mode remains an untouched read-only execution input and with the unchanged no-extra-effect policy. It is a conditional source conflict, not a witnessed chmod or a claim that crashpad was launched. An absent/nonrequired handler or a genuinely different admitted library feature branch must be established honestly; simply dropping a required dependency is not a solution.

At I the Linux descriptor names debug SO blob `7fcf9f2401ef93ad4e0defec0c263718207cc81d` (4,974,456 B), release SO `fa6cf1af59a9fb4cbb8bf8a7b2e10179ab7bec82` (4,472,280 B), and dependency crashpad_handler `a1127a09bc08f18cb4c4add7afcd11ef4dc02ecd` (940,520 B). These are GitHub inventory identities/sizes, not newly downloaded or executed binaries, not a fresh SHA256/ELF closure and not proof the handler runs. The selected feature branch, exact binary-to-S build provenance and any native constructors still need the bound native-input evidence.

## 4. Seed meaning, producer ordering and bounded writer closure

I's `dd1_import_semantics` parses bounded text without evaluating Godot Variants. It binds each eligible `.import` to its original Git/SHA identity, asset path and actual asset SHA, importer/type/UID, and explicit generated dependency paths. `validate_result` compares complete parsed remap/deps/params values, preserving lexical value spelling and interior whitespace. Section/key order and comments are not native execution authentication. There is no post-result normalization whitelist. Every target must be recorded, in the generated inventory and nonempty. Existing `.uid`, code, project, account or arbitrary content cannot be relabelled as a writable seed.

G's `_reimport_file` loads the original parameters/UID/group/generator information, applies the fixed importer's compatibility/default rules, invokes the importer with the resolved parameters, then writes the sidecar and MD5 companion. It may insert missing declared defaults, omit old unknown options, serialize parameters, add importer-version/metadata/platform outputs, or create a UID if the original has no valid one. These are source-established transformations, **not** permission to accept any changed output. They must either preserve the validator's exact admitted meaning or receive a separately reviewed source-explained contract change; no such change is made here.

The group route is separate: `_reimport_group` assembles member seeds/options/UIDs, invokes group import, and writes member sidecars/MD5s and group information. Startup resource loads can call `_load_resource_on_startup` and `_reimport_file` before the ordinary pass. Signals, registered ResourceFormatLoader/Saver classes and import-thread begin/end hooks are additional writer-entry classes. A final matching sidecar alone does not rule out a second import, alternate option use or restore-after-use sequence.

| Actual bounded input family | Identity/meaning established | Admission consequence |
|---|---|---|
| Font | NotoSerifTC-Regular sidecar blob `385f295d83e10d6b7f82775fa74c34d36d4f4e3b`, 801 B: font_data_dynamic, FontFile, UID b8t3h5se5o2ax, exact source and `.fontdata` target. Its authored params include the fixed importer's rendering/default keys, false MSDF, empty preload/fallback/language/script/opentype collections, and allow_system_fallback=true. | S: the inspected font options are not an empty disposable sidecar. G's dynamic-font importer reads original font bytes and uses the passed options. No missing-default conflict is invented for this example. Actual fontdata, output length and dependency behavior remain H. Allow-system-fallback remains authored; explicit application fallback chaining is not permission to assume all system font lookup disappears. |
| Texture | Icon sidecar blob `4a1962ea54ff2be6bc55cb6f519d233953d20ef5`, 955 B: texture, CompressedTexture2D, UID c4igq5s7ttxin, `.ctex`, multiline metadata with vram_texture=false and compress/mode=0. | S: multiline metadata is covered by the retained pure reader. Do not equate this icon with every compressed/platform texture; each actual target/variant and any changed metadata needs the same closed inventory and lexical checks. |
| MP3 | act1-boss sidecar blob `a4d1b77ebee91fee829b37abd093084df3351752`, 374 B: mp3, AudioStreamMP3, UID gv2dlw5nlp4s, `.mp3str`, loop=false, loop_offset=0, bpm=0, beat_count=0, bar_beats=4. | S: a concrete authored seed/target. Music is not loaded by the headless fixture buses, but a staged MP3 can still be found/imported by the editor scan. No audio execution or generated-MP3 evidence is claimed. |
| Other staged assets / groups | The validator's eligible-extension set is broader than these three observed families. The actual immutable execution inventory, not the extension allowlist, determines whether additional importers, group files or tool resources can be reached. | M: an admitted native inventory must name those assets/seeds/targets and the associated producer witness. An unlisted family does not inherit qualification from the three samples. If present but unqualified, the affected native stage is refused. |

The retained three-sample metadata command is reused at its pure-reader scope; the font/icon/music binary bytes were not fetched to reproduce it. The additional Noto sidecar/source inspection above is new analysis, not a fourth executed metadata sample or the original lost four-sample run.

Inert positive evidence establishes the actual original→derived→sealed mapping and a runtime consumption independent of later mutable-workspace edits. The sixteen-negative evidence includes an actual quality-9 generated resource followed by quality-7 restored metadata, a failed seed-history audit and no seal. The supporting inotify audit is explicitly not a complete read/call-origin trace. Kernel event coalescing, final hashes and the inert producer do not establish native code/writer qualification.

I's `native_bindings` instead requires unchanged H's prospective roles, a trusted preparation disposition and a writer-closure proof bound to the exact source manifest, producer/argv/runtime, engine G and sorted seed paths. It checks the proof is authenticated/bound and carries qualified source witnesses; it does not make those witnesses true by construction. A future issuer must cover the single/group/lazy-import and registered callback classes above, forbid unapproved intermediate settings, and match actual bytes/build/features. This report is not that issuer and produces no native positive. A copied boolean, synthetic context or future P_v receipt is not substituted.

## 5. Complete conditional input/output inventory

The bound root is I's tree, not current main and not all files on a workstation. The following is a finite source-level inventory rule and set of roots; it is not a fabricated launchable manifest. Concrete unit construction must expand the reachable closure to exact per-file bytes, SHA256, modes and output bounds before release. A Git tree identity alone is not the complete runtime SHA256 manifest.

| Surface | Required or excluded closure at the selected stage |
|---|---|
| Executable/source roots | Preserve the actual transitive extends/preload/global-class closure rooted at test_dd1_source_repair, dd1_native_main_route, dd1_native_driver_main, dd1_unit_grant, journal/ordinary-capture/receipt, Main, MapCompose and Pilot. Domain/content/application/presentation/scripts/tests/tools source at I remains immutable. Global classes are real dependencies even without literal preload strings; loading a script is not proof every function's I/O ran. The source manifest must not be limited to the 18 restored tooling refs or the 22-file control closure. |
| Content hydration | `ContentDB.load_full(false)` resolves `content/full-content.json` and `_load_line_table`; it suppresses the mob-overrides branch. Full-content/line-table identities belong to I. The fixed fixture does not select `load_slice` merely because slice files occur in another method. Data-dependent IDs are bounded by those immutable dictionaries; dynamically selected class/content dependencies must not escape the admitted inventory. |
| Main construction/fonts | DriverMain extends Main; Main._init calls GlassStyle.theme and creates HintGuide. Theme immediately loads NotoSerifTC-Regular, NotoSansSymbols2-Glassvow and Alegreya-400 through its normal-font path. These three fonts and their exact sidecars are real constructor dependencies; headless does not remove them. GlassStyle's remaining face paths form a finite nine-font set, not arbitrary host files. Additional reached HintGuide/transition/UI routes must use their bound source paths/declared outputs, not an unbounded wildcard. |
| Bounded font inventory | I fonts tree `fb965110125b0b2fcd69e55f81312b9702abd6c0` contains nine fonts: Alegreya 400/700; Cinzel 500/700/800; NotoSerifTC Regular/SemiBold/Black; NotoSansSymbols2-Glassvow, each with one tracked sidecar. Raw font bytes total 2,349,952; sidecars total 7,099. This is a metadata-derived nine-face upper set, not proof all nine are loaded. No font payload is republished. |
| Fixture visual exclusions | Route creates a DriverMain that the fixture does not attach as the shipping main scene. Its screen overrides clear routes rather than construct the production map/reward/end screens. `MapCompose.fake_layout_compile` is the selected callable. Thus production `_ready`, UI rendering, screenshot capture and every literal art path in unused methods are not automatically executed obligations. Their scripts can still be parse dependencies, and constructors remain separate. |
| Music/SFX | At I MusicBus._init returns on headless before creating players; play returns when players are absent. SfxBus._init mutes/returns; play returns before dynamic `_stream` access. The music FILES map is finite, and SFX's `%s.mp3` path is not reached by these headless branches. This discharges sample-loading through these fixture buses, not editor import of any MP3 included in the staged source tree or the engine's dummy-audio thread. |
| Project assets | Original project splash/icon/default_bus_layout references stay bound. The editor scans the actual materialized source tree, skipping its project-data directory, hidden paths, nested projects and real `.gdignore` boundaries according to G. A directory named `_attic` or `research` is not automatically ignored. Do not copy the entire repository blindly, omit required imports, or call a lexical-path list transitive closure. |
| Plugins/extensions | All three pinned plugin trees must be present; selected Sentry descriptor/library and required dependency closure must be honest. Extension/plugin icons, scripts, cfg files, tool resources and any additional source-present `.gdextension` are included according to the same scan/skip rule. Native dynamic libraries require both dependency-name and ABI/symbol-version closure. |
| UID/global classes | G scans/registers classes/extensions, rebuilds UID mappings, may create a missing script/resource UID file and may repair duplicate IDs. Existing authored `.uid` remains immutable. Any required new UID file must have an individually admitted generated location; changing an existing immutable UID is X, not an implicit PREP permission. Global-script-class-cache and extension-list identities must belong to this exact source/build, not a Mac cache or hand-authored substitute. |
| Preparation generated data | Enumerate each sidecar slot and every final/temporary relative path: imported resource outputs/platform variants, `.md5` companions, UID cache, global_script_class_cache.cfg, extension-list, filesystem_cache10/filesystem_update4 and any reached editor-settings/layout/cache outputs. Names have producer/feature/data conditions; they are not all asserted created. G can read/remove an update cache and reimport lazily. Original project, scripts/assets/accounts and existing UID files remain outside writable slots. No arbitrary writable assets directory or unknown generated tail is accepted. |
| Fixture writable effects | The fixed test uses isolated user-data run/vigil JSON paths, its deliberate file-not-directory failure target and capture/journal/result outputs. It checks Unit.remaining>=2 and consumes two contained combat creations before effect; those are not two OS child processes. Bound save writes, repeated replacements, removals, failure artifacts and final records are raw costs. No live or earned account/profile is used. |
| Sealed reuse | Every later parse/fixture snapshot must bind the exact accepted seal, complete payload/original archive/recipe, successful parent reservation and restored project. No gameplay uses the mutable preparation directory. Old staging changes cannot be used to mutate the sealed payload; replay/splice/engine/view mismatches are rejected by the retained controls. |

The actual schema limits make this conditional inventory finite: recipe bytes at most 131,072; at most 128 slots/seeded-sidecar bound and 4,096 projected runtime files, plus the separately enforced generated/temporary-layout and audit bounds in the pinned code. Slot/path alias/traversal and source overlap protections remain. An expanded native inventory exceeding a bound, requiring an unsupported normalization, new UID rewrite, undeclared temporary, external importer process, socket or library is a concrete refusal, not an authorization to widen a wildcard. Generated byte maxima are variables until justified by the selected producer; the price cannot be inferred from the three sample sidecar lengths.

## 6. Native-only obligations, with finite endpoints

| ID | Classification and missing item | Smallest legitimate next evidence, not an engine instruction |
|---|---|---|
| M1 | Missing exact prospective native unit inventory: complete per-file source/execution/runtime/seed/asset identities, selected Sentry library feature branch, compiled module/backend features and optional ELF/dlopen/ABI closure. The seven published core runtime payloads do not include a proof of every optional library or extension constructor. | Bind the actual prepared host inputs and existing static/build provenance; inspect the specific unqualified library/build dependencies. Do not restage 150 MB merely to repeat the known size, claim missing libraries are harmless, or use a synthetic H context. |
| X1 | W+4 lifetime import demand conflicts with limit4 whenever the ordinary threaded path has W>=1. Additional backend/module births cannot improve it. | Resolve this source incompatibility within real authority before native entry. This task supplies no implementation change, new thread allowance or different engine. A native attempt is not needed to discover the inequality. |
| X2 | Debug Sentry permission repair conflicts with an included source handler copied0400, even when auto-init is false. | Establish the actually selected library/handler requirement and mode path; any needed contract/source change requires its own authority/review. No chmod, omission or shim is performed here. |
| M2 | No authenticated native writer-closure/disposition is supplied for the expanded actual seed/importer/callback set. | Use the already-required trusted H roles to bind source/build witnesses covering the enumerated writers and intermediate option use. This source-only report is input to that analysis, not an authentication system or issuer. |
| H1 | Actual generated outputs, semantic equality, producer completion, audit/cleanup and compatibility behavior for an eventually admitted native stage are unobserved. | Only a later lawfully admitted contained native execution can supply these observations. They are not manufactured to make this source/inert review green. |
| H2 | Actual current private executor lock/account state, lawful outstanding remainder, fresh controller/ABI/libc/limits and stage-specific authority are not witnessed by a published Git ledger or this reader host. | Read/bind the real executor state at future point of use; no live write/reservation occurred here. |
| M3 | Complete cumulative resource admission is absent: expanded source/generated unit lengths and prior cumulative static/failed costs are not all known. | Use DORMANT-PRICING.json's exact formula with actual unit/input/output bounds and unreconciled-cost terms. UNKNOWN is not zero; seven nominal starts are not an admission. |

These are specifically classified native inputs, conditional incompatibilities and future observations. They are not a request to keep replaying intact controls or to leave the PREP source/inert review waiting for forbidden native success. Source-only review may assess B→final/I equivalence and these limitations; only the legitimate independent reviewer/planner can approve/accept. N0 remains BLOCKED, certificates0/3.

## 7. Exact source index and evidence limits

All following contents or named ranges were actually read. For I, immutable links are `https://github.com/fol2/glassvow/blob/d4e91e4fc70d57c81922cc1883b339877d835a53/<path>`; for G, `https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/<path>`; for S, `https://github.com/getsentry/sentry-godot/blob/d288ad983c30bf7a7d924fbceb8ed7cf6e64de9c/<path>`. Git blobs identify whole files even when analysis uses a bounded range.

| Ref | Path / functions | Git blob |
|---|---|---|
| I | tools/dd1_prep_view.py | 84da0f4b7e6d35d6d2cf59bc15f8770040b19874 |
| I | tools/dd1_import_semantics.py | 00fc12812aecb91c44f0ddc7243aa8c6197ba0a3 |
| I | tools/dd1_preparation.py | ddb491997436f86af223ca90fa5129f7bfd49c51 |
| I | tools/dd1_preparation_watch.py | 3c1cd817fc7abbffe236c98860161969a5170d97 |
| I | tools/dd1_linux_snapshot.py | 351bdc111735caf12b7fa23a6df37766b8667c51 |
| I | tools/dd1_linux_backend.py | fc7547d31d9222481eb370c07de33a0f74fe5153 |
| I | tools/dd1_meter_entry.py | 3ae1a4509c520f68df825f782ac2c0ad90408c5c |
| I | tools/dd1_linux/policy.c | 97b54c5ee36e9e2028e48be38b7d8628e09bdc1b |
| I | tools/dd1_compatibility.py | 89119865b05f51b08bb5b620e1711c138fc7cbcb |
| I | project.godot | 319cdc9006d3579ff2abb567830e204a8ff86d9c |
| I | addons/glassvow_web_export/plugin.gd | 8db46d9fe02c152a64c2149fae8a03f351c7b2cc |
| I | addons/glassvow_ios_export/plugin.gd | 2156d3bfe1bd9a6e628d6559a6e09f82b696fb21 |
| I | addons/sentry/sentry.gdextension | 47f4eb1723d5a61e706ce1fc078f9f68d6defa33 |
| I | tests/test_dd1_source_repair.gd | 0fd416c2c904220cfd2622f46b7188dc1578586f |
| I | tests/support/dd1_native_main_route.gd | e17d436d8cf0e06dd1aca391625d4e1c03a83c8e |
| I | tests/support/dd1_native_driver_main.gd | d8d2eff17399ff1ea49fcd3b45877d29bee5d4e4 |
| I | tests/run_all.gd | df036e9667825b1b39e3ee666fb3664f31c7885f |
| I | application/main.gd, _init/_ready | 4b89091a6d8052491ca1c59ab93280759a9f68d4 |
| I | content/content_db.gd, load_full/load_from | c5aff7a34f40652a653f6cc76d6a3af4b77a99b1 |
| I | presentation/combat/glass_style.gd, faces/theme/_theme_type | 3aea0b8bcd1298e236e798c288cd23d444574d62 |
| I | presentation/audio/music_bus.gd | c62b470413b8da97d4cb4127e2496a3ef8d07449 |
| I | presentation/audio/sfx_bus.gd | f345f61c8b616ac7bc68c9926f5f00709bf11412 |
| G | main/main.cpp, setup/setup2/Start script branches | 2fc62fdc54303e51951a69317055aa2982f3f945 |
| G | editor/file_system/editor_file_system.cpp, complete file including scan/reimport/group/UID/cache paths | 1a9e609fb0e8e7af32e4c1b28b22b4bafa26406c |
| G | core/register_core_types.cpp, IP creation/default worker setting | 80696814aab5d1fc2199a1cd57c36c56010fe383 |
| G | core/io/ip.cpp, IP constructor | ec7c72d27479acaabd9d2b0edaf7953730e8fe3f |
| G | core/object/worker_thread_pool.cpp, init | f5574c47a05c6d959382972556ed7413076c0328 |
| G | core/os/os.h, default pool size | fd7b0c896a969ed2a2a59bd78a08f4bf8f1e0938 |
| G | servers/audio/audio_driver_dummy.cpp / .h | 3d1070a6cb67da9972996870c5f610713dd14c56 / fe292c8d0f36f1a4569bdf4e2be7f2c11eb5f113 |
| G | servers/audio/audio_server.cpp, AudioDriverManager::initialize | 98bb2cebd94299cd2d87bc1ca5fd4d5c73ab1807 |
| G | editor/import/resource_importer_dynamic_font.cpp, options/import | b0312152ece357368f78e378ab651882d0682d69 |
| S | src/register_types.cpp | e17d646ebeaec034c198ebdb979ed5e86579b518 |
| S | src/sentry/sentry_sdk.cpp, preparation/auto-init/permissions/settings | ccc0f9a04f9a0838c8b789de9b897975d45ee621 |
| S | src/sentry/engine_lifecycle/engine_lifecycle.cpp | ddea2b6258bece9dadbcb978962c0771c4196799 |
| S | src/editor/dotnet_tools.cpp | bba9b6933313bab074ccf3109904af432bc9668d |

Headless-preview exclusion, core seven-payload ELF observations, earlier Funplay startup and historical static costs are explicitly reused from `source-repair/entry-1/commissioning-20260920/REPORT.md`, blob `e7d3cd40970f3c693c4d536dbd32788e21bc9a6c`, at their original scope. This pass did not repeat their binaries, runs or timing. Default-branch code search was used only to locate paths; factual source claims above use the pinned reads. No web-master code, native output, font/library payload or fake cache is incorporated into I.

Both real accounts, original missing-record history, accepted B1/COMPAT-2/D/C/H/#547 and protected evidence are unchanged. Deadline remains 2026-09-24T17:54:40Z. No engine, inert control, helper build, H suite, live-account operation, reviewer, self-approval, merge or release was executed in this completion pass.
