# B1 delta to the dormant native execution-demand map

**NOT A LAUNCH DISPOSITION OR NEW ALLOWANCE.** This updates the engineering prerequisites of the preserved `../EXECUTION-DEMAND.md`; it does not freeze a new scientific procedure. Implementation I is `efdac6a25dd0436de5ef2c52136b5f8ce71c4da7`, tree `19781b453689203eb3d6696a247e3d5a08ca8d3a`. B1 real inert evidence does not establish Godot compatibility or cross-host equivalence.

## Available engineering path, still gated

The former unconditional backend stub is replaced by the fixed Linux implementation. A future host must qualify the exact Linux 6.18.44 x86_64 LP64 venue, complete immutable executable/interpreter/runtime/source/content snapshot and code-pinned supervisor. Required isolation and kernel primitives must work; there is no fallback to ordinary subprocess, separate unrestricted per-process CPU caps or stdout-only accounting. The tested helper SHA256 is `dae7a481fd0d6c25f093c38056673a6039f98d899e816a65953537a54d00c7ce`; all compilation/runtime identities are in `BUILD.json`.

The public path is `dd1_recovery_meter.run_metered` -> `dd1_meter_entry.run_complete_unit` -> concrete `Prepared` -> existing `reserve_and_run`. The host must supply its already-authenticated empirical H context and exact `execution_demand` role bytes, operation/head/argv, ancestry object chain, existing launch receipt and current account identity. A CLI flag, source task, copied receipt or synthetic context cannot supply that authority. No such empirical execution context was made or tested in this batch.

Before workload release, reserve the complete unit's engine plus contained starts, CPU, all raw bytes including setup/control copies, and wall/deadline. The concrete Linux backend needs **at least 11 CPU seconds** and **at least 3 wall seconds**; 300 CPU/3600 wall are implementation maxima, with the existing deadline and stricter limits still applying. It allows one workload process, no additional processes, at most four lifetime thread births including main, one CPU affinity, 2 GiB address space and 128 fds. A required but unsupported syscall/ABI must fail, never be silently disabled to manufacture a game result.

These restrictions are an engineering venue, not a change to Pilot choices or game laws. Whether they support the actual engine and its necessary threads, resources, import state, save operations and progress is unverified. Native setup itself requires explicit metered authority and must not be hidden inside a purported three-start test. The historical native venue was macOS; moving to Linux is not authorised or qualified by this source artifact.

## Smallest integrated falsifiers, not commands to run now

**First, engine/venue/source preflight under a separately established bound.** Bind the exact engine bytes, loader/libraries, source/content/import inputs, existing receipt and output root. Demonstrate startup/serialization and that all required channels and thread counts fit the supported backend. There is no unmetered version/import/parse attempt. A failed startup is an unsuccessful charged unit, not ordinary-route proof. This demand cannot be numerically closed from inert helper success alone.

**Then the preserved focused two-combat/save/terminal control.** The authored `tests/test_dd1_source_repair.gd` exercised through `tests/run_all.gd` must cover dispatch -> reward completion -> safe node -> stale-result rejection -> genuinely new combat positive, plus initial/preterminal/Dawn/save/receipt linkage and denied contained-start exhaustion. Preserve its synthetic labels; forced fixture outcomes do not become P_v witnesses. The previously described three-start figure is one engine plus two contained combat creations ONLY; it excludes any still-unqualified setup and must be composed with its actual enclosing bound. No launch is authorised here.

**Then, only if separately assigned, the fixed first-root ordinary capture diagnostic.** The existing `tools/dd1_recovery_acquire.gd`, `dd1_ordinary_capture.gd` and `dd1_unit_grant.gd` can use a one-root prefix at seed 5421600, blank ordinary Dusk Vigil, original Pilot and public-choice mapping, vow `min(vow_unlocked,4)`. A conservative full 240-step contained-start envelope is 241 engine-plus-contained starts, not eight. This remains UNFUNDED and UNEXECUTED. It must stop on capture/save/route failure; no fabricated terminal or repeated command for logging. Preserve original captures; all outputs must be new. Inspect the full linked bytes and actual stop enforcement before any larger sequence.

A later corrected replay with the same laws, root order and policy is a verification repair of the producer defects, not a new statistical sample and not evidence P5 will appear. Changing the native venue requires qualification/disposition; changing policy, root cohort/order, thresholds or gameplay scope requires the corresponding separate authority. This map authorises none of those changes.

## Evidence validity and accounts

Old reported native outcomes, old launch/counter records and old missing sequence bytes cannot serve as qualification of the corrected producer or this backend. They remain immutable observations with their original uncertainties and debits, not refunded experiments. Existing dependency-scoped pending/save, reader/control and visual evidence remains as described in the prior evidence map; B1 does not invalidate unchanged component behavior merely to rerun it.

The new inert records establish the restricted backend's observed process/isolation/CPU/raw/cleanup controls, not GDScript correctness, real ordinary progression, H empirical extraction/authentication, N0 acceptance or balance. All such tests remain UNEXECUTED in this zero-engine batch. In particular the int-0x80 host SIGSEGV does not prove the seccomp alternate-architecture branch.

Recovery ACCOUNT retains reported 2040/2048 and eight NOT AUTHORISED. Historical 1277/8192 and 6915 unspendable/UNKNOWN are unchanged. The recovery window stays 2026-09-17T17:54:40Z–2026-09-24T17:54:40Z. No refund, reset, new recovery or implied top-up. Accepted D/C/H, H ALLOCATION, #547 acceptance and certificates 0/3 are unchanged.

Next: planner's scoped review and source/venue/resource disposition using this exact artifact. No concurrent reviewer was invoked by this author, and no self-approval or automatic native transition was performed.
