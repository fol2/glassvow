# DD1-LINUX-ENTRY-1 — post-COMPAT-2 commissioning admission

**BLOCKED_BEFORE_ENGINE_ENTRY. COMPAT-2 remains ACCEPTED and unchanged.** This is the terminal static-admission outcome under [#542/5752743928](https://github.com/fol2/glassvow/issues/542#issuecomment-5752743928), not another COMPAT-2 implementation, review, publication-recovery task or observed engine failure. No engine or contained-game start, live reservation or live-account write occurred.

The reviewed execution-input identity remains **E=aedb67dc0bad4bfff3abb1963c338f676bd38413**, tree **abd4b54215675ae2b72644511569efef9e38c322**; implementation I=d8ea1d696242ed26c8c598474e39e68b4800ba9c. Subsequent publication heads contain only this commissioning evidence, not a different approved executable input. Current-main operating rules were refreshed at 07b5aa9dec8436132a524511d5438c510e322070 without rebasing science. The [independent source/inert review](https://github.com/fol2/glassvow/issues/542#issuecomment-5751340783), planner acceptance and earlier publisher receipt remain intact. The sole commissioning author resumed in [5752956845](https://github.com/fol2/glassvow/issues/542#issuecomment-5752956845); other writers/reviewers/publisher stayed parked.

## Actual new observations

The retained official engine and six declared library/interpreter payloads were read as bytes and copied to private read-only inspection paths. Engine SHA256 is `8d106cbe6144c2dc7e881d61d2429c1a8a76e6b22ef48bd5e48dcf934953f71e`; engine size is 146414384 bytes. The seven runtime payload files total **149655752 bytes**. The accepted pure ELF reader resolves their declared interpreter/DT_NEEDED names. This renews point-of-use byte identity; it does not establish optional dlopen, fontconfig, plugin, import or complete engine behavior. No loader/ldd/version call was used.

Twenty retained implementation/test files were checked at their I identities, unchanged at E; the two H files were checked at H=5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6. The new local source manifest initially mislabelled those H file refs as I, while their bytes were correct. `SOURCE-REFERENCE-CORRECTION.json` and the actual correction script preserve that provenance correction. No accepted H or historical evidence was edited. These 22 files are **not** the complete transitive game inputs.

One compiler invocation rebuilt the accepted supervisor. It exited 0 with empty compiler stdout/stderr and produced **958936 bytes**, SHA256 `b22fdaba90df2a1a317f1d3a690522736a1301f9b442016764d5bf173f2fdf0f`, matching the reviewed pin. The supervisor was **not executed**. Neither the accepted 69+22 controls nor an old B1 suite was rerun.

The inspector observed Linux 6.18.44/x86_64/LP64, glibc 2.41 and one Python task, without allocator/library preload variables. That inspector had already consumed over one CPU second; it is **not** represented as the fresh less-than-one-second controller required by native entry. A future admitted unit still needs its own actual dedicated controller and complete host/source/account checks.

## 1. Fresh headless import reaches the original enabled plugins

The exact upstream source is **ed1daf0bf001b61586d9930840f2f1394092c079**, not current master. The bound `--import --quit` branch sets editor/wait-for-import behavior. EditorNode's headless command-line flag does not bypass the first scan. [EditorFileSystem::_first_scan_filesystem](https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/editor/file_system/editor_file_system.cpp#L325-L362) prepares global classes and extensions, initializes autoloads, then calls `EditorNode::init_plugins()`.

[init_plugins](https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/editor/editor_node.cpp#L1230-L1246) iterates `editor_plugins/enabled`. The [actual plugin loader and add-child path](https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/editor/editor_node.cpp#L4365-L4541) have no headless/command-line exclusion for these valid tool scripts.

The unchanged project enables Funplay MCP and the web/iOS export plugins. Funplay's [tool entry](https://github.com/fol2/glassvow/blob/aedb67dc0bad4bfff3abb1963c338f676bd38413/addons/funplay_mcp/plugin.gd#L21-L37) constructs its settings/server and starts the server when enabled. [Fresh settings](https://github.com/fol2/glassvow/blob/aedb67dc0bad4bfff3abb1963c338f676bd38413/addons/funplay_mcp/core/funplay_mcp_settings.gd#L6-L31) default to enabled. Its [port probes](https://github.com/fol2/glassvow/blob/aedb67dc0bad4bfff3abb1963c338f676bd38413/addons/funplay_mcp/core/funplay_mcp_server.gd#L219-L249) use TCPServer.listen and StreamPeerTCP.connect_to_host. Those socket operations are outside COMPAT-2's two explicitly refused capability classes.

**Finding:** the successfully loaded original fresh preparation path requires an unadmitted networking capability. Handling the socket error cannot turn an unexpected denial into compatibility success. This is exact-source/policy reasoning, not an engine execution, a prediction of the first denied syscall or proof that every possible pre-imported arrangement fails. Deliberately omitting/failing plugin dependencies is not a complete-input solution. No plugin was disabled, no disabling user configuration was injected, and no sockets or further refusal classes were permitted.

## 2. Fresh reimport needs to write an existing frozen sidecar

The [pinned _reimport_file implementation](https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/editor/file_system/editor_file_system.cpp#L2815-L2990) reads an existing asset `.import` file's parameters/UID, invokes the importer, then opens that same `.import` path for WRITE. Only the explicit keep/skip path avoids this standard reimport behavior.

The actual tracked example is [NotoSerifTC-Regular.woff2.import](https://github.com/fol2/glassvow/blob/aedb67dc0bad4bfff3abb1963c338f676bd38413/assets/fonts/NotoSerifTC-Regular.woff2.import), blob `385f295d83e10d6b7f82775fa74c34d36d4f4e3b`, 801 bytes. It declares `font_data_dynamic`, its existing UID and a `.godot/imported/...fontdata` target, not keep/skip. Its exact text is retained as `TRACKED-SIDECAR.import.txt`; no font or fabricated imported metadata is included.

A new **pure static check** fed this exact frozen sidecar into the real accepted `validate_recipe` overlap predicate. It reached `ReservationError: slot shadows/aliases frozen input`, as required. The checker exited 0 with empty stderr. Its input is deliberately a partial static shape, not a launchable demand or empirical context. Full command, output and script are retained.

**Finding:** where this fresh reimport is necessary, the original sidecar cannot be rewritten through the accepted read-only-original/generated-slot layout. Making the sidecar a generated slot over the frozen input is specifically rejected. No suitable genuinely generated sealed cache/lineage was supplied in the retained package; an unqualified Mac cache or dropping the sidecar's UID/parameters is not a substitute. This does not prove that all authentic pre-imported alternatives are impossible. The conflict is in actual original-project preparation admission, not a reopened COMPAT-2 source verdict.

## Independent admission facts and remaining input map

The thread analysis has a useful exclusion: [EditorResourcePreview::start](https://github.com/godotengine/godot/blob/ed1daf0bf001b61586d9930840f2f1394092c079/editor/inspector/editor_resource_preview.cpp#L584-L598) returns for headless before creating a worker. It is **not** counted as unavoidable. The editor worker pool still uses its default branch rather than the non-editor project setting; the IP constructor starts a resolver thread; dummy audio starts a thread conditionally; scan/import/renderer/module/plugin lifetime births remain to be fully bounded. No claim of a proved four-thread whole-engine upper bound, or a made-up exact excess, is made.

The fixture is not resource-free merely because its outcomes are synthetic. Its route preloads driver, journal, Pilot, map-compose and other source; driver extends Main; Main constructs theme/hint objects; route constructs transition/music/SFX nodes. `ContentDB.load_full(false)`, global-class/UID metadata and GlassStyle's explicit font loads are actual dependencies. The exact additional reads and earlier accepted dependency map are retained in SOURCE-MAP and the accepted DORMANT-DEMAND. Complete source/content/font/dlopen/GDExtension/export-plugin input inventories and authentic generated output maxima remain **unclosed**, not silently replaced with the 22 recovered tooling files. No engine-generated cache was created or promoted.

The unchanged H modules are materialised at their actual external H refs. Existing owner selection, independent review and planner acceptance are real authoritative inputs. No new stage-specific launch disposition/context was issued because real admission prerequisites were not closed. The required roles remain execution_demand, compatibility_profile, compatibility_disposition and applicable recipe/seal, bound through H's prospective verify_bindings. Future P_v/extraction outcomes are **not** prerequisites. A supplied/caller-made boolean or synthetic context would not fill the gap; neither was used. There is no claim that legitimate future host admission is impossible or that the owner must repeat a grant.

The account was read only as a **published GitHub ledger**. No mounted current executor ledger, active-lock observation or new live-account identity was constructed. Published totals are not a certification of private executor state or historical completeness. There was no reason to acquire a live execution lease after the fixed useful path failed static admission.

## Narrow parse plan and complete cost formula

The provisional narrow parser set is the amended `tests/support/dd1_unit_grant.gd` plus `tests/test_dd1_source_repair.gd` with its actual transitive preloads/global classes. The unchanged filtered runner is exercised by the subsequent fixture process, not an unrelated full-suite replay. This selection does not waive any missing dependency: it is sufficient only once the sealed transitive input contract really closes.

With m=2, one identity + one preparation + two separate parser processes + the fixture's one engine/two contained creations totals **seven starts**, leaving one nominal start if there are no other prerequisites. Each additional engine invocation carries its own complete reservation; this is not a retry allowance. No parser wrapper is counted as one engine for multiple scripts.

Five engine invocations copy at least **753073440 bytes** of runtime plus helper payload. Published raw remainder is **1072848685**, leaving **319775245 bytes before all source, metadata, generated/sealed outputs, repeated writes and control records**. This pass also wrote a known 150811617 bytes of static runtime/helper/source payload. Including that required additional lower bound leaves **168963628 bytes** before the other costs. Compiler temporary writes and diagnostic filesystem/metadata costs are not asserted retrospectively fully metered. A future cumulative budget must include these observations; no static artifact becomes free execution setup or account credit.

BUDGET.json gives all m=0..3 scenarios and the actual implementation formula. For each unit, reserve helper/runtime/original/prior-generated payloads, 16384 bytes per copied file, complete preparation promotion reserve, five pre-account lengths, receipt length, four encoded-unit lengths, 593920 fixed setup/control/probe bytes, and the workload raw envelope. Promotion includes a complete source copy, generated maxima, journal/manifest and **all** temporary layout entries. Every overwrite/create/rename/delete remains cumulatively charged. Final numeric bounds cannot be honestly completed without the missing source/generated inventories and actual unit/account bytes.

The supported unit CPU domain remains 11..300 seconds inclusive of controller/bootstrap/supervisor/workload/cleanup partitioning. Five hypothetical 300-second envelopes fit the reported CPU remainder arithmetically, but that is neither engine completion-time evidence nor raw/host admission. No expensive maximum burn or accepted-control replay occurred.

## Graph outcome and preservation

**0 STATIC_ADMISSION_COMPLETED_WITH_BLOCKERS → 1 NOT_ENTERED → 2 NOT_ENTERED → 3 terminal evidence/account handoff.** The original graph requires admission and complete pricing of the intended stage and useful endpoint before launch. A version banner alone would not resolve these original-project preparation conflicts. This report does **not** claim the plugin path runs during `--version`; no identity-stage failure is invented.

Execution demand and launch disposition remain null. No prospective manifest is falsely presented as launchable. Zero engine/helper runs, contained starts, live reservations or live-account writes occurred. The sole new pure overlap check and byte/compiler observations are labelled separately from accepted source/inert controls.

Recovery remains reported 2040/2048, CPU398617197992/14400000000000 ns, raw893139/1073741824 bytes, deadline **2026-09-24T17:54:40Z**, at ACCOUNT blob `8b7e4e6367ad4bc366c04b930ef4fc3810e2a900`. Historical1277/8192, unspendable6915, attempt1/1 and UNKNOWNs remain intact. No allowance, refund, clock reset, source patch, self-approval, reviewer invocation, ordinary acquisition, N1/N2, production CI, merge or release. D/C/H, H ALLOCATION/#547 and all closed/protected records retain their original scope; certificates remain0/3.

The remaining transition is a precise original-project preparation/configuration/derived-sidecar compatibility or authentic prepared-input disposition, followed by real stage/source/account admission. It is **not** another engine transfer, GitHub grant, COMPAT-2 implementation/review or automatic top-up. No such further scope is selected in this author handoff.

## Evidence and readback

STATIC-OBSERVATIONS.json.xz contains the actual byte inspector, manifest correction script, complete compiler streams and host/runtime/source records; STATIC-TRANSPORT.json gives decoded and file identities. ADMISSION-RECORDS.json.xz contains this pass's new exact-sidecar check, command/output, source map, structured admission and arithmetic, with ADMISSION-TRANSPORT.json for decoding. No engine/library/font payload is published. Source references point to exact immutable GitHub inputs, not an assertion that a quoted range is the full transitive closure.

The terminal #156 handoff supplies the containing publication head/tree and actual GitHub readbacks. E remains the reviewed execution input. The earlier capsule/publication snapshots are preserved at their original identities; current commissioning status is additive, not a retroactive rewrite of acceptance or historical outcomes.
