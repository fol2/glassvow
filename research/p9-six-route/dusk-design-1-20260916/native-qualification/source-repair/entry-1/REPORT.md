# DD1-LINUX-ENTRY-1 — pre-launch disposition

**BLOCKED at stage 0, before engine entry.** Owner selection is recorded; no complete engine unit was admitted. This is the finite graph's pre-launch stop branch, not ENTRY_VERIFIED, FIXTURE_VERIFIED, an observed Godot incompatibility, or a completed native qualification.

## Authority and unchanged candidate

The owner selected proposal #421/5734400738 directly in the author instruction. Selection #421/5735810042 was published and fetched back, created 2026-09-18T20:30:07Z. It permits engineering-only commissioning with at most min(8, actual lawfully available recovery remainder), no top-up or campaign. No further owner approval was requested.

The exact accepted candidate is E=`3bb4205caf55f882a4507272445997ae448fcd7b`, tree `56ad1534dcec2c2d0f5d3f5125dd152065c5ec45`; B1 implementation I=`efdac6a25dd0436de5ef2c52136b5f8ce71c4da7`. B1 review #542/5732200372 and acceptance #542/5734355049 remain in force within source/inert scope. Operating main was refreshed at `07b5aa9dec8436132a524511d5438c510e322070`. This delivery changes only this task's records and the existing capsule, not B1, GDScript, production, Pilot or accepted H code. The final GitHub diff/readback and handoff anchor the publication head separately from E.

## Actual stage-0 work and blocking evidence

**Fixed engine identified, executable not staged.** The official GitHub release is 372477026, tag 4.7.2-stable, non-draft/non-prerelease. The specified standard Linux x86_64 ZIP is asset 519677358, 77,860,424 bytes; the release metadata gives archive SHA256 `cadd3204e728a35d3f13adb7fd0d7902636b79f6b95c40c265eb73b6c35329e4`. This is an expected archive digest, not an independently measured executable digest.

Normal HTTP retrieval in the execution container failed DNS resolution for api.github.com and godotengine.org. The download tool did not transfer the archive: it required a web-viewed URL, while the corresponding web open returned a cache miss; the subsequent download attempt still refused before creating a file. GitHub connector reads did obtain release metadata. No release absence, HTTP 404, corrupt download or engine crash is inferred from those transport observations. No alternate version, .NET build, patched engine or existing toolchain replacement was used. There was no Godot executable on PATH or in the bounded inspected locations; this is not a whole-filesystem absence claim. The requested archive does not exist in the staging directory. Full actual HTTP-client errors and the transcribed tool errors are in RAW-OBSERVATIONS.json.

Without the executable bytes, the archive/extracted ELF identity, interpreter and full runtime closure cannot be independently verified. The pinned supervisor binary was not staged or rebuilt merely to proceed toward an already blocked launch. Source/runtime-copy costs and a complete useful stage demand therefore remain unclosed. This is the decisive pre-launch blocker; no capacity was spent to demonstrate a predictable setup failure.

**Supported host tuple observed, not an engine qualification.** The runtime reports Linux 6.18.44, x86_64 and glibc 2.41; Python is 3.13.5. These match the accepted named Linux venue components. No namespace/cgroup/Landlock/FUSE probes or B1 23+12 tests were repeated. The known fd-64, int-0x80 and clone3 limitations retain the review's original scope.

**Real H dependency closure materialised and checked.** The accepted H evidence_boundary.py and its reference_kernel.py dependency were recovered byte-exactly from H=`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`, with blobs `a88db0791572f430ea3e5cde85683c8527cead39` and `ee091fb503849117358b3691264fca71803f8bbc`. Their remaining imports are Python standard-library modules. The task-local checker ran in Python isolated/no-site/no-bytecode mode, imported the real files and checked their paths and hashes. A deliberately absent-authority context reached H's real `verify_bindings` and raised `MissingAuthority: missing_external_manifest`. Exit 0; the complete captured stdout and empty stderr are stored.

This is import/negative-boundary evidence, not a fabricated empirical context, a mocked H test, an accepted execution demand, full H integration or a numerical/statistical result. No numerical H suite, post-output `verify` receipt conjunction, backend launch or account debit was called. The positive prospective native demand was not manufactured with unknown binary/cache hashes. The owner selection is real; the exact executable/runtime-bound demand is not yet established. Future P_v/extraction outcomes were not demanded as commissioning prerequisites.

## Project/cache and resource diagnosis of the stopped stage

The accepted snapshot code copies required source bytes under `/source`; isolate.c mounts the root read-only and only `/out` writable. The actual project source retains its configured main scene/main loop, plugins, rendering and warning settings. There is no verified imported project/cache snapshot staged here, and no source-supported alternative cache mapping or bounded engine-thread configuration was established. One-core affinity is not being counted as proof of the engine's thread count. A generic `--import` into the immutable source is not an admitted plan; this run did not invent `.godot` content, hand-edit import sidecars, make `/source` writable, disable required functionality or relax a B1 guard. These unresolved requirements are recorded, not declared universally impossible.

Current-main check_imports.sh invokes one `godot --headless --import` and grades both exit status and stderr diagnostics. check_scripts.sh invokes an engine for EACH explicitly selected script and grades status plus `SCRIPT ERROR` / `Failed to load script`. Their shell wrappers also spawn utilities; they cannot simply become the restricted single-workload executable. Any real commissioning adapter must preserve the relevant gate semantics while admitting every underlying engine through the existing native entry and accounting for trusted controller work. No replacement gate or adapter was implemented or treated as reviewed here.

The unchanged fixture is tests/test_dd1_source_repair.gd, blob `0fd416c2c904220cfd2622f46b7188dc1578586f`. Its declared minimum is one engine plus two actual contained combat creations; its forced topology/results remain synthetic. The no-third-start observation is a failed-capture refusal in the written fixture, not an ordinary third combat. None of these native assertions ran.

Eight starts would leave at most four for ALL project/import/parse/other engine prerequisites after one identity process and the three-start fixture. That is only `8 - 1 - 3 = 4`, not a demonstrated complete envelope, proof that all required checks fit, or a reason to omit a relevant prerequisite. No exact deficit is invented while the imported-input and gate selection remains unclosed. CPU, raw-copy, syscall, thread and lifetime feasibility must also hold; nominal start arithmetic alone cannot admit stage 2.

## Accounts and stage transitions

The current GitHub account is still blob `8b7e4e6367ad4bc366c04b930ef4fc3810e2a900`. It reports 2040/2048 starts, 398617197992 ns of 14400000000000 ns CPU, and 893139 of 1073741824 raw bytes. Arithmetic remaining is eight starts, 14001382802008 ns and 1072848685 bytes. No `unit_reservations_v2` has been added to that unchanged blob. Old events and their uncertainties are not re-audited, refunded or silently reconciled. These are available GitHub ledger observations, not an independent certification of complete historical metering or another live executor's private state.

The historical account remains 1277/8192, arithmetic 6915 unspendable, attempt 1/1 consumed and CPU/elapsed/raw UNKNOWN. The recovery window remains 2026-09-17T17:54:40Z through 2026-09-24T17:54:40Z. No new account, grant, reservation, live-account write, clock reset or additional capacity was created. Static planning/import-control records are separate from native workload evidence; no claim of measured engine CPU/raw or completed execution-snapshot metering is made.

The graph is: **0 BLOCKED -> 1 NOT_ENTERED -> 2 NOT_ENTERED -> 3 terminal evidence/account handoff**. Stage 3's publication and containing head/tree are confirmed by the single #156 handoff and GITHUB-READBACK.json. STAGE0-MANIFEST.json intentionally has null execution demand, argv and grant rather than plausible but unverified values. A complete prospective stage manifest must be published/read back before any eventual launch; this blocked manifest cannot be used as one.

## Remaining prerequisite, not a new authorisation request

A permitted artifact channel must first make the exact official ZIP/executable available in the supported execution venue. Then its actual hash/ELF/library closure, pinned helper, unchanged H/controller imports, immutable imported inputs/cache layout, bounded engine settings and genuine H-backed prospective demand must close within the existing remainder/deadline before stage 1. Any B1/engine restriction needing expansion stops under the original graph; no automatic source patch, engine switch, top-up or campaign follows.

Accepted B1/D/C/H, #547, H ALLOCATION, the three-strategy goal, protected/exposed cohorts, old outcomes and missing historical trace bytes retain their scope. No ordinary acquisition, P_0/P_5 search, N1/N2, alpha/#548, self-approval, reviewer invocation, production PR/CI, main mutation, merge, closure or release occurred. Certificates remain 0/3. No P_5 success or macOS/Linux equivalence is inferred.
