# DD1-B1-COMPAT-2 — dormant engine-demand and prerequisite map

**NOT A LAUNCH MANIFEST. No executable demand, reservation, active grant or
native authority is created here.** This source/inert batch has performed zero
engine runs. Source head `d8ea1d696242ed26c8c598474e39e68b4800ba9c`, tree `aea292f13a8096560e1cb2c507acc806cb9e0e2d`.

The later engineering graph remains DD1-LINUX-ENTRY-1, bounded by the existing
recovery account and 2026-09-24T17:54:40Z deadline. It is not renewed by this
document. At most min(8, actual lawful remainder) engine-plus-contained starts
can be committed across that entire operation. Expiry is a stop, not a reset.

## Required stages and observations

1. Static admission, no engine. Verify mounted official bytes and exact accepted
   compiler/helper/controller/engine/library/source/content identities. Materialise
   unchanged H. Close whole-program inputs, output paths, resource demands and
   trustworthy host context. Publish/read back an exact prospective stage manifest
   before any later launch. A caller-written hash or synthetic context is not authority.

2. Identity: the profile accepts only the bound argv shape `[engine, --version]`.
   Reserve one engine start and zero contained starts. Correct version text alone
   is insufficient: require the exact refusal class/finite counts, no unexpected
   denial/resource/cleanup failure, successful task status and preserved raw streams.
   Expected version text in current source is4.7.2.stable.official.ed1daf0bf.
   This stage is not project/GDScript/import qualification.

3. Preparation: exact bound shape is `[engine, --headless, --path, /source,
   --import, --quit]`. One engine process is one start, including a failed attempt.
   Use untouched original inputs plus explicit generated output slots; never grant
   writable original source. All generated paths, temporary paths and byte maxima
   must be predeclared. Validate exact inventory, successful task/cleanup and the
   resulting seal; failure or interruption cannot promote. No Godot-generated data
   is supplied by the harmless fixtures in this packet.

4. Parse: each bound shape is `[engine, --headless, --path, /source,
   --check-only, -s, exact-owned-script]`, using validated sealed input.
   `tools/check_scripts.sh` loops over each nonempty supplied/tracked path and
   invokes the engine once per path. It is **not one start for a multi-path wrapper**.
   It checks exit status and stderr for SCRIPT ERROR / Failed to load script.
   The exact narrow required script set is not established by these Python tests.
   The recently changed unit-grant consumer must not be silently exempted.

5. Fixed fixture: `[engine, --headless, --path, /source, -s,
   res://tests/run_all.gd, --, --tests=res://tests/test_dd1_source_repair.gd]`.
   One engine plus two contained combat creations consumes a three-start unit.
   The source fixture is unchanged blob0fd416c2c904220cfd2622f46b7188dc1578586f.
   Its forced nodes/results are synthetic; they cannot become earned profiles or
   ordinary wins. Require its actual runner result, error checks, combat-identity,
   initial/preterminal/save/Dawn/clear chain and capture refusal observations.
   A third contained creation remains disallowed.

No ordinary acquisition, N1/N2, strategy certificate, production merge/release,
or numerical H acceptance follows from any engineering success.

## Cost map, not an invented affordable allocation

With one identity invocation, one preparation, m parse invocations and the fixed
fixture, total starts are5+m. That leaves room for at most3 parse processes
ONLY if those are all the necessary engine prerequisites. No prerequisite may
be skipped to fit eight. Extra setup/parse/failing invocations add their own cost.

The existing receiving manifest records engine plus six declared libraries
(including the interpreter) at149655752 bytes per copied runtime closure.
At m=3, six engine processes alone require897934512 bytes of cumulative runtime
payload copies. Against the published remaining raw arithmetic1072848685 bytes,
that leaves174914173 bytes before any source/content/helper/control/output,
generated/preparation/sealed copy or temporary-layout costs. It is not proof
the actual useful sequence fits. Full scenario arithmetic is retained in
DORMANT-BUDGET-ARITHMETIC.json.

Use the actual implementation's full demand formula, not compressed archive size:
- snapshot setup includes helper and each actual runtime/source/generated file
  plus path/creation metadata;
- preparation adds a complete sealed copy, generated maxima, journal/manifest
  reserve and ALL predeclared temporary-file/directory layout;
- entry includes receipt/account input copies and control metadata;
- workload raw covers every repeated write and metadata request before effect;
- failed/interrupted units retain the whole reserved envelope.

The bound CPU domain is11..300 seconds per complete unit, not300 seconds just
for the child. The controller/supervisor/workload partition and enforcement
headroom are inside that total; installed workload ceiling isU-10.
The actual engine-specific CPU/wall/raw budgets remain unset. The successful
small inert workload does not price a150MB runtime or the game's import work.

## Complete known-gap categories

**Engine/runtime:** the retained fixed archive/executable pins are
cadd3204e728a35d3f13adb7fd0d7902636b79f6b95c40c265eb73b6c35329e4 and
8d106cbe6144c2dc7e881d61d2429c1a8a76e6b22ef48bd5e48dcf934953f71e.
The six-library/interpreter DT_NEEDED and symbol-version manifest is useful
prior static evidence, not optional dlopen/fontconfig/platform/plugin/data
closure. Exact helper and the controller's dependency/environment closure also
need later host admission. No engine is replaced or compiled here.

**Threads/ABI:** the fixed upstream worker loop calls naming then continues;
that expected refusal is now bounded and tested as a capability class.
Editor/project-manager startup selects default worker-pool sizing, not the
non-editor project setting. All lifetime births must include main, worker pools,
resource/import tasks, audio, network resolver, rendering and enabled plugins
that are actually reachable. Affinity, reserve(5), one get_nprocs result or one
successful harmless4-thread fixture is not that whole-engine proof. The hard
ceiling remains four lifetime threads, including main. A further required
capability is not automatically added to this profile.

**Game/static preload closure:** the fixture directly preloads
dd1_native_main_route.gd, dd1_unit_grant.gd and dd1_route_journal.gd.
The route preloads test_map_compose.gd, balance_pilot.gd, launch receipt,
driver-main, ordinary capture and unit grant; the driver extends global Main.
Main's constructor invokes GlassStyle.theme and creates HintGuide; the route
constructs transition/music/SFX nodes. ContentDB.load_full(false) and global
classes/resources are real dependencies even though the two combat outcomes
are synthetic. The inspected direct edges are recorded in STATIC-SOURCE-REFERENCES.
A complete transitive source/content/font/resource inventory, global-class and
UID/import lineage is still required. Direct preloads are not asserted to be
the complete engine source closure.

**Original editor plugins:** project.godot enables Funplay MCP and the two export
plugins. The inspected Funplay entry creates settings/server when in editor
mode; fresh settings default server_enabled=true and its start path probes a
port and invokes transport.listen. This is a concrete networking prerequisite
to settle for the actual preparation invocation, not a newly observed engine
denial. Whether exact --import startup loads that code, and the remaining
export/plugin/native-library paths, is unexecuted. Do not silently disable the
plugin, preload substitute functions, grant sockets, or infer absence of these
dependencies from headless rendering. Source-supported nonexecution or a
separately valid prospective configuration is required before affected admission.

**Generated layout:** current code protects original files continuously and
allows explicit .godot directory / .uid / .import file slots. Actual native
writers must fit their declared paths, rename/temporary behavior and inventory.
Tracked original sidecars cannot simply be shadowed; stale/missing/spliced
caches and unqualified Mac cache copies remain invalid. Harmless bytes named
DERIVED-INERT / INERT-UID are explicitly not Godot cache metadata.

**H and consumer:** exact real H imports and refusal/role-shape checks passed.
They provide no authenticated native positive context. The later host must
bind the exact demand/head/profile/stage/argv/environment, accepted amendment
disposition and any recipe/seal through H's prospective verify_bindings boundary.
Do not require future profile or extraction outcomes for prerequisite engineering
entry, and do not manufacture them. The actual GDScript consumer amendment
still needs its allowed later parse/fixture observation; source-marker checks
do not satisfy that native evidence.

## Gate and stop transitions

All engine stages remain NOT_ENTERED. `launchable=false`, `execution_demand=null`,
`reservations=[]`. The source/inert artifact has no self-approval.
First publish this actual source/evidence and exact-head handoff with readback.
Then one fresh scoped review covers the selected amendment and changed
interactions. Only a valid reviewed/accepted, affordable, unexpired and genuinely
host-bound stage may return to the already-selected engine graph.
Unexpected denial, failed task, resource/cleanup failure, unclosed dependency,
unaffordable useful stage or expiry stops the affected native stage. No automatic
top-up, repeated green run, new account or changed game law follows.
