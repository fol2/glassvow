# DD1-B1-COMPAT-2 — source/inert validation, unpublished handoff

**Source/inert controls validated locally; GitHub evidence publication is BLOCKED.**
This is a recovery/delivery packet, not a new GitHub handoff, independent review,
engine admission, N0 acceptance or strategy certificate.

## Exact scope and source

The owner selected option A in #421/5744752364; the existing selection and task
remain #421/5745100317 and #542/5745114075. This continuation uses that same
source/inert authority. No new grant, account, deadline or engine allowance exists.

Validated source head: `d8ea1d696242ed26c8c598474e39e68b4800ba9c`  
Tree: `aea292f13a8096560e1cb2c507acc806cb9e0e2d`  
Retained branch: `research/p9-six-route/dd1-native-1-overlay`  
Current operating main: `07b5aa9dec8436132a524511d5438c510e322070`

GitHub reads confirmed the branch remained at this head after validation and
the live ACCOUNT blob remained `8b7e4e6367ad4bc366c04b930ef4fc3810e2a900`.
The 22 files in `evidence/SOURCE-IDENTITIES.json` were recovered from exact
GitHub objects, hashed before execution and checked unchanged afterward.
The source directory is the complete recovered closure needed by the included
build/matrix/binding commands, **not a checkout of the whole game repository**.

The new remote head was already present on arrival. It contains a prior
65-assertion checkpoint summary at `compat-2/VALIDATION-CHECKPOINT.json`, but
not the complete underlying raw records in that folder. This continuation did
not adopt those assertions as newly executed proof: it rebuilt the exact
binaries and executed the current strengthened matrix, retaining all records.
The current matrix has 69 groups, including four additional finite-bound
binding controls. No repository source repair was necessary to pass it.

## Actual verification performed

The owned static supervisor and harmless dynamic fixture rebuilt successfully
using the committed build script. Both compiler invocations exited 0 with
empty stdout and stderr. Supervisor SHA256:
`b22fdaba90df2a1a317f1d3a690522736a1301f9b442016764d5bf173f2fdf0f`.
Fixture SHA256:
`00c91a9e612699082649e44536a8f40c436572378841ee4eaf70e8c45d1f7fa7`.
Pinned libc/loader also matched. Complete compiler arguments, streams and
binary identities are in `evidence/BUILD.stdout.json`.

**The full current connected matrix exited 0; all 69 assertion groups passed;
checker stderr was empty.** This is not 69 successful strict-B1 units.
The matrix deliberately expects failures for forbidden effects, altered
bindings, resource exhaustion, invalid task results, corrupt preparation
and interruption. Each control retains actual commands, synthetic accounts,
demands, grants, kernel refusal records, stdout/stderr and result state.

Combined refused process creation and refused naming completed a useful
threaded atomic save/readback. Compatibility succeeded while strict B1
correctly remained failed: one classified process refusal, one naming refusal,
two separately recorded clone3 ENOSYS responses, four total refused requests.
The unrelated same-signature spawn is reported only as CAPABILITY_CLASS_ONLY,
not authenticated xdg-user-dir origin. A second/wrong/late/worker process
request, repeated naming, other prctl, excess clone3/thread creation,
later exec and unknown syscall all retain failure.

CPU controls verified actual installed limits at total demands 11,16,300 and14.
The 16-second unit completed more than four seconds of workload CPU
(actual recorded 4.202279 s), beyond the former inherited three-second cap.
The 300-second unit verified installed workload soft/hard limits290/290
without burning that maximum. The 14-second unit was killed by its four-second
workload CPU limit (signal9, recorded workload CPU4.135258 s).
The pure partition control checks every integer total11..300; it is arithmetic
coverage, not an empirical300-second stress test.

Preparation controls exercised kernel read-only originals, actual harmless
generated outputs in declared slots, nested/temporary inventory accounting,
before-effect symlink/hardlink refusal, successful sealed promotion, and
rejection of transient/unexpected/missing/corrupt/failed outputs.
A later runtime read the sealed copy even after its former mutable generated
backing was deliberately changed. This is **not Godot import evidence**.
Host-side corruption injection is explicitly test setup after the previous
workload exits; it does not make runtime source writable.

Controller and supervisor SIGKILL controls preserve full synthetic charges and
executor exclusivity. Interrupted preparation does not produce a valid seal.
Controller death leaves RESERVED until the existing no-credit reconciliation
uses absence evidence. All126 distinct recorded matrix process identities
were absent in the post-matrix check. The matrix exercised42 harmless workload
releases; the separate matched positive added one. These are inert fixtures,
not43 engine or contained-game starts.

**The real-H/public-entry checker also exited0 with22 checks and empty stderr.**
It imports exact unchanged accepted H files at
`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`. No H mock, numerical suite,
empirical positive context or future outcome receipt was used. Tests include
actual public-entry refusal of missing authority, callbacks and synthetic
context; missing/altered roles; finite profile prerequisites; supplied
diagnostic-byte checks; and the fixture grant consumer's source-routing
markers. The consumer's GDScript parse and runtime remain UNEXECUTED.

## Infrastructure failure retained, not hidden

The first attempted connected positive failed before any reservation with
`dedicated single-thread controller required`. Own-process inventory identified
an inherited jemalloc background thread (`jemalloc_bg_thd`) before repository
imports. The relevant Python-tool environment had background_thread:true.
That failed command, its account/output observations and full streams are
retained as FIRST-CONTROL records.

A controller-only allocator setting background_thread:false restored a genuine
single-thread process and the matched positive passed. The full matrix was
then executed through the normal container shell, whose LD_PRELOAD was absent
and MALLOC_CONF was empty; this exact environment is recorded separately.
No guard was removed, no engine setting changed, and no preload shim was
introduced. The supervisor and workload still use the committed fixed
environment. These are two recorded launch-environment observations, not a
claim that the full matrix inherited the Python-tool allocator override.
Jemalloc's documented option is the basis for this controller provisioning:
https://jemalloc.net/jemalloc.3.html#opt.background_thread

## Consolidated obligation status

A — Connected capability profile, strict/compatibility/task separation, finite
refusal bounds and identity/demand refusals: exercised by the real current
matrix and real-H/public-entry checks. Native authenticated positive remains
a later host admission, not an output of this source-only task.

B — CPU bootstrap/inheritance/aggregate partition: supported by the installed
limit controls, actual above-three-second workload and bounded exhaustion.
The original reserved envelope is not enlarged. No claim that the earlier
strict artifact already proved these changed interactions.

C — Protected preparation, mutation accounting, validation and sealed-input
lineage: connected harmless positives/adverses passed. Authentic engine
generated cache/UID/import outputs do not exist in this evidence.

D — Dormant engine-stage/known-gap map: see DORMANT-DEMAND.md and arithmetic
JSON. Native resource/import/plugin/thread closure and real host-authenticated
stage admission remain unclosed. They do not negate these completed independent
source/inert controls and are not silently waived.

## Evidence completeness and preservation

`evidence/matrix/` contains79 JSON record files, including INDEX/SUMMARY and
pre-release/concurrency/replay observations. The full checker streams and
command record are separate. POST-MATRIX-ARTIFACTS.json retains306 end-state
files (including grants, results, streams, generated bytes and seals) without
following aliases or embedding executable/library binaries. During-run
observations remain in the per-control records; end-state collection is not
mislabelled as an earlier observation.

Source identities, real-H outputs, compiler streams, the initial setup failure,
corrective controller inventory, actual matrix outcomes and post-run cleanup
are all in this packet. Earlier failed/development evidence remains at the
existing GitHub history; it was not rewritten or promoted by this continuation.

Both real accounts were untouched. Recovery remains reported2040/2048 starts,
CPU398617197992/14400000000000ns, raw893139/1073741824B. Historical1277/8192,
unspendable6915, consumed attempt1/1 and UNKNOWN observations remain unchanged.
Deadline remains2026-09-24T17:54:40Z. Account identity verification is not
retrospective metering-completeness certification or knowledge of another
executor's private state.

No engine/Godot/engine-loader/version/import/parse/game/profile/capture run,
live-account write/reservation, ordinary acquisition, N1/N2, self-approval,
reviewer invocation, production CI, merge or release occurred. D/C/H, H
ALLOCATION/kernel/#547, the fixed official engine, prior strict-B1 acceptance
and closed/protected evidence retain their original scope. Certificates0/3.

## Publication boundary

This session exposes GitHub read actions but no create/write action. The normal
Git probe returned128, Could not resolve host: github.com. See the actual
PUBLICATION-STATUS.json. No new GitHub comment, commit, branch update or final
readback receipt was produced. Do not mistake this local packet for the requested
completed GitHub delivery.

Next delivery action is publication of this actual evidence, the reconciled
capsule and a single #156 handoff on the retained branch, then file/comment/head
readback. Do not rerun69+22 merely because publication was unavailable; rerun
only when changed source, environment or evidence integrity invalidates a
specific result. One subsequent fresh scoped review is still required by the
selected contract. Neither this report nor successful inert tests authorises an
automatic engine run.
