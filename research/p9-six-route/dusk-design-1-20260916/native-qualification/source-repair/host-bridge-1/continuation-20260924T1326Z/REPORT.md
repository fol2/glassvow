# DD1-HOST-BRIDGE-1 — source/inert integration handoff

**Author source/inert delivery complete; independent whole-bridge review next. Live bootstrap is NOT qualified, custody is UNREGISTERED, and native admission remains BLOCKED_BEFORE_RESERVATION.** This is not self-approval, a native-positive receipt, ledger adoption, or completion of DD1-LINUX-ENTRY-1.

Authority is owner option A in #421/5812756062, recorded by selection #421/5813769137. Read-only custody registration is #421/5813829774. These records were retrieved through the GitHub connector; they are not claimed as a successful execution of the adapter's own HTTPS bootstrap. The proposal explicitly permits a precise unavailable-channel outcome while independent work is completed.

Whole HOST-BRIDGE base is `2fcfbfced835eb4c4e61a19abbb25dcc4c4a6122`. This continuation resumed saved source `cb2e41ad4353b9e9167add8a2b96c4b826b7f646`; final tested implementation is `f6202b33fe5d451fee4cbf155fc1a4e3ff9c2061`, tree `1e50f284580366a5e476b6bf5985f8933e425328`. Later commits add evidence only until the containing report/index/capsule commit. Its exact identity and final readback belong in the single #156 handoff, not an invented self-referential hash in this file.

Current-main AGENTS and the applicable AI-SDLC/domain rules were refreshed at `07b5aa9dec8436132a524511d5438c510e322070` without rebasing frozen science. One Pro author worked; other writers and reviewers remained parked. Accepted FIT at `5cd51a959ee98e68ede46d1d86d68f30240fdd80`, PREP/B1/COMPAT-2, H, existing enforcement and both accounts were preserved.

## Concrete host integration

The new files are `tools/dd1_host_channel.py`, `tools/dd1_host_custody.py`, `tools/dd1_host_bridge.py` and `tools/test_dd1_host_bridge.py`. No existing native entry, backend, helper, recovery ledger or H implementation is amended.

The source now names the prospective producer: owner-controlled bridge code using the fixed repository `fol2/glassvow`, owner login/id `fol2`/105634418 and issuing role `github:fol2/glassvow:owner-105634418:DD1-HOST-BRIDGE-1`. It no longer delegates to an unnamed pre-existing runner. That designation alone does not make technical claims or arbitrary JSON true.

`GitHubReadOnly` uses fixed-origin HTTPS GET to api.github.com, normal TLS certificate/hostname verification, bounded JSON/file decoding, no redirects, exact comment/repository/owner matching, and Git blob plus byte checks. Only an already-configured GH_TOKEN/GITHUB_TOKEN may be used; no credential discovery, new credential, logging of tokens or offline authority fallback exists. The process-local client closes before workload entry. Workloads receive the existing restricted environment and only the admitted snapshot, never the bridge's credentials or host-policy files.

`inspect` reports separate window, host, controller, custody, authenticated retrieval, deployment, qualification, local-input, price and existing-validator rows. `commission` performs the same live bootstrap before calling the unchanged public `run_complete_unit` in the same process. It accepts no supplied TrustedContext, authority callback, replacement runner, clock or alternative installed root. It does not fork to reset the existing one-thread/less-than-one-second controller requirement.

The authenticated records must bind exact artifact, stage, host, account generation, role bytes and qualification. The adapter consumes compatibility/PREP/FIT dispositions, thread/writer evidence, exact native input/output contracts and cumulative prices; it does not generate `complete_creation_site_closure`, `all_reachable_writers_qualified` or `launch_admitted` from ownership or a passing fixture. Required future generated outputs are still prospective bounds, not fabricated existing files. H's later statistical/P_v/extraction receipts are not early engineering prerequisites.

Before trusted composition, code-pin validation walks transitive dd1_ imports and checks both accepted H modules. H remains the external dependency at `5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`; local materialization for tests is not an overlay commit or a merge of the old research runtime. The internal empirical transport is created only after live authenticated inputs and known admission gates qualify. Python object encapsulation is not advertised as protection against arbitrary code already running inside the trusted host.

Two completion corrections were tested and committed together: selection now requires the exact already-issued body SHA256 `f3e2dc66284f50995b542d853681cf388d9fea09f9f9b7a7c72f9fe3b260d7cc` after TLS/issuer validation, instead of keyword matching; and bootstrap refreshes all six mutable authority records plus custody before entry, rather than only registration. Changed body or updated_at refuses. These rereads are not an atomic transaction across GitHub and a filesystem and do not prove distributed exclusivity.

## Actual bootstrap and custody evidence

The actual nonexecuting command used `/usr/bin/python3 -S tools/dd1_host_bridge.py inspect`, implementation head f6202b33, and a deliberately incomplete preparation inspection object. At `2026-09-24T13:55:15.931962Z`, its host, fresh single-thread controller and exact local H bytes passed their narrow checks. Host was Linux6.18.44/x86_64/LP64/glibc2.41, boot `a0f555bc-1e32-40cc-ba07-006483f0de9a`. Controller baseline was 0.056495 CPU seconds; this is not a proof that a complete future successful bootstrap fits that limit.

Its indispensable interface failed exactly at HTTPS GET `/repos/fol2/glassvow/issues/comments/5813769137` on api.github.com: `ChannelUnavailable:api.github.com HTTPS GET unavailable: gaierror`. Exit2, native_admitted=false, executed=false. There is **no successful actual adapter TLS retrieval/qualification positive**. Synthetic transport tests and connector reads below do not fill that gap. No transport ladder, redirect, relaxed TLS, copied offline owner record or invented provider was substituted.

Custody uses the actual recovery module's ACCOUNT and `.lock` paths. The inspection found no account at the installed source root. One additional targeted read at `2026-09-24T14:26:14.965634Z` used only the original executor root named by registration: `/Users/jamesto/.grok/worktrees/coding-glassvow/2026-09-17-1969f3e5`. It returned FileNotFoundError at that path on this Linux host. This is not global absence, a new broad workspace scan, or a remote inspection of the original machine.

Registration #5813829774 therefore correctly remains SOURCE_INERT_ONLY/UNREGISTERED. Actual custodian, executor identity, current live generation/hash, reservation-history hash and release record remain null; deployment/qualification comment locators remain null. A `pending=[]` field on an observation without an account is not a cleared-reservation assertion. Lease was NOT_INSPECTED; zero locks were acquired and no live account was copied, created, adopted or written. The published ledger snapshot is not promoted to authoritative custody.

Restart rules are explicit: preserve the existing account bytes and outstanding reservations; authenticate the actual original custodian and same-executor identity; reconcile unknown costs and interrupted history without refunds; confirm other executors retired before use. This source task does not implement or select a cross-host transfer. The bridge rejects custody mode changes/different executors. Any later different-executor handoff requires an actual released/reconciled custody decision, not a Git snapshot or local flock. After admission, the unchanged public entry still performs its exact-account checks and reservation lease transaction.

## Proof, with attribution kept separate

`SOURCE-BUNDLE.json` retains the complete original 12-method run and final **13-method/88-record passing suite**, source-before/after identities, real accepted H role checks, actual bootstrap command/streams and the targeted custody/code-pin observation. The first suite is not counted again in the final total. Synthetic transport fixtures test TLS-origin/GET/redirect rules, owner/scope/head/host/byte changes and credential redaction; they are not an actual GitHub authentication positive. Pure custody and deployment shapes test generation/history/transfer/pending/expiry/review separation. Real H checks all supplied prospective role identities in an explicitly synthetic context, not an empirical positive or numerical-suite replay.

The wrapper spy proves only argument forwarding. Separate tests exercise the unchanged reservation test seam in process and preserve complete synthetic account transitions, including interruption/replay. Those are explicitly not OS confinement evidence. A further targeted custody observation is separate from four source-pin checks: recursive import-pin shape, omitted transitive source, local source substitution and changed H. The recursive pin test uses declared synthetic module texts; it is not full native code/dependency qualification.

`CONNECTED-PLAN.json` was committed and read back before the two genuinely new harmless workload units. `CONNECTED-BUNDLE.json` contains complete scripts, source manifests, compiler commands, raw units/receipts/grants/accounts, streams and cleanup observations for the actual route:

`bridge.inert -> existing _run_inert_unit -> existing reservation/backend -> pinned Linux supervisor -> pinned harmless inert.c`.

Two compiler commands exited0 with empty compiler streams. The 958936-byte supervisor SHA256 is `41a4529af3bb2fb3f065d164dee8ac9cd2af7d761311c5ee63538347a44e45a4`; the 893192-byte static harmless fixture is `ead0fc4d0660d8a691469296c59827c66bad717b98d54671ecdb6eed8dcc9129`. Both match existing accepted pins. No allowlist or enforcement policy was changed. No engine, dynamic engine loader, actual Sentry or font payload was used.

The positive unit began at `2026-09-24T14:21:02.478475Z`, exited0 and returned strict backend success. The independent task observation checked stdout exactly `OK SAVE THREADS\n` (16 bytes), stderr `ERR\n`, save bytes `{"v":2}\n`, and two THREAD files. Three lifetime threads including main were recorded. Its full synthetic reservation completed and controller/supervisor/workload cleanup was observed.

The initial evidence reader incorrectly expected a seventeenth NUL in stdout and stopped after the workload had succeeded. That failed reader, original command and raw result remain intact as `connected-RESULTS.json`. The correction changed only the reader, reused that exact positive and did not rerun the workload. `connected-REMAINDER.json` links its predecessor by length/hash and records the corrected byte checks. Neither fixture nor backend was changed to pass.

A same-demand replay then refused before another reservation/workload and left account bytes unchanged. The second permitted harmless unit held at LIVE after durable process binding. The observer matched controller/supervisor/workload identities and killed the controller with SIGKILL. Full reservation remained RESERVED, before-kill account bytes equalled after-kill bytes, and no final UNIT-RESULT was invented. Parent-death containment and the evidence observer's own-descendant reaping produced recorded absence of all three identities. This is two released harmless units, one pre-reservation replay refusal, and six original workload-tree process identities with recorded cleanup—not another FIT matrix.

Each unit reserved one synthetic start, 16 CPU seconds and 12582912 raw bytes, with zero contained-game starts. Total disposable-unit envelopes were two starts, 32 CPU seconds and 25165824 raw bytes. These are reservations, not measured consumption or charges to either real account. Source identities stayed unchanged before/after. Measured compiler/command CPU and wall scopes are retained separately; total session/static-write/token costs are not fabricated or set to zero.

`VERIFICATION-BUNDLE.json` retains the exact safe reader and its new command/result. It decodes both complete transports, checks their lengths and compressed/decoded hashes, verifies 29 embedded hex-record identities and demand/receipt/charge/replay/interruption/task-byte relationships, checks source/H identities and the preserved reader correction, and exits0/PASS. It never executes embedded commands/code. This is new verification of retained evidence, not workload replay or independent review. Binary/source references and omitted duplicate private-root payloads keep their stated pin-based provenance.

## Usage and remaining deployment boundary

From the installed exact bridge/source tree with the exact accepted H dependency in its declared location, the public nonexecuting usage is:

```sh
python3 -S tools/dd1_host_bridge.py inspect \
  --head f6202b33fe5d451fee4cbf155fc1a4e3ff9c2061 \
  --unit /absolute/path/to/the/existing-stage-demand.json
```

`inspect` performs no reservation. The actual incomplete inspection input and complete command are retained in SOURCE-BUNDLE; they are not a launchable manifest. A later `commission` action uses the same interface only after separately admitted deployment and real custody/qualification. No runnable empirical example or forged positive receipt is supplied by this source/inert handoff.

The smallest current operational obstruction is the concrete adapter GET interface's DNS failure, alongside unavailable authoritative ledger/custodian fields. Resolving DNS alone is insufficient: actual distinct bridge/qualification review, scoped deployment record, native stage/build/thread/input/writer/output-bound/cost evidence and same-account custody must also qualify. The original account deadline remains `2026-09-24T17:54:40Z`; source work and review do not extend it or renew eight nominal starts. An expired window must be preserved, not bypassed.

## Preservation and next action

Published ACCOUNT stays blob `8b7e4e6367ad4bc366c04b930ef4fc3810e2a900`: recovery2040/2048, CPU398617197992/14400000000000ns, raw893139/1073741824B. Historical1277/8192, consumed attempt,6915unspendable and UNKNOWN history remain. Launch receipt stays blob `a99a29b76e645c8a4052f0cd43fdf58d2dd46a2a`. Final-head GitHub readback is required in the terminal comment; no private-state attestation is claimed.

The next gate is one fresh, isolated review of the **whole HOST-BRIDGE base-to-final artifact**, including this trust/custody composition and evidence limitations. The author did not invoke or impersonate that reviewer. Accepted unchanged FIT/PREP/B1/H are dependencies, not new review tasks. No further source polishing, broad custody scan or old control replay is assigned by this handoff.

No engine/loader/version/import/parser/game/actual-Sentry run, live account change/reservation/adoption/transfer, new allowance/credentials/provider, statistical spending, ordinary acquisition, N1/N2, self-approval, production PR/CI, merge, closure or release occurred. N0 remains BLOCKED and certificates0/3. Checkpoints preserved source and complete proof progressively; READBACK.json and the single #156 handoff bind final publication.
