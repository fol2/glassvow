# DD1-RUNTIME-FIT-1: connected proof and conditional admission

Authority: owner option A in #421/5793093948, recorded in #421/5793181710 and #542/5793187593. Run `runtime-fit-completion-20260923T185813Z`. Sole Pro author; no reviewer or native issuer. This is a newly authored completion map, not recovered history or a native-positive disposition.

## Exact source and preserved boundaries

Whole FIT comparison base is accepted PREP-1 `8e13ccd6ca045a17df75597f5c06d31ecd177947`. New connected checks use preserved source `b6b7d24a3c995f5481e33ea2891d266cce9916d0`, tree `077522ede1ce0a342c9928697c68b909bc7fee42`. Earlier positive execution used `b82d4c47d033a57dae91ee57907b09e44d479fbb`; the actual comparison from that head to b6 changes evidence only. The current completion does not change implementation. Its complete local 20-file controller/helper/fixture closure was materialized from pinned GitHub bytes and identity-checked before and after the commands.

Current-main operating instructions were independently refreshed at `07b5aa9dec8436132a524511d5438c510e322070`, without rebasing frozen research. Accepted PREP-1, B1, COMPAT-2, D/C/H/#547, allocations and original missing-record history retain their exact scopes. No renewed PREP review or old numerical suite is part of this batch.

Official engine SHA256 remains `8d106cbe6144c2dc7e881d61d2429c1a8a76e6b22ef48bd5e48dcf934953f71e`; source G is `ed1daf0bf001b61586d9930840f2f1394092c079`. Neither engine nor loader, actual Sentry library/handler, fonts, or game was executed or restaged. Both real accounts remain at published ACCOUNT blob `8b7e4e6367ad4bc366c04b930ef4fc3810e2a900`; only disposable synthetic accounts entered the tests.

## Implemented thread envelope

`tools/dd1_runtime_fit.py` fixes the selected implementation maximum at 14. This was declared before testing. The diagnostic storage is 32 refusal records: T naming refusals, at most T+1 clone3 refusals and one classified process-unavailable refusal require 2T+2 records; T=14 uses 30, retaining two-record headroom. It is a storage/validation maximum, NOT a derived native stage upper bound. Default/unselected behavior remains at most four. Non-integer, boolean, negative, zero, overflow and out-of-range counts cannot supply an expanded limit.

The exact profile binds operation, source, engine/fixture, helper, stage, argv, runtime, execution bytes, mode projection, mode map, host facts and lifetime count. Python validation, helper arguments, C profile checks, finite naming/clone3 allowances, grant and returned profile identity are connected. Lifetime accounting includes the initial workload and attempted allowed-shape thread births; joins and failed work confer no credit. Allowed clone flags, one workload process, first-exec restriction, single CPU, aggregate CPU/raw/lifetime/cleanup and memory protections are not widened. Unexpected syscalls remain failures rather than tolerated compatibility events.

The point-of-use reader host was Linux 6.18.44 x86_64 LP64, glibc 2.41, one standalone Python task, cpu_count 5 and affinity [0,1,2,3,4], boot `cbcf1bc7-18f0-4a25-9892-d9847d24d213`. Those are non-engine observations, not a native-admitted executor or the engine's measured/default pool size.

## Exact mode projection

For native input, the only executable source role is `res://addons/sentry/bin/linux/x86_64/crashpad_handler`, blob `a1127a09bc08f18cb4c4add7afcd11ef4dc02ecd`. The original Sentry descriptor is blob `47f4eb1723d5a61e706ce1fc078f9f68d6defa33`; the selected Linux debug/release library branch must match its actual pinned bytes and descriptor route. No basename, arbitrary source executable, runtime /source override, inherited host mode or wildcard supplies this role.

The private immutable copy is exactly 0555 before workload release. All three owner/group/other execute predicates are satisfied; no write/setid permission is introduced. Other immutable source remains 0400, ordinary executable runtime inputs retain their existing mode, seeded generated backing remains 0600/noexec and the sealed runtime restores the original configuration. Bytes, path, role, feature, dependencies and complete intended modes bind the demand and seal; load_sealed checks actual modes even when bytes match. Later exec, process creation and chmod remain prohibited. Mode metadata is not process authority.

The harmless stand-in is `res://inputs/fit-handler.txt`, exact bytes `DD1-FIT-HARMLESS-MODE-STANDIN\n`; it is never relabelled as the real handler. Actual native handler/library/build provenance must still be established before assigning the native role. This batch's mode execution evidence concerns that stand-in only.

## Complete connected evidence map: 46 cases

Canonical earlier positive unit: `../continuation-20260923T171716Z/POSITIVE-BUNDLE.json`, Git blob `041e938b3a1ab5a51e8701b04ba78efcfe91c319`. Its complete payload was freshly decoded/restored and source/grant/result/account/mode/seal relationships checked in this completion. `POSITIVE-READBACK-BUNDLE.json` contains the new reader, command and result. The underlying five positives were NOT rerun. Their original boot `aa13b145-bb3f-4f64-b274-3d719ce8aeb1` and timing remain separate from this completion.

All newly executed sections have complete outer commands, start/end/status/streams, source/build before/after identities, observer code, original per-case records and a stricter evidence-only verifier. Raw data is independently decodable XZ/base64, with complete lengths/SHA256 and original-record reconstruction identities. Pinned external source descriptors resolve actual immutable bytes, not placeholders. No terminal multipart tail is required.

| Family | Cases and decisive outcome | Canonical records |
|---|---|---|
| Reused positive: 5 | Preparation and same sealed runtime; concurrent work; boundary14; memory limit. Sequential boundary creates/completes 13 children with peak one worker, proving lifetime rather than peak accounting. Concurrent positive has five worker threads. Each checks 0555/all execute bits/read-only and atomic FIT1 save/readback. 3GiB allocation returns ENOMEM under the unchanged memory cap. | Earlier POSITIVE-BUNDLE plus current POSITIVE-READBACK-BUNDLE |
| New kernel: 8 | next-birth, race, chmod, exec, process, socket, raw, cpu. Every negative reached its specific intended target, not merely success=false. | KERNEL-COMMAND.json and independently complete KERNEL-CASES-1..4.json |
| New guards: 21 | missing/wrong profile; fractional/negative/boolean/overflow/too-large count; differing stage/source/helper/storage; mode500/writable/path/digest/feature/missing/extra/setid; naming and clone3 bounds. All reject before reservation and output creation with unchanged synthetic account. | GUARDS-BUNDLE.json |
| New sealed: 8 | One independently captured connected seal-parent, then same-byte500, missing-owner-bit455, missing-group-bit545, missing-other-bit554, dropped-profile, spliced-projection and seal-replay. Each substitution is rejected before new reservation. | SEALED-BUNDLE.json |
| New interruption/exclusivity: 4 | concurrent-controller, kill-controller, concurrent-supervisor, kill-supervisor. Competing entry is refused while the real held workload owns the lease. Controller kill retains RESERVED byte-identical to the pre-kill account; supervisor kill yields FAILED with cleanup and no seal. | CRASH-BUNDLE.json |

Specific kernel observations: at limit14, 13 children complete and the next child (15th lifetime thread including main) is refused with clone syscall56/errno95. The concurrent cap4 race admits exactly one competing final child and rejects the other. chmod90, later-exec59, fork57 and socket41 each reach errno95 and a failed enforcement outcome. Raw exhaustion reaches write1/errno27 with workload raw reservation262144 and observed reserved262108; subsequent failures are not relabelled as the primary target. CPU exhaustion reaches the six-second workload partition inside the sixteen-second complete-unit envelope, with SIGKILL and recorded workload CPU6.000153 seconds; it is not described as a wall-time or channel failure.

There are 41 new cases: 11 connected units and 30 pre-release rejections. The new units retain eleven full synthetic reservations (one start, 16,000,000,000 CPU ns and 12,582,912 raw bytes each). These envelopes are not measured expenditure or live-account charges. The 33 current-boot controller/supervisor/workload identities were observed absent in their recorded cleanup checks. Fifteen earlier positive identities retain their earlier boot's observation; these are not one invented same-run process count. Deliberately killed-controller final artifacts remain absent, not reconstructed.

Backend source persists process identities using attach_processes before sending G to release the workload. Raw unit/account/grant/result records bind that before-effect reservation protocol. All new original source bytes and both real accounts remain unchanged; negative results do not grant a refund.

## H and authority scope

Accepted H remains `5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6`, evidence_boundary blob `a88db0791572f430ea3e5cde85683c8527cead39`, reference_kernel blob `ee091fb503849117358b3691264fca71803f8bbc`. The intact previously verified real-H command is PREP revalidation `prep1-revalidation-20260921T211017Z/H-BINDINGS-COMMAND.json`, blob `785fa889dd66c8fbb1d89b5f48b42f3fcc2d6db2`. Its generic prospective-role and public-entry missing/callback/synthetic refusals are reused at their recorded scope. No old H numerical suite, empirical context or synthetic native-positive issuer is created in this completion.

FIT adds runtime_fit_profile, runtime_fit_modes, runtime_fit_projection, runtime_fit_disposition and runtime_fit_thread_bound roles after the existing real H.verify_bindings boundary. The native path requires a non-synthetic authenticated issuer, matching independent review/planner acceptance/launch disposition, exact profile/host/build/projection identities and a finite creation-site closure. It adds one initial thread to unique natural-number site birth counts and refuses a sum exceeding the selected limit. These checks enforce the bound attestation; source presence and a self-hashed statement do not establish its truth. The new empirical stage/thread/feature disposition is not tested by manufacturing an accepted empirical context. That remains an explicit future host input, not an inert positive.

## Per-stage finite bounds and remaining admission

Reuse the accepted PREP source map at `../../prep-1/revalidation/prep1-final-20260922T211833Z/SOURCE-ADMISSION.md` and its exact G/Sentry source witnesses. The following updates only the selected FIT deltas; it does not reopen accepted source/inert evidence.

| Stage | Established bound components | Native admission condition |
|---|---|---|
| Identity | Initial thread, IP resolver, actual default worker pool W, ordinary threaded dummy audio, plus selected startup backends/modules/extensions. Version handling is not a process-free probe. | No complete source/build-specific upper bound is supplied. The authenticated site map must bound startup and any pump/named-pool growth within selected T<=14. |
| Preparation | At least W+4 on the previously identified ordinary threaded path, with first scan, later/repeated scan births, import callbacks and platform/module effects separately bounded. Headless resource-preview worker remains excluded; ordinary import group tasks reuse pool threads. | Four was incompatible for W>=1. Fourteen removes the universal fixed-four limitation but is not proof of sufficiency. Repeated scan/pump/extension creation limits and the actually selected build/features remain specifically unbound. |
| Parse | One process per declared script. Script loading and setup precede check-only return; the selected MainLoop is not instantiated by a successful check-only return. Baseline W+3 plus other startup effects is not an upper bound. | Each parser unit requires its own authentic stage/site/source/library bound and full reservation; do not price several parser invocations as one. |
| Fixed fixture | Script-selected filtered SceneTree runner, not shipping main-scene/custom-MainLoop startup. Actual DriverMain/Main/theme/HintGuide/content/font constructors remain dependencies. Headless MusicBus/SfxBus exclude sample loads, not the engine audio thread. | Bound all reachable source/global-class/resource and native service births. No gameplay/acquisition/P_v claim follows from the harmless FIT fixture. |

W=5 inferred from this host's Python cpu_count would illustrate nine preparation baseline threads, not an engine upper bound or a host-authenticated launch demand. CPU affinity alone, peak concurrency, source feature names or a lower bound cannot justify a fourteen-thread native stage. Missing exact build/module/backend/extension and finite repeated/pump creation bounds remain M inputs; no new policy experiment or cap increase is authorised by a missing bound.

Sentry source S=`d288ad983c30bf7a7d924fbceb8ed7cf6e64de9c` tests all three execute bits before auto-init. The 0555 projection satisfies the identified permission predicate on a qualified exact private handler copy, while chmod/later-exec remain denied. Actual selected debug/release SO/descriptor/handler dependency/constructor/ELF/dlopen/ABI and build-to-source provenance remain required. auto_init=false does not erase earlier extension preparation, registered editor callbacks or mode-sensitive reads. No required binary may simply be omitted to avoid its effects.

Producer/writer conditions are unchanged: bind each original seed/asset/UID/importer/type/effective authored option and actual generated resource/MD5/metadata targets; qualify single, group and lazy import, callbacks and all reachable writers. Supporting inotify ordering and final hashes cannot authenticate intermediate option use. Existing UID files remain immutable; new UID/class-cache/extension/cache/editor-temporary outputs need individual inventory and finite maxima. No wildcard writable source, extra executable source, normalization waiver, new socket or external importer process is added. H's writer-closure/PREP disposition and FIT's stage/thread/mode disposition are distinct required inputs.

## Revised dormant accounting

The exact complete-unit raw predicate remains:

`H + sum_F(bytes + 16384) + P + L + 5*A + 4*U + 593920 + W_raw <= reserved_raw`

Retain all earlier strict guards. F is the actual final private-root map including runtime, original/execution bytes, generated/sealed inputs and original archives; H is the exact supervisor byte length. P includes complete sealed-source copying, generated maxima, seeded backing initialization, immutable originals, two recipe lengths, journal/manifest and every final/temporary layout entry. L/A/U are actual bound receipt, growing before-account and canonical demand lengths. W_raw includes repeated writes/truncations/renames/deletes, capture and failed work, not just final file sizes.

New FIT fields increase U, grant/result/receipt and seal metadata; the complete raw case records retain their exact serialized bytes and measured setup/workload reservations. The additional dd1_runtime_fit source and executable stand-in/native handler inventory must count at each actual copied destination. A 0555 mode does not add executable bytes, but its metadata/map/lineage and actual copying are not free. The changed supervisor is958936B, SHA256 `41a4529af3bb2fb3f065d164dee8ac9cd2af7d761311c5ee63538347a44e45a4`; harmless fit-inert is17776B, SHA256 `cec3475556f7ac929a699b0ce28e52848c917cf1707d0b61c2fdf9129af75c7f`. Their exact build commands/pins and point-of-use identities are in BUILD-BUNDLE and each section command; no reassurance rebuild occurred after that build.

The previous conditional 3+m engine-process /5+m engine-plus-contained-start sketch remains conditional. With m=2, seven starts and the core runtime/helper/file-metadata/fixed-control lower bound756616480B do not establish affordability. The core seven payloads149655752B are reused published inventory facts, not restaged bytes. Add actual source/H/plugin/selected extension/handler/library, execution/original/seed/generated/sealed copies, FIT metadata, L+5A+4U+W_raw for every unit, and distinct applicable prior static/compiler/failed costs. Re-described observations are not charged twice; unmeasured cumulative work is not zero. Actual native unit/account/receipt sizes, generated maxima, optional closure and private outstanding reservations are not available, so no affordable native sequence is certified.

Each new outer build/section command records its own wall/CPU/rusage scope. Earlier positive resources retain their own execution identity. Eleven synthetic envelopes total11 starts,176000000000 reserved CPU ns and138412032 reserved raw bytes only within the disposable tests; they do not consume, refund or top up either real account. Full session/token/publication/static-write totals are not fabricated.

## Finite next gate

Source/inert guarantees are supported by the attributed connected records above. Native entry remains blocked pending the specifically named complete per-stage thread/build closure, exact native byte/feature/dependency inventory, authenticated writer/PREP/FIT dispositions, individually bounded generated/temporary outputs, complete cumulative pricing and point-of-use supported host/private-account/lease observations. A new unexpected requirement outside the two selected deltas is not authorised here.

One fresh isolated reviewer must assess the WHOLE FIT base-to-final artifact and changed interfaces, not just this report, before scoped acceptance. This author does not approve itself or invoke a reviewer/native transition. Published recovery remains2040/2048; historical1277/8192, consumed attempt,6915unspendable and UNKNOWNs remain. Deadline `2026-09-24T17:54:40Z` is unchanged; a later expired/unaﬀordable operation stays blocked without separate real authority. N0 BLOCKED; certificates0/3. No engine/live-account write/production change/merge/release/closure.
