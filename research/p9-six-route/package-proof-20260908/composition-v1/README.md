# Closed named comparisons, not package admission

Frozen at `0726f9e4eecd84ede09ae587befd1d216c038ab1` before one native execution.
All source, tests, logs, results and 1,861,970 original raw bytes are preserved
losslessly in the six manifest-ordered parts. Concatenate and unpack as tar.xz.
The complete archive SHA256 is in TRANSFER.json. No engine or font is included.

Facet: the specified Chisel/coupled-Shatter producer is not the same full-state
contract as either old Eclipse Slash or Shatterhymn producer. Sixteen common
contexts retain costs, shell/chips, coupled Stun, Cracked, events and reset.
This refutes those exact producer-role aliases, not every closed-family union.

Cycle: exact source expansion proves the two-effect loop is the same guarded
composition inside the unchanged whole-command envelope. Forty paired complete
native traces match. Draw, death guards, pending chips, costs, shared triggers,
RNG and CardInst identity are retained. The active card cannot draw itself
inside its own action in a valid single-zone state; a later turn may return it.
This is not whole-card equivalence to old Honing or a two-card play sequence.

The capture contains 128 constructed traces, not whole runs or independent
samples. Post-capture adversarial self-review preserves the frozen reader and
checks all 272 convenience views against the retained reflection; 29 tests pass.
The review is AUTHOR SELF REVIEW, NOT INDEPENDENT. No prior negative or product
law changes. Detailed scope, executable code and full evidence are in the pack.

Do not repeat the old 672-row verifier, Facet language/subwords, v25 or the
1024/2048 panels. P9 remains unfinished, 0/6 current packages admitted. Complete
closed-package identity, causal/policy/reachability certificates, detector,
confirmation/retention, guardrails and exact-product integration remain open.
