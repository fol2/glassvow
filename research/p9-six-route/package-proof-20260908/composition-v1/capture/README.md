# Exact Facet comparison and guarded Cycle decomposition

This packet closes two named source/trace questions from the current handoff.
It does not admit a package, reopen Node O, change the candidate, or reproduce
old statistical panels. The prospective native query was committed at
`0726f9e4eecd84ede09ae587befd1d216c038ab1` before its one native process.

## Decisions

**Facet is not the same complete producer contract as either old Resonance
pairing.** Sixteen common initial contexts compare Chisel, Eclipse Slash and
Shatterhymn followed by Lance and the next native turn. Chisel's threshold
crossing can change coupled Stun, shell, chip balance and resources, not merely
apply the same Cracked status. The distinguishing observations contain no card
names. All complete state and event traces are retained. This refutes only
these two exact producer-role aliases, not every union of old Shatter/Echo
packages, nor all possible state abstractions. It does not rescue the previous
672-row failed control contract or prove natural frequency/viability.

**Cycle is an exact guarded composition, not a new atomic effect operator.**
`compose.py` pins the baseline and candidate and proves the actual two-effect
loop is equivalent to its guarded expansion inside the unchanged whole-card
pre/post envelope. This preserves Energy, CardInst, every shared trigger,
RNG, death guards, draw, chip flush ordering and card zones. Forty complete
native paired traces agree, including reshuffle and combat-ending cases.
The argument is source call-sequence identity; forty examples do not establish
a universal theorem by themselves. The theorem assumes the pinned, well-typed,
immutable effect arrays and ordinary single-zone CardInst validity.

The currently played Momentum instance cannot draw itself during its own
Draw(1): the instance has already left the hand and has not yet entered discard.
A later native turn can draw it again. Growth parameters, rarity and the single
card-action envelope are retained. Consequently this is NOT a proof that the
whole candidate equals old Honing Edge, equals two separate card plays, or is
universally futile. Complete closed-family package comparison remains open.

## Capture and review

One Godot `4.7.2.stable.official.ed1daf0bf` process returned 128 finite constructed
traces (48 Facet, 80 Cycle), not whole-run outcomes or independent samples.
Executable SHA-256 is recorded in PROTOCOL.json. There were no parse/script
errors or experiment diagnostics. Engine, source and stderr records are retained;
this is not a claim of qualified workload provenance or a new Node O PASS.

The five preregistered code hashes remain unchanged. Subsequent author review
added adversarial reader tests and `review_capture.py`, which separately checks
header/source identity and all 272 convenience views against the full retained
reflection. It does not alter the old reader or rerun the engine. All 29 tests
pass. Review is **AUTHOR SELF REVIEW, NOT INDEPENDENT**, under #421 comment
5586269454. No independent confirmation claim is made.

## Offline reproduction

From the unpacked packet:

```
python -m unittest -v test_compose test_native_reader test_capture_review
python read_native.py raw.ndjson
python review_capture.py .
python compose.py EXACT_BASE_REPOSITORY EXACT_ASSEMBLED_CANDIDATE
```

The last command uses the existing repository's
`research/p9-six-route/source-package-audit-20260908/assemble.py`; it does not
execute Godot. `COMPOSITION.json`, `NATIVE.json` and `CAPTURE-REVIEW.json` are
complete derived outputs, not summary substitutes for the included full raw.
The local PROTOCOL.json is a whitespace-formatted copy of the remotely frozen
JSON object; its semantic object and every bound source identity are unchanged.
No claim of identical protocol text bytes is made.

## Continuation boundary

Do not repeat these two comparisons, the old Facet language/subword proof, the
672-row verifier, v25, the 1024 validation or 2048 original-arm panel. Do not
reinterpret this exact loop expansion as the complete closed-package quotient.
The remaining admission question concerns whole package law/closed-family union,
then complete causal/subset/policy/real-economy evidence. The seven-direction
detector, corrected untouched confirmation, unrestricted endpoint retention,
guardrails, lifecycle and exact-product/#108 receipt remain unpassed.

P9 remains unfinished; 0/6 current packages are formally admitted.
