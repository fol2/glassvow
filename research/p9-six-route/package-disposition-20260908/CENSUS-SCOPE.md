# Complete existing-cohort census, not package admission

The builder read all 32 exact original-arm screen files (2048 outcomes), checking
engine/content/driver/tool identities, seed order, policy bytes and complete
screen-file coverage against the mounted 518348-byte archive b866d60d...58d326c.
It added no game run, seed, policy or independent sample. The four committed parts
losslessly preserve all retained seed-level offer, final-coownership, aggregate
co-play and win flags, the exact base policy and its binding for every context.
The source archive is still NOT wholly remotely preserved; these parts are a
compact derivative, not its replacement or full-state reconstruction.

The cohort contains ONE base-policy vector under four build/play modes. It is
not a multi-policy support rectangle. The later 1024-row controller validation
is a different experiment, not invalidated by this finding. A nominal route's
co-play is not same-combat order or causal activation; Cycle's two role IDs are
the same card, so its co-play flag is only at-least-one-play, never same-uid repeat.
Starter Chisel has no offer event. A scarce Night Sight/Phantom anchor pair is
not a census of every draw-to-handstock producer; substitute sources must remain.

Nineteen regression tests pass locally. The remote cold-readback receipt must
be obtained before calling these new compact bytes remotely reconstructed.
The malformed single-file transport at 098e99cf was removed at 158a1320 before
any consumer or workflow ran. Each replacement binary blob matched its computed
Git identity. This is a delivery correction, not a new experiment or a changed
observation, hypothesis, threshold or frozen negative.
