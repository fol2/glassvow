# #542 fixed three-lead source qualification — 15 September 2026

**Author disposition: NO_SOURCE_QUALIFIED_TRIPLE_WITHIN_THIS_SLATE.** All three
shipping leads have identifiable source transitions. None has a completed
full-package source qualification. The exact unresolved comparisons are below;
this is neither a universal impossibility claim nor permission to refill a slot.

Authority: [prepared batch 5686963192](https://github.com/fol2/glassvow/issues/542#issuecomment-5686963192).
Scientific and operating main: `07b5aa9dec8436132a524511d5438c510e322070`.
Existing branch: `research/p9-six-route-local-20260905`; sole Pro author, local
writer parked. No product edits, native/game calls, empirical spend, new seeds,
policy fitting, PR, CI invocation, or renewed #547 review.

## 1. Correction and immutable boundary

This replaces the classification once. The complete original remains at
[`7602711a:SOURCE-ENTRY.md`](https://github.com/fol2/glassvow/blob/7602711a0732bada6632956f445fb8c182d49421/research/p9-six-route/duskblade-source-entry-20260915/SOURCE-ENTRY.md),
blob `45ee46692bb403ca51b4119f7281db8c8964ec14`.

The omitted `minimal-native-20260909/CONTRACT.json`, blob
`6bf2758b32c34f405591352692b91e42a91e3767` at that same head, explicitly defines
shipping Empower -> player Strength -> three-hit Flurry. Thus “no source-backed
K2 tuple” was wrong. Shipping Honing also retains its own combat-copy bonus
through normal discard/reshuffle/draw. Absence of the modified grow-14-plus-draw
card does not establish absence of native recycling. These corrections do not
admit either lead. Shipping is the **assigned bounded product**, not a proof that
it is the only conceivable non-closed product.

Bound artifact remains read-only at
`5b6b3a718b8c6200d12d5c06c85702ea9a0f35c6:research/p9-six-route/duskblade-first-proof-20260912/`.
Binding: [5683984558](https://github.com/fol2/glassvow/issues/547#issuecomment-5683984558);
review: [5682513452](https://github.com/fol2/glassvow/issues/547#issuecomment-5682513452);
delivery: [5684770969](https://github.com/fol2/glassvow/issues/542#issuecomment-5684770969).
#547 is complete; no closeout action is pending. `ADAPTER_RECORDS.md` blob
`ba53bce69de7667ac62038026eb29474c5567460` remains the active adapter map.
The on-disk allocation remains its zero-spend unbound template, blob
`824ff18acbca2f776533beed83a43b21d9a50f2c`; the separate binding receipt is operative.
`SESSION-STATE.json` blob `8be35c9286b4a31ffc90ea9cac411e4653707311` is unchanged.

### Preserved valid source-entry findings

| Identity / finding | Preserved scope |
|---|---|
| Shipping content blob `1fdf17c5b5acda33159b4cc5badac9d166c3bf8e`; SHA256 `a0d608a5142d2e3aab799cdf33d3163922b402c2aaf2a895e46e096399b56cf1` | Assigned baseline, not an empirical candidate freeze. |
| Shipping combat blob `58c9b8c8f162e77d802fac6c10e5b884b53c8b15`; SHA256 `3adb0e063a536bf249d3b5d9524427facf1398304206da59d97594d3fff246e8` | Complete native collateral/lifecycle laws remain authoritative. |
| Bulk content `3c7b2f9dba362d19128ef82ad559d3f26e54925371d823a665767032255eadaa`; restoration `91fd5de56727df42fdcb539b61e7cc7a32e77f3f85dbbe13baef3513e7003cfb` | Closed by R6's assigned screens; no reuse or rescue. Not universal Dusk impossibility. |
| Candidate combat `a6fd99eb53030d3cf1401153bdeceec8b86ccc2af8b7643481855c681906f2ba`; candidate-only Banklight | Not shipping identities. R1–R5 native proofs do not transfer merely by matching card names. |
| R13 content `4107c7c0bbed5d9acf8c2bdf97023552426920242ea958c8ebdec793b712afd9` / combat `3ccb89f69f50e41d5a46eadd8f48c0a907fd0e382cd492b2c34dd5f93e091ad0` | Different Ash nomination; not this Dusk slate and not an R6 identity. |
| R3 whole-command control, 672 rows / 64 failed checks | COMMAND_CONTRACT_FAIL remains. Component algebra here does not repair that negative. |
| R14 `BalancePolicy.sample_range(73209000,0,128)` | Closed shipping Dusk V5 stock-family necessary screen. V0 unopened; no retry or parameter/cohort rescue. |
| `balance_sim.gd` blob `3578488bea72c6ba45a787228dfc1e669c11969c` | `mature-three-act-no-side-state-v1`, all reveals, unlocks `[aspect2]`, no cross-run side state: not legitimate `P_v` provenance. Signed B is unchanged. |
| R11 SUMMARY_ONLY / R12 INCOMPLETE | Block only claims requiring their missing bytes; no rerouted payload or recapture. |

## 2. Source-bound complete tuples

All references resolve through `SOURCE-INPUTS.json` (commit, path, Git blob,
reviewed operator fragments). Product rules apply at both vows 0 and 5; this does
not assert that the constructed projection states occur naturally at either vow.
Upgrade is a separate dimension from vow; neither changes the rule ordering.

Shared state for any full comparison must retain player HP/Block/Energy and
statuses; live target identities, HP/Block/Facet/chips/statuses/flags/intents;
card-instance identity, upgrades, bonuses and ordered zones; terminal state;
Embers/relic latches/counters; ordinary profile, offers, costs and acquisition;
and public action history. Shared native background must be present on **both**
sides. Removing native Empower or Honing from the comparator would manufacture
novelty. The checker is an explicitly smaller projection, not that full system.

### N1 — coupled Facet

**Producer:** a paid, legal connecting Chisel attack (`cost=1`, damage 4/7,
printed extra chip 1). `play_card` checks hand UID, live target, terminal and
`eff_cost`, removes the card and pays first. `hit_enemy` applies Strength,
Dimmed/Cracked rounding and Block, then records the pending connecting hit.
Thorns/death resolution follows; only after the entire card resolves does settlement grant
`1 + printed_chip + beacon` once per connected living target. Printed extra
chip is not an unconditional explicit `kind=chip` effect.

**Mediator:** on the same living target, threshold overflow and harder next
Facet, coupled Stun/stagger and Cracked 2, Ember gain and applicable native
collateral. Adamant can spend its first hold without an actual Shatter.
Prism, Bell, Smolder transfer, Ember cap and RNG side effects cannot be erased.
Shatter itself is not an extra invented direct-damage hit.

**Consumer:** later affordable `resonantLance`, cost 1, `shatterEcho n=7/10`.
It reads `staggered OR vulnerable>0` **before** its own hit/chip settlement and
multiplies base n by 2 when enabled. Strength and mitigation then apply normally.
The card is playable without this mediator; a legal unboosted attack is not an
unavailable tail. The nominated payoff requires attribution to the earlier
Shatter, not merely an enabled Echo guard. Direct Cracked is a genuine substitute.

**Observable/lifetime:** target status/Facet/Stun and target-preview path are
source-visible. Retargeting does not carry target state. Status decay/Stun
consumption, death and combat reset matter; do not assume a fixed perpetual
window or erase one coupled component. Native field names are `vulnerable` and
`staggered`. See causal design for direct-Cracked confounding and physical HP
removed versus reported damage/overkill.

**Acquisition:** Chisel is in the ordinary Dusk starter. Resonant Lance is rare
and locked by `paneBreaker` (15 Shatters); it must subsequently be naturally
offered/acquired. Earning the deed does not grant a target deck. Source tuple
exists at both vows; earned common profiles and support have not been witnessed.

### N2 — shipping native Fervor

**Producer:** Empower, cost-one uncommon Power, grants player `str` 2/3.
Its Power lifecycle consumes the played power; do not treat it as an ordinary
exhaust event that earns Ember or Verdant Branch draw.

**Mediator:** player Strength persists across hits/actions and follows the
player when retargeting; new combat starts a new status context. Preserve
Strength from other sources separately from the producer-attributed component.

**Consumer:** uncommon Flurry, one cost-one attack with three sequential hits
of base 2/3. Each hit reads Strength; all hits share one card lifecycle; implicit
chip settles once after the action, not once per hit. Block is depleted across
hits, not reset. Death or lethal Thorns can deny later actions. A new Shatter
cannot retroactively amplify the earlier hits of that same card.

**Payoff/independence:** Flurry remains an affordable three-hit attack without
Empower. On the plain unclipped no-collateral domain, producer P and repeated
consumer component C yield interaction `(3-1)*s`; C-minus removes only the
producer Strength contribution from hits 2+, retaining hits and first-hit
contribution. This is not a universal actual-HP formula: full Block or terminal
clipping is a counterexample. It does not establish command-level subset nulls.

**Observable/acquisition:** public Strength and target preview offer a source
observation path. Both cards belong to the uncommon progression/reward system;
normal offers, purchases, skipped alternatives and upgrades remain recorded.
No Ritual primitive, five-hit card, R14 sampler, or old native panel is imported.

### N3 — native same-instance Honing replay

**Producer:** uncommon cost-one Honing `momentum`, damage 6/grow 4, upgraded
8/6, no draw. `_apply_special` hits with `n + inst.bonus` **before** incrementing
that same object's bonus. The bonus increment is not proof of a future legal
attack: the preceding hit may have ended the fight.

**Mediator/enabler:** fight-scoped bonus on the same combat copy. Ordinary
non-exhaust discard keeps that object. With empty draw, `draw_cards` moves the
discard array to draw, performs native Fisher–Yates, and returns existing objects
to hand subject to the ten-card hand cap. No insertion/teleport or selected RNG
permutation is allowed in a factual run. Deflect costs one, gives Block 6/9,
then draws one; it is only one possible enabler. Ordinary turn draw can substitute.
The proof checks identity under an admissible permutation; it does not sample or
predict a real shuffle.

**Consumer:** a later paid legal play of the same `(fight, uid)`. It reads prior
bonus then adds new growth. A second copy does not inherit growth.
`CardInst.combat_copy()` creates a new zero-bonus instance while retaining UID,
so UID alone across fights is insufficient. Without bonus, the same card can
still be legally played; that establishes affordability independence, **not**
that repeated self-growth is a non-redundant complete package.

**Observable/payoff:** `CombatRules.preview_play` includes `inst.bonus` in
momentum's damage calculation; `presentation/combat/combat_screen.gd` passes
preview loss to `EnemyView.set_preview`. This is a real source projection, not a
claim that raw bonus/UID is printed or that total preview is an injective decoder
in all clipped/rounded contexts. Selected-card public history plus preview may
be used; hidden deck order or raw UID as a class label may not. Later damage alone
cannot identify growth: Strength can produce the same number.

**Acquisition/reset:** normal uncommon Honing offer/acquisition, plus applicable
`poolWave2` reveal (runsPlayed 2). No particular draw card is a frozen required
component. One legal returning instance is required for enactment; ownership or
two copies alone do not suffice. No permanent run-deck upgrade is inferred.

## 3. Comparisons, actual proofs, and exact missing comparisons

These are source/transition conclusions, not re-estimates of old negatives.
Archive contract head A is `c802be36510273481b6d0f865b92fac43abf6aff`.
Closure index E is `f8a3c7b9965c6cb21fa1714b59c8537bcb89c3be`.

| Comparison | Checkable conclusion | Boundary / outstanding dependency |
|---|---|---|
| N1 vs old Eclipse Slash/WarCry -> Lance (`resonance-package-v1.json`, blob `9e6b3bc6...`) | Exact old producer set is not Chisel. In the plain near-threshold projection, Chisel can leave harder Facet + Stun + Cracked + Embers; direct Cracked alone leaves the same Echo guard without that attributed tuple. Checker proves the guard alias and target-retarget distinction. | This refutes a **guard-only** identity/attribution rule, not every legal old producer word. Direct producer attacks can themselves chip; the full common background must stay. Missing Q1 below. |
| N1 vs `shatter-flow-v1.json`, blob `e6e5d04a...` | Old Fracture Rhythm has persistent `shatterflow`, draw 2 and Energy 1 per Shatter/stack. Shipping coupled Shatter is not that extra action-economy power. This is an exact contract-level distinction, not name matching. | The old prototype retains baseline native Shatter/Echo. Distinguishing its added payoff is not proof the proposed package is outside a union containing that background. Q1 remains. |
| N1 applicable Shatter/Cracked families | Coverage index explicitly names partialFacet, crackedTempo, crownEffect, shatterTempo, reseamBreak, rewardOverchip, retainedDusk. No negative is waived. | **Q1:** complete relation/composition against each of these and resonance/ShatterFlow with shared source background, target identity, resources, lifecycle, acquisition and collateral; no complete map is supplied here. The index is not an exact behavioural quotient. R3's whole-command control must also be met, not replaced by component tests. |
| N2 vs one-bit setup (`post-v38-action-grammar-inventory-v1.json`, blob `1fcc694e...`) | Old setup bit is non-stacking and consumed by the next Attack; its proposed payoff is extra chip, not Strength. Checker word Power,Attack,Attack gives `[true,false]`; native Strength is not consumed. Energy/Cycle classes have their own declared producer sets. | Refutes identification of the Strength state with this bit alone. Old background still includes native Strength; not a complete non-redundancy proof. |
| N2 vs Power-cycle (`post-v38-power-cycle-capacity-v1.json`, blob `1834721e...`) | Old two booleans require a distinct reward draw-Skill in either order before next Attack. Empower,Flurry needs no draw-Skill. Checker verifies missing-cycle and either-order cases. | Both old factors were fixed at zero/unimplemented in capacity evidence. Their negative cohorts do not disprove native Fervor. **Q2:** full shared-background decomposition versus these grammars, persistent scalar-Strength/multihit baseline, and old Fervor's canonical role contract is unresolved. |
| N2 vs old five-hit R1/Fervor | Shipping has three hits, not candidate five; checker rejects the five-hit parameter substitution. Native contract positively defines the shipping tuple. | Different hit count/storage is not a new class proof; old candidate native evidence cannot transfer. Q2 must resolve canonical equivalence/decomposition rather than claim novelty from 3 vs 5. R14 remains closed separately. |
| N3 vs exact Honing-repeat capacity (`post-v38-momentum-preference-capacity-v1.json`, blob `90d32549...`) | Archived event predicate is two Momentum plays with same fight and UID. The checker reproduces that projection and rejects second-copy/cross-fight aliases. Native growth/replay is not a newly discovered event grammar. | Protocol varied one policy preference, kept baseline native content and an unimplemented extra repeat payoff at zero. **Q3:** a count-predicate projection does not by itself prove equivalence or separation of the proposed complete native bonus package with its legal/public observation and interventions. Do not rerun or rescue the closed remap/cohort. |
| N3 vs modified grow+draw Cycle, one-bit Cycle, Power-cycle | No on-card draw in shipping Honing. Ordinary zone movement retains bonus; a draw Skill does not create the bonus. Deflect's indispensability is refuted by the shared ordinary draw operator, not by deleting its Block/cost. | Q3 also requires full composition versus inherited self-growth + generic redraw and the exact closed/admitted cycle role. Source concatenation alone is not the missing behavioural quotient. |
| N1/N2, N1/N3, N2/N3 peers | Retargeting transfers player Strength but not target Shatter; second-copy substitution preserves player Strength but not prior instance bonus. Named projections differ. | **QP:** full pairwise observational/causal package comparison with common native background and resources remains unproved. These discriminators are not full peer certificates. |

### Written source proofs checked at vulnerable points

**Order:** the play loop resolves all effects before pending chips. Echo samples
its guard inside `_apply_special`; thus a Shatter caused by its own later chip
cannot cause that earlier Echo decision. With no prior guard, its base is n,
not 2n. Chisel's blocked/dead-target and Adamant contrary cases prevent false
attribution. Existing direct Cracked can still enable Echo when attributed
Shatter is absent. No Stun-only intervention is introduced.

**Fervor:** for all plain unclipped cases with baseline contribution b, the four
component responses are `3(n+b+s)`, `3(n+b)+s`, `3(n+b)`, `3(n+b)`; subtraction
gives `2s`. This is a symbolic identity on the stated domain; two source upgrade
instances check bookkeeping. Full Block gives zero and disproves universal
extension. Exact native HP removed, reported damage and overkill differ.

**Identity:** zone moves and any permutation of references preserve object
identity and bonus. Induction over legal move sequences gives preservation until
an explicit write or combat-copy construction. Fresh `combat_copy` initializes
bonus zero even with the same UID. The checker hashes the full bundled CardInst
source back to Git blob `bfb25f04...` and tests replay, second copy, reset,
permutation corruption, hand cap and denied affordability.

These source proofs are useful, but neither a small projection nor a historical
coverage table proves Q1/Q2/Q3/QP. Those are **unresolved proof dependencies**, not
claims that GitHub is missing every referenced file or that computation proved
impossibility. No SOURCE_QUALIFIED_FOR_PREFLIGHT label is issued.

## 4. Executed checker and recovery

`check_qualification.py` is 278 lines of standard-library Python, with no engine
or network invocation in its default mode. Run:

```
python3 check_qualification.py > CHECKER-RESULTS.json
```

Actual result: **69 matched checks**, including expected contrary/invalid-domain
rejections; exit 0. The two vow-labelled sets test the same shipping operators,
not independent samples or earned-profile reachability. Full Block, target death,
Thorns, finale handoff, collateral Shatter, absent Energy, hand cap, duplicate
shuffle entries and five-hit substitution are not silently made successes.
Thorns/finale/relic-Smolder continuation requires native evidence and returns
UNKNOWN in this projection; it is not a zero-payoff assertion.

Inputs were read via GitHub at immutable refs. There was no authenticated local
repository checkout. Default mode recomputes **only the complete bundled
CardInst blob**, not all shipping files. The optional read-only `--repository`
mode verifies each commit:path, Git blob and retained fragment; it was **not run**
against the private repository. Manual correspondence of the remaining reviewed
operators is disclosed, not presented as automated source extraction.

`CHECKER-RESULTS.json` binds exact script/input SHA256 values and individual
observations. `RUN-RECEIPT.json` records execution, mutation rejection and delivery
integrity. `CAUSAL-PREFLIGHT.md` completes independent causal/acquisition/extractor
and staged designs for all leads, including those not qualified. `NOMINATION.json`
is the machine disposition. No adapter producer or certificate is fabricated.

## 5. Terminal disposition for this bounded batch

N1, N2 and N3 are each **UNRESOLVED_WITH_EXACT_DEPENDENCY** (Q1/Q2/Q3 plus QP).
There is **NO_SOURCE_QUALIFIED_TRIPLE_WITHIN_THIS_SLATE**. The source-only batch has
produced the correction, complete lead tuples, exact scoped comparisons and
missing comparisons, actual checker source/results, independent designs and
machine handoff. It has not completed the missing full quotient proofs. No
fourth lead or automatic content mutation follows. Certificates remain **0/3
Duskblade**; native attempts and empirical debits in this session are zero.
