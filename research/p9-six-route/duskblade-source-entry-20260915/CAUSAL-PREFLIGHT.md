# #542 causal, acquisition, extractor and stage design — NOT ACTIVATED

Applies only to N1/N2/N3 in `SOURCE-ENTRY.md`, scientific main `07b5aa9d`.
This is a completed design section, not a native producer, frozen policy roster,
issued receipt, selected seed assignment, empirical debit or qualification PASS.
The source disposition does not currently permit this triple to enter preflight.

## 1. Causal bookkeeping and controls

All factual commands use native legality, full effective cost, chosen instance
and living target. Pair observations by the same pre-intervention snapshot and
permitted effective root; branch only at the registered component. Record cost,
HP/Block, statuses, zone history, terminal transitions, full work and RNG changes.
Do not reset Block between hits or clone a dead target to complete a word.
Nominated component effects and full-command comparisons are different claims.
Both required obligations survive; R3 is not repaired by a convenient narrower
response. A legal unboosted consumer can yield a false immediate payoff label;
a missing future command after death/resource/zone divergence has UNKNOWN tail.

| Lead | Producer/mediator intervention | Consumer intervention | Decisive contrary/substitution controls |
|---|---|---|---|
| N1 | Diagnostic producer mask suppresses the attributed chip contribution while preserving the paid Chisel action and damage; full mediator removal follows the **complete** Shatter bundle, including Stun, Cracked, pane reseam/overflow, Ember and applicable collateral. Do not independently delete only Stun. A native implementation must justify how each component mask corresponds to a legal/scoped counterfactual. | Suppress only Echo's conditional doubling while retaining the paid native attack, base hit, Strength/mitigation, death, Thorns and subsequent chips. | Pre-existing direct Cracked; direct producer substituted at actual costs; retarget; fully blocked Chisel; Adamant hold; producer kill; guard-before-own-chip. If direct Cracked still enables the masked consumer, do not label it an attributed-Shatter negative. Whole-command null and matched resource alternatives remain required. |
| N2 | Remove only Empower's Strength contribution; keep cost, card removal and Power lifecycle. Other Strength sources remain background. Producer accounting records how much Strength actually persists at each hit. | Remove only the producer-attributed Strength from hits 2+, retaining three sequential hits, first-hit contribution, one lifecycle and one after-card chip settlement. A mask that turns Flurry into one hit is invalid. | Reverse order; no producer; retarget with Strength retained; second Flurry; first-hit death; full/partial Block; Weak/Cracked rounding; lethal Thorns; generic damage/Strength alternatives. Algebraic `2s` is expected only on its declared unclipped domain. |
| N3 | Suppress the earlier same-UID bonus write while preserving that earlier hit, payment, discard and other effects. Mediator clamp must keep fight/UID/zone identity and record its non-factual nature, not silently recreate a card. | Ignore only existing bonus in the later hit, while retaining base damage, cost, native post-hit growth and settlement. | Different copy; same UID next combat; ordinary turn draw instead of Deflect; blocked prior hit; kill/terminal; unreturned instance; insufficient Energy; hand cap. Later damage alone is not causal proof. Deflect-minus must preserve its Block/cost for the diagnostic and retain a separate full-command/economic alternative comparison. |

For each surviving nomination, register the required complete-factor/proper-subset
and natural-null cases before dependent outcomes. Bound SPEC §5 permits **at most
eight registered continuations per measurement root**. Do not add producer,
mediator, consumer, enabler and generic controls as an undeclared 16-cell grid.
A valid implementation must fit the required masks into that bound using the
exact bound semantics, or report an unresolved control design before execution.
Earlier MEC preflight probes also consume their actual existing allocation;
calling a probe “diagnostic” does not make it free. No allocation is instantiated here.

**Economic matching:** record native cards drawn/consumed, Energy, acquisition
price/opportunity cost, Block and timing. Compare a full chain to actual legal
alternatives available in the same factual information state, preserving budget
and ordinary choice rules. Do not gift the control extra Energy, remove cost
from setup, condition only on surviving arms or equate HP gained/lost by fiat.
Component masks diagnose a nominated pathway; they are not shipping cards or a
proof of superior whole-run play. Adaptive fallback remains allowed in later
policies and is not counted as route enactment.

## 2. Legal profiles and natural acquisition (each vow separately)

Bound SPEC §2 requires one common earned `P_v` for all five arms at each
vow in `{0,5}`. Freeze the required unlock set from a qualified slate before
confirmation outcomes; use the first valid witnessed ledger satisfying it,
not a ledger selected for win rate. No target deck, money, relic or victory grant.

Shipping initial Dusk source: Strike x4, Defend x3, Eclipse Slash, Chisel,
First Spark; Ember Heart; 99 gold; Energy 3; hand size 5. Verify native creation
and normal save/load provenance in a later separately authorized preflight.
These source values are not a recovered predecessor save.

| Lead | Source prerequisite | Required evidence and denominator |
|---|---|---|
| N1 | Chisel starter; `paneBreaker`, shatters >=15, unlocks `card:resonantLance`; rare consumer subsequently enters its legal economy. | Predecessor ledger proves earned Shatters; native pool records prove exposure. Count all ordinary runs from P_v, including no offer, skipped purchase, no acquisition and early loss. |
| N2 | Empower and Flurry are uncommon; ordinary reveal/pool restrictions remain, no Ritual. | Retain actual legal pool membership, offer set, price, affordability, choice and co-ownership snapshot. No initial forced combo. Offer/acquisition/enactment are separate indicators. |
| N3 | Honing is uncommon and participates in `poolWave2` (runsPlayed 2); ordinary acquisition and same-combat return required. | No mandatory Deflect unless separately justified; retain all ordinary draw alternatives. Co-owning two copies is not same-instance replay. No sampling only long fights or selected shuffle outcomes. |

Additional source binding: `domain/state/vigil_state.gd` at main, blob
`d4e0c0c0b966ee838e188444fd1b2c4caddb01c7`. `commit_run` accumulates ordinary
Shatters, records run receipts, increments runsPlayed and refreshes unlocks.
On a native win, `vow_unlocked = max(vow_unlocked, min(5, run.vow + 1))`.
Thus an earned V5 profile needs legitimate progression to that unlock, not just
passing `vow=5` to a fixture. This source path is not evidence that such a
predecessor has been obtained. A common profile requiring an unavailable Ash
action fails the bound entry condition. Night Sight/darkWalker, unrelated unlocks,
quests or bequests cannot be silently added to help this slate.

Whole-run probabilities keep **all assigned ordinary run roots** as denominator:
acquire, enact, win, and win-and-enact. No-acquire, no-consumer, loss and fallback
wins remain in that set. Opportunity-conditional diagnostic strata are separately
labelled and cannot replace these denominators. Measurement views/clones and
multiple fights are not independent roots. Per-vow results never average each
other away. Do not estimate a profile-reaching population rate from one legal
predecessor path. #543 retains campaign/exposure ownership.

`balance_sim.gd`'s all-reveals/three-act fixture is not P_v. Preserve the exact
signed B random-build/competent-play semantics; a new driver cannot quietly change
it to random/random or balanced build. No native policy or reference is declared
competent by these source proofs; R14 stock-family failure stays closed.

## 3. Smallest later producer/extractor contract

Use the existing `GlassvowGame.apply` seam (`domain/game.gd`), RunState, Vigil and
native events; do not implement another balance simulator. This batch produces
no native exporter. The smallest separately reviewed producer must export actual
native evidence and normalize it into the unchanged `D547-NATIVE-EXPORT-2`.

**Raw provenance sidecar, not new adapter fields:** source/engine/content/profile
and producer hashes; run/fight/event/command identities; effective root; native
save dependencies; current selected instance and zone movements; target identity;
pre/post Energy, player and target HP/Block/statuses; nominal hit, blocked amount,
reported loss, physical HP removed, overkill; per-card pending-chip settlement;
Shatter/Adamant/Stun/Cracked/Ember/collateral events; snapshots and ordinary terminal;
mask identity; legality, work and unavailable-tail reason. Sidecar identity fields
are for independent validation, **never policy/predictor features**.

| Native facts / reader duty | Existing adapter-facing fields |
|---|---|
| Host-authenticated producer and expected source roles | `producer_sha256`, `product`, `profile`, `native_oracle`, `policies` |
| Effective-root ownership and native ordinary terminal | `root`; `terminal.kind="ordinary"`, `.outcome`, `.sequence` |
| Earlier actual acquisition and available resources | `snapshots[].sequence, components, resources` |
| Per-lead legal complete chain after qualifying snapshot | `decisions.K1/K2/K3[]`, `native.chain_complete`, `snapshot_index` |
| Immediate eligible bounded payoff from independent native reader | `native.eligible`, `native.payoff`; unavailable continuation tail `UNKNOWN` |
| Full and registered masked decision views | `views[mask].public`, `.native`, `.reader_sha256` |
| Canonical behavior, no label shortcut | `public_fingerprint` |
| Real search/controller work | `work.forward_evaluations`, `work.cpu_seconds` |

N1 reader must prove an **earlier attributed** Shatter on the same surviving
target and distinguish direct/pre-existing Cracked. N2 reader must retain all
three per-hit effects and a single post-action settlement. N3 reader must prove
the same fight+UID and continuous ordinary zone lineage, prior write, later read
and affordability; it cannot infer the source of bonus from damage alone.
Source-only N labels are not yet registered K1/K2/K3 empirical definitions.

Missing receipts remain missing: `542-candidate/FROZEN`,
`542-extraction/VERIFIED`, `542-invariants/VERIFIED` with the exact required
semantic scopes, source closure, legal profile, signed B, information/work and
invariants. Host authenticates issuer and evidence bytes **before** TrustedContext;
the adapter does not authenticate itself. No synthetic issuer or packet `approved`
flag can substitute. Existing #547 review/binding receipts cover that artifact,
not this nomination, profiles or extractor. R11/R12 may be used only where their
required dependencies are actually available through permitted channels.

## 4. Public measurement design, before any development outcome

Candidate public feature inventory (not a frozen fitted model): Energy/effective
consumer cost, selected-card public base/hit count, player Strength/Dimmed,
selected target HP/Block/Cracked/Stun/Facet remainder, public Embers, public zone
counts, and public past producer/target/selected-instance history. Audit each for
an actual UI/history projection. The Honing preview path exists in source;
raw `bonus` and raw UID are not automatically public fields. When clipping or
indistinguishable-copy history makes the required attribution unobservable,
record that limitation instead of giving the policy native truth.

Stay within SPEC §5's at-most-32 canonical numeric/Boolean features for each role.
No card/package/policy names, seed, root, arm/mask, filename, actual hidden draw
order, future RNG or terminal win shortcut. Behavior fingerprints use observed
choices and normalized use patterns, not the final win label. Identical cosmetic
renaming must commute with extraction. Freeze the blind model's removal of the
registered mediator/eligibility/chain-history features by semantics **before**
development; do not select a weak blind baseline after observing results.

Retain bound deterministic nearest-class-centroid, development-only scaling,
equal priors, tie abstention, root selection and full/blind paired views. Native
reader supplies truth independently; predictor output never supplies its own
label. Conditional positive/null measurement strata retain their bound scope;
their selection does not alter whole-run acquisition denominators. No fitted
parameters, validation accuracy or MEAS PASS is asserted here.

## 5. Correct stage order and current status

| Stage | Inputs needed before that stage | What it may establish | Current disposition |
|---|---|---|---|
| Source qualification | Bound #547, exact product, fixed lead tuples, applicable full comparisons and legal/source pathway | Eligible same-product nomination; not runtime/policy/acquisition PASS | This batch: Q1/Q2/Q3/QP unresolved, no qualified triple. |
| CANDIDATE_PREFLIGHT | Source-qualified nomination and fixed required unlock set; exact venue/source/producer plan; permitted capture; actual allocation/stop/debit accounting | Missing legitimate predecessor profile **inside bounded preflight**; focused source-native/reader/guardrail qualification and public decision cases | Not activated. No final confirmation policy/model freeze is required to obtain its own prerequisites. |
| DEVELOPMENT | Eligible product/packages, qualified preflight/profile/source and legal predeclared rosters/information/work envelope; assigned development roots | Bound finite policy selection and feature/model training; panel differentiation | Not activated. Do not revive R14 or a closed controller. |
| REFERENCE_CONFIRMATION | Preflight/development complete; final confirmation freeze with exact policies, models, descriptors, source/profiles, receipts, independent effective assignments and accounting | Bound reference/B tests | Not activated; no confirmation outcomes opened. |
| ROUTE_CONFIRMATION | Qualified reference plus required route prerequisites and unchanged frozen contract | Full per-panel/per-vow route, acquisition, measurement and peer evidence | Not activated. |
| #548 / integration / release | Their own accepted prerequisites and authority | Separate work | Not assigned. |

The first later candidate-native invocation consumes the existing attempt and
records its real debit; names such as preflight, fixture or development do not
make it zero-cost. This session chooses no seeds, commits no alpha and spends
nothing. No new thresholds or numerical allowances have been created. The full
confirmation freeze follows the prerequisites that construct it, not vice versa.
