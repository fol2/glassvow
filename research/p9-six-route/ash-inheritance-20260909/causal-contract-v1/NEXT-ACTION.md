# Exact continuation — source utility is Energy, not draw

This packet contains **new, locally frozen and executed** source-utility
factorial evidence. Do not rerun it to recover or publish it. No historical
terminal, model, source, cohort or global gate was changed. No package is admitted.
The remote branch was read at `5d5375b3f2a36494ec575926b0450f585aad2cf0`.

## Scientific decision already gained

The BloodRite base exchange is 3 HP for 2/3 Energy, not draw. The appended Bloodfire
charge is separate. All-on native trajectories and the factor-separated ones
show that suppressing the charge retains exactly that original exchange.
Energy deprivation can make the fixed consumer command unavailable; the contrast
then stays undefined, not zero. Preparation/Surge empty-draw witnesses preserve
positive Phantom damage and the old necessary activation pattern but have no
source-draw contribution. Bloodfire can provide additional native healing even
when both hits already kill: capped HP removed and uncapped hit/heal are separate.

`CONTRACT.md` binds the entire remaining causal/value admission obligation map.
`review-1/CAUSAL-FIELDS.json` gives 192 fully traced constructed descriptors; it
is NOT a trained/validated policy classifier, model refit or population estimate.
Original `execution-1` records and its locally frozen reader remain immutable.

## Publication is the immediate delivery boundary, not a rerun

Current GitHub connector exposed read actions only; direct local network push
failed DNS. Local commits are on a self-contained history, **not descendants of
the actual remote branch**. Never force-push that history. Reconcile current head
and sole-writer status, then publish only the new `causal-contract-v1/` subtree on
the existing branch, preserving current source/capsules and all historical raw.
The supplied binary patch includes this subtree only; no product/workflow edits.

After publication, download/check out the exact published SHA, byte-compare the
whole subtree against `PACKET-MANIFEST.json`, run `verify_packet.py` and the
31 tests (`test_contract test_capture`) using the downloaded source. Verify the
manifest itself against the supplied SHA; don't rely on a write response. This
needs no native engine. Bind the remote commit and origin separately in its
receipt; the verifier deliberately cannot assert where caller files came from.
Only then update current shared handoff/state with this scoped evidence and the
HP/Energy correction. Historical prose/numerical terminals remain immutable.

## Next unfilled scientific obligation

Bind the planned full-run source/consumer intervention evaluation to the same
complete Energy/Bloodfire/draw semantics and an eligible fixed policy. The current
instrument is for externally fixed commands only: its switched public previews
and planner clone propagation **are not yet qualified for adaptive evaluation**.
It cannot be dropped into the existing planner and called a causal policy study.
Keep parameters/source identities and signed controls intact, qualify those
specific query/clone boundaries, then freeze the complete still-missing
producer/subset/descriptor and independently assigned confirmation plan before
opening any new cohort. The failed joint acquisition controller remains failed;
this packet neither selects a third combination nor retrospectively admits it.

No new cache, acquisition weight, root reroll, old panel replay, v25 rerun,
protected-seed exposure or frozen-model refit is indicated by these observations.
The first full certificate remains unfinished. Then six-package coverage,
seven-direction detector, independent confirmation/retention, all guardrails,
lifecycle and selected exact-product/#108 evidence are still required.
