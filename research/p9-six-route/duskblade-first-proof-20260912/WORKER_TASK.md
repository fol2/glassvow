# Sole local worker: implement D547-PC1, not another provenance search

## Settled owner authority

The user explicitly authorised option A in #421 comment 5663572978: the planner may complete missing prospective #547 definitions and specify a **new**, finite, independently reviewed allowance. Historical unknown balances, spent limits, closed negatives and protected cohorts stay unchanged. This is the new exception to the old no-new-thresholds / wholly-inherited-account assumption; it does not waive review/binding before new game outcomes.

## Inputs and publication state

The full design is `SPECIFICATION.md`. `reference_kernel.py` contains the actual numerical/accounting reference and 16 passing synthetic test methods. `ALLOCATION.json` is the zero-spend new-account template. `METRIC_REGISTRY.json` is generated reference output, not a second normative authority. `numerical_examples.json` and the full test log record the planner's **synthetic-only** checks. `GITHUB_COMMENT.md` is a ready-to-post copy of the complete specification, source reference and test output, sized below the normal issue-comment limit.

These files were produced in the planner's current run. They are not the old denied CHECKS.json, a reconstruction of #549's unavailable patch, or an alternate transport for blocked historical raw. No GitHub publication receipt is claimed: this planner session's discovered GitHub tools were read-only, while its CLI had no credentials and could not resolve GitHub. Repository permission metadata was not represented as a working write capability.

Use your already functioning normal GitHub publication path to preserve this **new authorised design** and its exact content, then integrate it into the existing research branch. Do not make another Pro invocation to repeat source discovery or tool checks. Respect any actual denial: no alternate-provider/path/encoding retry of denied content.

## Full author-side outcome

Use the existing research owner/worktree for `research/p9-six-route-local-20260905`, initially at 44951785542cd923ace036c68495f231cf4b6f81. Resolve it live and refresh current-main instructions without rebasing frozen research. Leave the read-only reviewer session unchanged.

1. Verify the supplied SHA256SUMS and read the complete design once. Publish the prepared design/source/check record through a permitted normal GitHub operation with readback. Refer to the actual new comment in issue status/handoff; do not claim it already exists.
2. Incorporate the new operative sections into the existing CONTRACT, preserving R1–R14, scientific history and the fixed release boundaries. Make all remaining POL/ACQ/MEAS/error-accounting rows executable under D547-PC1. Replace contradictory no-new-definition text narrowly; mark new rules as new. Do not alter existing detector, signed control or legacy ceiling semantics.
3. Implement the compact validator/adapter and split reference code/tests as appropriate. Initialise ALLOCATION.json with unbound/zero-spend state and UNKNOWN historical balances. Complete SPECIFICATION §8's native-provenance/manifest/logic/negative-control requirements that the numerical reference does not authenticate. There is no requirement to create a general evidence framework.
4. Complete the entire deterministic test/structure/source-identity batch and fix genuine implementation errors under the specified rules. Include a coherent synthetic final packet, all fail-closed cases, alias/duplicate/cohort controls, interval boundary and budget replay checks. No new population outcomes, protected-seed reads, Godot suite, product PR/CI or #549 reconstruction.
5. Preserve actual final source, tests, complete relevant verification logs, updated account and exact artifact/source identities remotely with readback. Existing capsule updates are subject to their actual permitted channel; record any unresolved capsule limitation precisely rather than pretending it succeeded. Do not send the task back after a table-only draft.
6. Return one neutral revised-head review packet: task authority, exact base/head, active constraints, actual deterministic evidence and unresolved limits. It is not an approval/binding receipt. The final independent review uses the same sole-worker slot in a genuinely fresh, read-only isolated context; do not role-play author self-review as independent. If your host can execute that sequential fresh context under the repository contract, use it; otherwise the author batch ends review-ready, not because an intermediate file was saved.

#547 closes only after the actual independent revised-artifact verdict, all acceptance requirements and its external binding/supply receipt. This assignment does **not** start #542 empirical execution even if the design has been implemented. The candidate-specific freeze and required evidence venue remain #542 prerequisites.

## No new technical choices handed back to the owner

The numerical margins, inference construction, fixed sample sizes, account limits, conditional measurement selection, model recipe, invalid-data treatment and thresholds are in SPECIFICATION. The worker chooses code layout and deterministic implementation details, not new cutoffs. Raise a single consolidated material contradiction only if implementation/verification falsifies the supplied design; do not ask the human to approve routine steps or fill in unspecified metadata.

Checkpoints are saves. Complete all executable acceptance rows, preserve remote evidence, then return one GitHub handoff.
