# P9 current handoff — 8 September 2026

P9 UNFINISHED. Continue #421 on `research/p9-six-route-local-20260905`.
The current machine state is `SESSION-STATE.json`; the scientific readout is
`natural-contribution-20260907/replication/continuation-20260908/DECISION.json`.
Refresh current repository instructions and issue authority; do not restart research.
Product/main remains `2ed6cdb0302ba3aab5845a18d862841165e8aaf7`.
The owner target remains three viable, reachable, genuinely distinct strategies per
aspect. Acceptance, aspect identity, coupled Stun/Shatter, IDs, RNG and saves stay fixed.

## Completed work — do not rerun

The prior capsule's “44 missing chunks / 704 rows” is obsolete. Source snapshot
`a7791720ec51a01f89ae24e32e6290bf25431bd4` already preserved them. Its downloaded
export matched all 920 Git blobs. The fixed validation is now reconciled at **64
chunks / 1,024 compact rows**: 721 wins, 303 losses, zero recorded stalls/errors;
619/768 core-route fingerprint predictions correct. The 320 older and 704 later
publications remain compact primary, not recovered native events.

`PUBLISHED-VALIDATION.json` is committed. Keep its frozen descriptor and candidate.
`read_published_validation.py` reproduces it without Godot or fitting.
`read_contribution_summary.py` completes the descriptive signed-vector readout;
all 12 anchor interaction sums have positive nominal bootstrap mean intervals.
This is stratified local contribution, not whole-run necessity or detector admission.

The original four-arm diagnostic is also complete: **2,048 rows**, 382 wins,
1,666 losses, zero stalls/errors. Its earlier commit message claimed completion
but no result bytes were recoverable, so this execution is explicitly reproduction
of a possibly exposed assignment, not fresh independent confirmation. It kept the
original six source hashes, candidate, reference catalogue, seeds and N. The
32-row smoke and 16 exact native-row original-domain parity pairs passed.
Do not credit the old reader's hard-coded eight metadata replays: zero were verified.
`ORIGINAL-PRIMARY.json` now stores all per-seed outcomes losslessly as 64-bit
bitmaps. `read_original_published.py` reproduces all 32 cells and 16 paired gaps;
these matched the complete native readout before publication.

A damaged compressed transport at `b7c31c07` was NOT admitted. Its current
replacement at `34f1f210` was generated from retained native rows and read back
with exact Git blob `99e9d0ff355811a4cee817662f16bf3b5734106b`.
Historical `PRIMARY-06.json` remains quarantined. No v25 study was reopened.
An interrupted stale-cache replay exposed 13 already-published validation rows;
all match the recovered primary and add zero independent samples. The owned
process was stopped. No research process is left running.

## Evidence boundary and next action

GitHub code, full validation compact records, original-arm per-seed outcomes and
reproducible readers are preserved. Full original-arm native capture is in
`p9-original-arms-native-20260908.tar.xz`, SHA256
`b866d60d7d9dad9bbd83610c145e242ed026bf046806ca5c7cab83f4b58d326c`.
It is **not remotely verified**. The current recovery download contains it, but
never call a local/archive download a verified remote backup. Drive upload returned
403 insufficient OAuth scope. A Notion upload handle did not upload bytes: its
binary POST was blocked by unavailable runtime DNS. Do not retry unchanged access.

The next actual capability action is upload of that native archive to an approved
file-capable store, followed by byte download and exact SHA256 equality. This is
access/evidence capability, not a renewed experiment quota. Do not expose a new
protected/admission cohort while full capture preservation cannot be verified.
Once this succeeds, continue the missing canonical-signature/null/proper-subset
and acquisition/package proofs on the fixed candidate before detector admission.
Do not start by executing `resume_validation.py remaining`: its old 320-row cache
would repeat already-completed work. Recover publications first.

No package certificate, seven-direction detector admission, unrestricted retention,
corrected independent confirmation, final guardrails, lifecycle/review/integration
or #108 P9 receipt exists. Zero of six packages is formally admitted. Do not infer
P9 completion percentages from row counts or convert these diagnostics into PASS.
Historical Node O and sealed campaigns remain immutable. Do not merge research
history into product truth or delegate unresolved research to local agents.
