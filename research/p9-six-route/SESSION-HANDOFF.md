# P9 session handoff — 8 September 2026

**Status: P9 UNFINISHED. This closes a conversation, not the research programme.**
This is the current recovery capsule, not a replacement for #421, the release bar,
or repository instructions. The owner explicitly requested this wrap-up and a
new session. No new gameplay experiment, content change, merge or release claim
is part of this handoff.

## Start here

- Repository: `fol2/glassvow`; programme #421; release consumer #108.
- Continue existing branch `research/p9-six-route-local-20260905`. Do not make a
  second writer, replacement issue, or new experiment branch merely to resume.
- Pre-handoff research HEAD verified: `e4ace6829a333f3d7b02e0e0dc5b4c9100db9f5a`.
- Product/main reference verified: `2ed6cdb0302ba3aab5845a18d862841165e8aaf7`.
  Refresh current instructions/ref before work, but do not rebase a frozen
  experiment or silently change its product/observer bindings.
- Owner target is now **three viable, reachable, genuinely distinct strategies
  per aspect**, a later extension of the issue's older two-package minimum.
  The identities of proposed routes are not requirements. The core system stays;
  justified card/value/acquisition changes are permitted, acceptance weakening is
  not. Keep aspect identity, coupled Stun/Shatter, IDs, RNG and save contracts.
- Follow current `AGENTS.md`, relevant `docs/agents/ai-sdlc.md`, #421 and
  `docs/rc-bar.md`. Historical Node O INCONCLUSIVE and sealed experiments remain
  historical; continuing the later six-route research does not turn them into PASS.
- Do the remaining research in the new session. Do not hand unresolved research
  to a local implementation agent or ask the owner to renew routine quotas.

## Current scientific decision

Keep the candidate and predictor fixed while finishing the already assigned
validation. Do not start another Nova buff or broad literature review.

Current candidate: `production2_demand3`, content SHA-256
`3c7b2f9dba362d19128ef82ad559d3f26e54925371d823a665767032255eadaa`.
Core routes in the frozen panel: Dusk `facet/fervor/cycle`; Ash
`smolder/hand/cycle`; each also has its balanced research control.
Ash Fervor is an explored alternative, not an admitted replacement.

Use the existing results instead of reconstructing conclusions from chat:

| Repository location under `research/p9-six-route/` | Recorded evidence and boundary |
| --- | --- |
| `v25-fuel-price/continuation-20260907T1434/` | Completed price, component and cost/competence comparisons. Do not rerun for orientation. Capped/free Nova did not establish selective whole-run Ember dependence. Greedy was cheaper but materially weaker; it is not a replacement acceptance controller. |
| `decision-20260907/TRANSFER-RESULTS.json`, `FACTORIAL-RESULTS.json` | Later complete Ash transfer and Fervor factorial publications, separate from withdrawn earlier v26 transmission. Exploratory, not package admission. |
| `natural-contribution-20260907/RESULTS.json`, `PRIMARY-A0.json`, `PRIMARY-A1.json`, `TRAINING-SUPPORT.json` | Published 288-run natural-state study; 9,451 sampled decisions and 37,804 cloned actions, with 9,451 factual matches recorded. Sufficient statistics remain available; full original native captures are not restored by those statistics. |
| `natural-contribution-20260907/replication/` | **Immediate unfinished work**: frozen trace-fingerprint and quality validation, not the seven-direction P9 detector. |
| `natural-contribution-20260907/original-arms/PROTOCOL.json` | Already specified next original-arm diagnostic. Check for completed evidence before executing; no completion was established in this wrap-up. |

A local same-state contribution and a whole-run adaptive replacement effect are
different estimands. If the bot changes its build after removal, unchanged wins
do not prove zero mechanism contribution. Conversely, a local combo, damage
share, policy name, or accurate route classifier does not certify viable plurality.
Do not retrofit old negatives or introduce a stronger hidden acceptance bar.

## Exact resume position

Read `replication/PREREGISTRATION.json`, `run_replication.py`, `resume_validation.py`
and `FROZEN-DESCRIPTOR.json` in the directory above. Frozen descriptor SHA-256:
`2cfe1d8ff00d5ff695664c597be3a93e3960f39732fa049eac82b9397184bab7`.
Assignment: seeds **44010000–44010063**, 16 route/aspect/vow contexts, 64 runs each:
**1,024 rows in 64 predeclared chunks of 16**. Never refit or extend N after peeking.

This wrap-up independently checked the available source export and publications:

- Source archive `p9-source-6217709e.zip` matched its recorded SHA-256 and all
  **892 archived Git blobs**. It is a source export, not a raw-run archive.
- `PRIMARY-01` through `PRIMARY-05` decode/parse to **20 unique valid-assignment
  chunks / 320 compact run records**. PRIMARY-05 passed its compressed-envelope
  length, SHA-256 and Git-blob checks. This is publication integrity, not a new
  native execution or proof of missing raw captures.
- `PRIMARY-06.json` is **TRANSPORT_MISMATCH_NOT_ADMITTED_PRIMARY**. Keep it
  quarantined. Do not repair its numbers from memory or inferred outcomes.
- With only the recovered publications, **44 chunks / 704 rows lack recovered
  records**. This is not a performance result. A previous readback reported 21
  further local receipt files; that workspace and `p9-validation-preserved-current.zip`
  are **absent from the current mounted files**. Search supplied attachments/new
  storage first; do not count those 21 without actual files and validation.
- Published training statistics reproduced the exact frozen model locally:
  288 retained records / 192 training-route records. No fitting on validation
  data, simulator execution, or new P9 admission was performed in this wrap-up.
- No active research process was found in the current runtime. This does not
  certify the status of a previous discarded runtime.

Recover before rerunning. `resume_validation.py` supports a published-primary
cache and a first-seed replay per recovered cell, then missing cells only. That
replay checks recovery consistency, not every old row's provenance. Keep reused
compact records and new native captures explicitly distinct. If a required claim
needs unavailable raw events, say so; a compact record is not a fabricated receipt.
The old `read_validation.py` expects one complete raw study: adapt aggregation
mechanically to the two verified source types, without inventing missing native
receipts or changing the frozen estimator. Preserve interrupted/failed exposures;
reproduction of exposed assignments is not fresh independent confirmation.

## Recoverable files and engine

`SESSION-STATE.json` records the exact wrap-up inventory. The accompanying
`p9-session-handoff-20260908.zip` is a portable recovery aid, **not a complete
archive of the missing latest validation run**. Its manifest binds the included
source export, legacy dependency, decision-source backup, checked primary cache,
statistical/storage audit outputs and pinned official engine ZIP.

The legacy assembler still needs
`glassvow-p9-native-continuation-20260905.zip`, SHA-256
`76041bfc907164e0c5b5252d48c4bb0aac7b29223a011eee9f03fef17f180e65`.
Keep those exact bytes; do not reimplement the legacy runner from narrative.
The source export is snapshot `6217709e`; fetch the already committed
`resume_validation.py` at the verified current branch when assembling. Do not
mistake the archive snapshot for the later branch HEAD.

Official executable: `4.7.2.stable.official.ed1daf0bf`, SHA-256
`8d106cbe6144c2dc7e881d61d2429c1a8a76e6b22ef48bd5e48dcf934953f71e`.
Check both binary identity and `--version` before native work. Do not substitute
an RC/Mono build. Preserve catalogue key order: sorting JSON can change seeded
selection. Do not assemble into a project with active or frozen studies.

## Lessons to apply, not another process platform

1. **Preserve executable state, not just a narrative.** Closed chunk -> validated
   raw output/receipt/log -> generated checkpoint -> remote write -> byte readback.
   Commit code and compact primary frequently; keep full raw archives in an
   approved durable file-capable store when available. Check its retrieval now,
   not at the next session. Never call a local ZIP a remote backup.
2. **Do not manually transcribe evidence.** Outcome strings, arrays and hashes
   must be generated from files. Transport failure is not scientific failure.
   A returned commit SHA alone does not establish exact file contents. Do not
   hide malformed compressed payloads behind a status summary.
3. **Avoid blind execution.** If outputs disappear, use a small bounded readback
   diagnostic, preserve/stop only owned processes as permitted, and report the
   exact last observed state. Do not spend hours issuing invisible reruns.
   Stop at a safe handoff when the surface is genuinely unavailable.
4. **Freeze science, not administrative quotas.** Retain sample size, estimator,
   protected cohorts and stopping rules. Internal chunking/recovery is autonomous;
   it does not require renewed owner permission or create fresh samples.
5. **Keep control definitions distinct.** Balanced rollout RandomBuild is not
   signed original arm2; the fingerprint classifier is not the seven-direction
   detector. A weaker greedy controller cannot erase a stronger counterexample.
6. **Measure decisions, not activity.** A batch must resolve a named remaining
   claim. Do not repeat v25 negatives, commission overlapping reviews, add a new
   balancing platform or pursue a particular route label instead of the outcome.
7. **Use the right context.** The restart prompt only points here. Refresh active
   instructions, then read the smallest required source. Do not paste the full
   conversation into new agents. Independent review remains independent; the
   author never certifies its own semantic delivery.

## Distance to done and exit

No six-package or final P9 certificate exists. Remaining substantive milestones:
(1) complete the fixed validation/reconcile required evidence on one candidate;
(2) establish all six viable/reachable/distinct packages and the accepted detector;
(3) corrected independent confirmation, unrestricted endpoint retention, signed
controls, ceilings and all hard guardrails; (4) minimum lifecycle machinery,
independent review, governed exact-product integration and #108 receipt.
The 320 preserved validation records are **not** a percentage-complete estimate
for P9. Do not merge this entire research history into product truth.

At the next genuine stop, report the exact decision gained, preserved artifact
identities, what is still missing, and which milestone remains. A new session is
appropriate for context hygiene, but is not evidence that tool/storage faults
are solved and does not restart scientific authority or the experiments.
