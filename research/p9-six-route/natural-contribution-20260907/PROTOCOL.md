# Natural-state contribution, not adaptive policy replacement

EXPLORATION_NOT_P9. Continue the existing owner/task/research branch. Main remains 2ed6cdb0302ba3aab5845a18d862841165e8aaf7. The latest research snapshot read at start was 33251d5ee9ff10735dcf66e7279a8cb5570918d8. No new ticket, product merge, protected cohort or changed acceptance.

## Decision being advanced

v25 did not establish a selective whole-run Ember package. Do not keep buffing Nova to preserve a route name. Existing-policy Ash transfer and the native substitution witness show why three claims must be separated: mechanism contribution at a fixed decision, a controller's adaptive replacement under content intervention, and unrestricted retention. None substitutes for the others. This study measures the first claim on naturally reached states, leaving content and policy unchanged.

Recovered source: all 140 files in the mounted p9-decision-source-20260907.zip matched its per-file SHA256 manifest. The official Godot 4.7.2 executable hash matches 8d106cbe6144c2dc7e881d61d2429c1a8a76e6b22ef48bd5e48dcf934953f71e. The common catalogue remains production2_demand3, SHA256 3c7b2f9dba362d19128ef82ad559d3f26e54925371d823a665767032255eadaa. Missing historical raw captures are not claimed recovered or rerun. Prior negatives remain immutable.

## Post-decision observation

Reuse the published exact-clone design. After the policy chooses an action, disposable clones compare mediator present/zero crossed with consumer present/erased. No clone or hidden future state reaches the policy. Every factual clone must match the subsequent actual events, return value and complete projected future state; prior append-only queue and live-state noninterference are checked separately.

Six predeclared roles: multihit (Fervor, multiplicity with same total base damage), growth (same-card bonus, bonus use), handstock (surplus above reserve, above-reserve slope while floor payoff stays), catalyst (existing poison, multiplier), chip_supply (existing chips, explicit chip bonus while physical damage/implicit chip stay), echo (staggered/vulnerable condition, extra conditional multiplier). Nulls are diagnostics, not proposed cards. Mediator manipulation is not represented as a legal player action. A single named source is not all production.

Record each arm's immediate actual enemy health removed, nominal hit damage, net poison change, and Shatter events. Do not collapse deferred poison and immediate damage into one fabricated utility. Saturation may make health differences zero; retained nominal/mediator outcomes explain the boundary. Keep negative interactions, capped deaths and all unsuccessful runs.

Deterministic sampling: first eligible decision in each role and mediator-positive/zero stratum in each combat. This is a stratified diagnostic of naturally reached states, not an unbiased estimate of every combat action. Record all eligibility counts and all sampled arm outcomes. No outcome-dependent selection. Four probe arms use the already chosen action; later live play remains untouched.

## Executed pre-row tests and fixed assignment

291 native assertions passed in this runtime across both aspects, base/up, zero/positive mediator, six roles, native legality, independently specified local interactions, state/event equality, corrupted-return rejection and lethal saturation. Import logs were readable and contained no script/parse failure. These tests do not admit a package.

Controller stays at two public determinizations/twelve continuations, current-plus-next bank estimate and leaf_terminal=false. Natural starting decks, reward economy, optional acquisition and existing route priors remain.

Dusk: facet, fervor, cycle, balanced RandomBuild. Ash: smolder, hand, cycle, fervor, balanced RandomBuild. Both vows 0/5: 18 contexts. First run 36 smoke cells (observer off/on) at seed43000100. Require all 18 complete gameplay/outcome records to match after removing only observational fields. Only then freeze and execute 18 x 16 seeds43010000..43010015 =288 new run-condition evaluations with the observer enabled. Four bounded workers; retain logs, all assigned outcomes, source/content/engine/config hashes and complete receipts. No edits inside a running batch or post-peek sample extension.

## Readout and completion boundary

Report every context, natural acquisition, mechanism enactment, per-run diagnostic vectors, exact factual match counts and all local interactions. Uncertainty is clustered by assigned run seed, not probe count. No early package selection, new detector threshold, signed-arm substitution, optimality claim, or use of aggregate win rate as strategic plurality. Same-state evidence can distinguish functional mechanisms but cannot by itself prove whole-run viability, reachability, policy sensitivity or retained endpoints.

Six viable/reachable aspect-scoped packages on one content, corrected confirmation, detector/guardrails, unrestricted retention, lifecycle, independent review and exact product integration remain open. Preserve source and machine-generated primary readout at checkpoints; verify remote blob identity rather than hand-entering results. Legacy publication previously blocked by safety is not repackaged through another endpoint.
