# Existing Fervor package under Ash: avoid attachment to one candidate name

EXPLORATION_NOT_P9. Same owner/task/research branch. The user requires three validated strategies per aspect, not preservation of a particular proposed third-route label. This test asks whether the already implemented Fervor/multi-hit controller can enact a viable alternative on Ash without another card or gameplay-law change. It does not discard or reinterpret any Ember negative, and does not choose from partial echo/fuel-price results.

## Exact unchanged basis

Use production2_demand3 content SHA2563c7b2f9dba362d19128ef82ad559d3f26e54925371d823a665767032255eadaa, the existing pre-PMF/pre-echo reference native planner, two public determinizations/twelve continuations, bank_mode=current-plus-next, leaf_terminal=false. Existing Fervor FIT priors transfer unchanged; no new route bonus, card forcing, deck substitution, resource or hidden-state access. All copied gameplay/controller GDScript bytes were checked equal to the assembled reference. Only the declared policy.route and usual aspect/vow context select behavior.

Ash still starts with its native deck,88HP,Smolder-capable rules and its native relic/Art. It does not acquire Dusk Shatter. Shared card/mechanism availability is not an assertion of globally novel mechanics: a family may require separate aspect-scoped validation. The eventual three selected packages must be distinct within each aspect; no six-globally-disjoint-family claim is made here.

## Actually observed cheap evidence

18 native assertions passed: Empower and Flurry are present in the real common pool; paid setup creates3/4 Fervor; paid five-hit consumer removes15/25 health in the specified base/upgraded no-modifier fixture; energy costs apply; Ash gets zero chips/Shatter; the existing native Fervor planner returns an action without mutating input state. These are constructed source/compatibility checks, not proof of natural acquisition or whole-run independence.

## Fixed comparative execution

After the running echo batch, preflight ten cells at seed36000100. Then five existing policies(smolder,hand,ember,fervor,balancedRandomBuild) x vows0/5 x32 assigned seeds36010000..36010031 =320 run-condition evaluations. Every arm uses identical content/source. Four workers,900-second per-cell watchdog; all losses/stalls/errors retained, no edits inside a frozen batch. No protected cohort. This test may run before the already designed v25 price screen because it can establish a lower-change alternative; no unfinished v25 result is used.

Report all policies, not only the best; actual producer/consumer offers,picks,casts; strength/multi-hit and source-health profiles; win flips with shared seed-cluster nominal uncertainty. The same seed index is not identical downstream randomness. A positive outcome licenses a separately frozen cross-component selectivity test, not automatic package admission. The diagnostic balanced control is not the signed C2 arm. Shared-mechanism transfer does not prove that the revised content is balanced or that optimisation will retain distinct endpoints.

No new ticket, product merge or P9 receipt. Six aspect-scoped package validations, detector/guardrails, corrected confirmation, unrestricted retention, lifecycle, independent review and exact integration remain necessary.
