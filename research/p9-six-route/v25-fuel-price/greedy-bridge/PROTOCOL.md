# Cost/competence bridge — same source, content and build rules

Exploration only; no P9 admission or relaxation. This is motivated by measured native root cost and the need to minimise end-to-end experimentation time. It is not selected from a partial price winner.

Use all four v25 price catalogues and all three Planned controllers plus balanced RandomBuild for both aspects/vows0,5. Change only native_rollout from true to false. Keep every other parameter, legal action/build grammar, reward rule and catalogue. The already-published greedy fallback is used; no new model is trained, no policy is tuned and no new hidden information is read. No forced deck or deliberately impaired RandomBuild is introduced.

Wait for the active price and component batches/audits to finish. Preflight64 one-run cells at35000100; then64x32 at35010000..35010031. Reuse complete hash-bound price-screen controls at those same indices explicitly. There are2048 NEW cheap evaluations plus2048 previously observed native controls; these are not4096 new independent trials or fresh confirmation. It is an adaptive, paired exploratory instrument comparison, with32 shared seed clusters. Preserve every loss/stall/error; four workers and per-cell watchdogs; stop on invalid capture; do not edit the observer during a batch.

Report every route/context/content outcome difference, natural acquisition/enactment, and actual end-to-end runtime. Summed native time includes differing run lengths and concurrent scheduling; do not call it an isolated instruction-level benchmark. Aggregate uncertainty clusters by numeric seed, not by row. Do not select a cheaper controller by pooled win rate alone or use it to argue past a stronger controller's counterexample.

This comparison can justify a later measured multi-fidelity discovery schedule. It cannot replace signed arm2, lower any package/detector/retention/ceiling bar, establish non-inferiority from overlapping intervals, or certify P9. Any eventual candidate still requires strong independent confirmation and unrestricted optimisation. Prior matrices remain unchanged. No main change, new external provider or human quota gate.
