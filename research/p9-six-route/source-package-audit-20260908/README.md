# Finite source/package audit — not P9 admission

This continuation closes two missing observations without rerunning v25, producer-chain studies, the 1024-row fixed validation, or the original-arm diagnostic. Product and frozen model remain unchanged.

## Reproduce from committed bytes

From this directory, run `python read_audit.py`, `python test_read_audit.py`, and `python read_acquisition.py`. Readers use only the standard library, validate complete split tar.xz bytes and inner-file hashes, and generate deterministic readouts. Concatenating each manifest's ordered parts reconstructs an ordinary tar.xz archive. No model, simulator, opaque custom compression or external dataset is required for the readouts.

Expected `RESULTS.json` SHA256: `82f5757b5d187dc41bb5fce4f492e7a48b12bb1689bfc7ceef6dabfd41c5ae49`.
Expected `ACQUISITION-RESULTS.json` SHA256: `70a929132c737b71b6c736e42c630a3cf8980619f8e7482401e18b0cd2c84e76`.

`python assemble.py REPOSITORY FRESH_OUTPUT` materialises the fixed fixture project from source, applying exact archived candidate/observer deltas without changing repository product files. The patches reproduce earlier candidate bytes, not a new retune. All required declared hashes and the existing vow dependency are checked. Use the separately pinned official engine only. Fresh assembly passed import/parse; the initial missing vow dependency is a repaired assembly error, not a gameplay result. Existing native fixture execution is already complete; do not rerun it for orientation.

## Decision

The legacy multihit consumer control changes hit topology even at zero Fervor. It failed exact dormant state/event equality in all 24 declared multihit fixtures. Actual collateral differences include Weak rounding (0 vs 3 damage), thorns (10 vs 2 self damage), and Cracked rounding (5 vs 7 damage). Therefore those old readings cannot certify an exact-null Fervor-amplification contrast. They remain the original frozen multiplicity estimand and descriptor data; do not refit, overwrite or retroactively reclassify them.

A separate research-only operator keeps the first hit's strength and removes extra-hit strength per target, retaining hit topology. It passed 24 dormant and 48 explicit-off equalities and 48 active hit/thorns comparisons. All 677 native assertions passed. This finite panel does not prove universal null correctness or authorize product integration.

Momentum has card-instance memory and resets on a fresh combat copy; Fervor is player memory and transfers across card copies without being consumed. These are distinguishing witnesses, not the complete canonical quotient. Echo's predicate is staggered OR Cracked, including Ash: do not attribute every Echo payoff uniquely to Shatter.

The candidate's base reward pools include some deed-labelled cards. A `locked` metadata value alone is not a runtime acquisition gate. Existing original planned-arm captures provide 12 component co-ownership/use witnesses in nine unique rows across all six routes and vows 0/5. These are deliberately post-hoc existence exhibits, not estimates of frequency, same-state combo enactment, route-controller competence or new independent confirmation. No acquisition system change is justified by a claim that these components cannot be obtained.

## Preservation boundary

`native.parts` preserves the entire emitted fixture NDJSON, stdout, stderr, native receipt, import and parse logs. `acquisition.parts` preserves complete selected old native rows, their manifests/receipts/logs and a selection/source index. Readback must download, decode and hash these bytes; a successful write is not the receipt.

The original 518348-byte historical native archive is still not wholly remote verified. Neither the small storage probe nor these selected rows claim otherwise. No package is formally admitted, no detector has been certified, and no #108 P9 receipt is issued here.
