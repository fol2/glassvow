# #421 TARGET_ACTS_LAST_THIS_PHASE natural-opportunity and identity-design disposition

## Disposition

`SUCCESS — AUTHORISE_ONE_RESEARCH_PROTOTYPE_AND_IDENTITY_GATE`.

The sole deterministic zero-row audit ran against clean exact main `5f53020f588145ec5fbf803de32d82c7d29c06d2`. It authorises exactly one separately versioned research-only prototype of the already selected `TARGET_ACTS_LAST_THIS_PHASE` abstract contract and its bound identity gate.

## Natural opportunity

The complete canonical four-act encounter array and every present theme mirror passed equality checks. Two exact Act 2 normal encounters satisfy the frozen universal first-turn predicate:

- index 1: `tidecaller + mirelurker`; Mirelurker can choose `sting` or `barb`, and both are one-packet attacks;
- index 5: `tidecaller + voltEel`; Volt Eel selects one-packet `shock`.

Tidecaller deterministically selects `surge` on turn 1 and applies +2 Strength to all living enemies after its own slot. Current enemy action order follows the authored array, and enemy damage reads attacker Strength before Weak, player Vulnerable and block resolution. Stable-moving Tidecaller to the last living slot therefore removes one guaranteed same-phase +2 Strength exposure from the later attack in each encounter while preserving move identities, action count, targets and RNG identity.

Observed estimands met their exact thresholds: two witness encounter definitions, two distinct later enemy IDs and two guaranteed first-turn Strength exposures.

## Bounded prototype design

All frozen design checks passed:

- one default-false research switch;
- one one-Energy, Duskblade-only, enemy-targeted research Skill with no bundled effect;
- one logical fight-local marker in the existing `EnemyCombatant.flags` carrier, omitted from state projections and cleared on ordinary, death and combat-end exits;
- two gameplay hooks: exact research special producer and stable local phase-order consumer without mutating `cb.enemies`;
- one direct event type with `targetIdx`, before/after living positions and `moved`, plus one presentation branch;
- at most three product-source files in the research projection and two research-only files;
- exact whole-run disabled-path and matched direct-null contracts;
- no policy score, target override, acquisition override, ML or RL method.

Scoreline and Afterimage remained separate factors fixed off. Any later joint cells still require shared CRN interference anchors with measured decision value.

## Mechanical history and accounting

Independent preflight rejected v1 before any live output because its exact-witness, duplicate-surface and design-schema proofs were incomplete. Mechanical correction 1/1 produced v2, whose self-check passed 9 checks. A bounded follow-up confirmed that the canonical top-level four-act array plus equality of every actually present theme duplicate is the complete bound surface; an absent Act 4 theme duplicate is not a missing authored surface.

- Live static-audit runs: 1.
- Output: 4,554 bytes; SHA-256 `b2e382f0c2d958fdff6668bc23d3ea676523b4fde26ea0a4aea02e36c498c758`; wall time 0.040019 seconds.
- Authorised research prototypes: 1; candidates constructed: 0.
- Godot processes, simulator rows, cache reads/writes, ledger reads/writes and protected-seed rows: all 0.
- Development checkout remained clean and untouched.

## Claim boundary and next gate

This proves repeated authored opportunity and a complete bounded research design only. It does not prove prototype correctness, identity safety, natural capacity, policy sensitivity, complementarity, reachability, a product candidate, #421 acceptance or the #108 P9 receipt.

Next: construct exactly one research-only source/content projection, freeze the exact compliant Godot binary and fresh unprotected identities, then run the preregistered whole-run null, matched direct-null, two direct positives and invalid/expiry matrix within the frozen ceilings. No capacity or causal row is authorised before that identity gate passes.
