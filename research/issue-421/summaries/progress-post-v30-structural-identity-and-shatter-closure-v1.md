# Issue #421 structural identity and Shatter closure

Source remained exact current `main` `c4130163c7fb8edd865c0adc95732aae03e1bad2` under the consolidated owner authority in comment `5453573232`. The append-only ledger remained byte-identical at 480,372 records with zero protected-seed rows.

## Decisions

- The source-complete Facet-ownership audit selected only `cross-enemy-partial-facet-extra-chip`. Its ten direct maps and RNG checks completed, but the frozen focused-mediator comparison retained distinct control IDs and failed before any sentinel or whole-run row. Decision boundary 2 closes the primitive and target-topology family without comparator repair or rerun.
- The exhaustive enemy-Ward transition audit selected only `ward-break-without-blood`. Ten direct maps, RNG checks and the explicitly ID-free mediator comparison passed. The first capture sentinel then found poison-generated synthetic `hitEnemy` events outside `hit_enemy` without the frozen Ward fields. The one-attempt rule stopped after 16 null observations and before factor identity or enabled execution. Decision boundary 2 closes the complete enemy-Ward transition family without trace repair or rerun.
- The source-complete per-card Shatter partition selected only two or more Shatter events across two or more enemy targets. The sole authorised 4.7.2 trace exposes PLAY identity and aggregate fight Shatters but no SHATTER enemy `idx` or matching card-terminal event. The preregistered observability gate therefore closed the topology before opening the eligible cache. No proxy, capacity or causal claim was made.

## Execution boundary

The newly published work used two Godot processes and 16 null whole-run observations, all in the enemy-Ward identity preflight. Cross-enemy Facet used one direct-control process but recorded zero whole-run observations. Both Shatter audits were zero-row. No causal row, enabled candidate row, ledger write, protected seed, product mutation, ML, RL or optimiser entered any decision.

## Next bounded step

Issue #421 continues with one zero-row source-complete audit of exact Shatter aftermath branches. It will map unconditional vulnerable/Ember output and the Prism Charm and Bell of Endings branches to immutable closures, and may select at most the existing Shatter-triggered Smolder transfer for a separately preregistered upper-capacity screen. No support is read before that partition is frozen.
