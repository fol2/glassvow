# Post-b7b5099 Ward-spend family disposition

**Issue:** #421
**Controlling authority:** owner comment `5464228499`
**Current main:** `5f53020f588145ec5fbf803de32d82c7d29c06d2`
**Archive parent preserved:** `b7b509939ff0bd8f75ba2218309e09cb94808878`

## Frozen result

The preregistered source/direct gate completed all 82 controlled observations in
two Godot processes and 7.391 seconds. Source identity had zero faults, both
current-main worktrees and every immutable input matched, and the default,
omitted and explicit-off comparison was evaluated under the frozen matrix. The
gate returned **FUTILITY** with six direct faults and the automatic decision
`close-ward-spend-family-at-source-direct-gate`.

No capacity, CRN, endpoint, held-out, product, acceptance or protected-seed row
ran. The append-only ledger remained byte-identical at 482,036 records,
SHA-256 `97750ded8787049a62b0f6eac6bf157b682e0dd0bfc92db72eb373810807a213`,
with zero protected rows.

## Decision-relevant contradiction

The `enemy-ward-separate-payoff` control is independently decisive. The enemy
began with 7 Ward. The consumer's ordinary 5-damage Attack was fully absorbed,
leaving 2 Ward; the separate 4-damage non-Attack payoff then absorbed the final
2 Ward and realised 2 HP loss.

- Execution correctly produced **zero** Facet chip because the Attack itself
  drew no blood and the separate payoff was non-Attack.
- Preview predicted **one** Facet chip after aggregating the separate payoff's
  HP loss with the Attack hit.

The enabled preview therefore exposed a false policy signal and failed the
frozen truthful-preview and factor-isolation gate. This is a behavioural
contradiction, not a statistical or analyser-only failure.

## Remaining frozen faults

- `upgraded-brace-realised-credit` observed 14 Ward rather than the frozen 11.
- `victory-expiry` observed 10 Ward rather than the frozen 8.

Those two values exactly match rounding the authored Ward through current-main
Heavy Air's 1.25 `wardMult`. This is a source-backed inference from the observed
values and current-main modifier law: the direct matrix did not freeze all
realised-Ward modifier state, so its exact source expectations failed.

- `single-consume` retained the first payoff's two-HP delta in the later
  ordinary hit's `hpAfter` event. The frozen strict path normaliser removed the
  separate payoff event and final bounded state delta but did not admit that
  downstream event-field shift, so path isolation failed.

The upgraded/victory findings and the downstream-event finding do not weaken or
repair the decisive preview contradiction. Under the preregistered automatic
disposition, none may receive an analyser, scenario, source, threshold, card,
producer, payoff, cohort or rerun rescue.

## Bounded conclusion and authority boundary

Only the exact deliberate-Ward-investment to same-turn Ward-spend-finisher
family selected by comment `5464228499` is closed. This does not establish that
every imaginable defence/offence trade-off is impossible. The conditional
`bulwark` capacity level was never reached because the common source/direct gate
failed before capacity.

The same owner comment permits a new human-authority package only after this
complete family is validly exhausted. That condition is now met. Automation has
no authority to invent another product-semantic producer/consumer family, relax
an immutable closure, amend #421 acceptance or introduce an optimiser. The safe
next action is one concise owner package on #421 while preserving every archive
and closed-family disposition.
