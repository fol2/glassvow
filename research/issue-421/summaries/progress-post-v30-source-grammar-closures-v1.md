# Issue #421 source-grammar closures

Source remained exact current `main` `c4130163c7fb8edd865c0adc95732aae03e1bad2` under the consolidated owner authority in comment `5453573232`. The append-only ledger remained byte-identical at 480,372 records with zero protected-seed rows.

## Decisions

- Shatter aftermath v1 stopped before evidence access because its frozen runner assumed an array where exact current-main stores a keyed dictionary. The sole permitted v2 corrected only that schema iteration, then stopped because its frozen exact-order comparator disagreed with the immutable evidence order. Both results are inconclusive at cap; the whole frontier is quarantined without a support or capacity claim and without a v3.
- A source-complete stagger lifecycle audit selected only the existing damaging-intent enemy-action suppression relation. Its separately frozen observability gate found that the sole authorised trace lacks every required intent and stagger identity field. Decision boundary 2 closes that relation without reading its eligible cache, using an aggregate proxy or adding telemetry.
- The unused-discrete-transition audit stopped on its frozen source assertion before selecting terminal-hit or reshuffle representations. It is inconclusive at cap and quarantined without correction or inference.
- The complete 61-card Cracked-by-Dimmed lattice has one both-status producer, `warCry`, but no joint-status consumer. Every branch aliases already closed Cracked or Dimmed families, so decision boundary 2 closes the conjunction without support access or an invented payoff.
- The existing non-damaging Skill-chip line has one producer, `limitBreak`, and 20 observed later-Attack consumers. Reusing 256 frozen observations identified 12 potentially active, 37 exact-inactive and five viable policies, with no faults. Afterimage separation passed, but Scoreline separation failed its fixed candidate-only gate (3 observed, 8 required). Decision boundary 2 closes the line without retuning or adding a producer.

## Execution boundary

The newly published work created no simulator or ledger row and used no Godot process, protected seed, ML, RL, optimiser, product mutation or human label. Only the Skill-chip capacity screen read prior observations: the same immutable 256-row cohort used for every policy and anchor comparison. All other decisions stopped before trace, cache or support access.

## Next bounded step

Issue #421 continues with zero-row source-first narrowing from a materially independent Dusk state or zone grammar. It must freeze a finite grammar and its aliases before any support access and must not reopen Shatter, Facet, Cracked, Dimmed, Ward, Kindle, Ember, action economy, acquisition, trigger, fight quality, Skill chip or any failed observation rescue.
