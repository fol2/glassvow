# Issue #421 owner fallback closure and authority boundary

## Scope and identities

- Task SSOT: issue #421.
- Controlling authority delta: comment `5457447279`.
- Exact source and `origin/main`: `c4130163c7fb8edd865c0adc95732aae03e1bad2`.
- Qualified runtime: Godot `4.7.2.stable.official.ed1daf0bf` at the frozen absolute path.
- Ledger before and after: 480,372 records, SHA-256 `4563f536f6a57e97ac7a5b51129f3967806e5bd445088547f0c874b1cd77b2e0`, SQLite integrity `ok`, zero protected-seed rows.
- Parent archive head: `9f2d063fbeaf4ae2d92e52f12cdf8ee50197f6ee`.

This snapshot is research evidence only. It does not admit a detector, promote product code, pass #421, or issue the #108 P9 receipt.

## Controlling Option 1

The zero-row v2 preflight proved that current-main `CardInst` declares `up`, that the corrected probe contains no `CardInst.upgraded` access, and that the semantic delta from immutable v1 is limited to the three `upgraded` to `up` projections plus version/provenance metadata.

The one frozen v2 identity attempt then passed all 16 direct mappings. All three preregistered whole-run arms returned zero comparable rows, so the frozen row-count gate closed the exact private-debt design. No capacity cache, enabled whole-run, causal, ledger or protected-seed row was used. No v3, retune, repair or rescue is authorised.

## Automatic Option 2 fallback

A separate zero-row source design audit found one unique minimum contract:

- reuse the existing generic `ChoiceScreen`;
- keep the two existing Dusk producers in the starter deck;
- add exactly one existing package consumer to the existing persistent deck;
- preserve `poolWave2` availability;
- reuse `BalancePilot.choose_card` with null RNG, followed by unchanged `cardDecline`.

It added no content ID, save field, presentation type, RNG call or payoff.

The one frozen identity attempt rejected invalid configuration and passed all six direct controls: Scoreline selected `executioner`, Afterimage selected `guardedStrike`, decline remained effective, RandomBuild stayed deterministic with null RNG, Ashwarden was inert, explicit off was inert, accepted arms added exactly one card, save round-trip was exact, and content/payoffs were unchanged.

The fixed 768 null whole-run observations then differed at the preregistered cross-probe content-identity assertion before endpoint comparison. Decision boundary 2 therefore closes the exact ChoiceScreen/existing-consumer contract without comparator inspection, repair, selector change, alternate consumer, cohort change or rerun. Enabled whole-run and causal rows remain zero.

## Post-acquisition structural boundary

One preregistered zero-row audit partitioned the finite #421 mechanism grammar into nine mutually exclusive lifetime and representation classes. It validated exact current-main, the current task capsule, nine immutable decision summaries and the unchanged ledger.

Result:

- autonomously eligible classes: zero;
- materially distinct classes needing new authority: `new-combat-or-card-representation`, `new-persistent-run-state-or-relic-contract`, and `new-presentation-or-acquisition-contract`;
- simulator rows: zero;
- support rows: zero;
- Godot processes: zero;
- new ledger rows: zero;
- protected-seed rows: zero.

This is an unavailable relevant gate under the #421 human-authority contract. It is not a universal negative about all imaginable mechanics and does not declare autonomous balancing impossible.

## Decision required

Automation cannot choose the next step because the one owner-bounded fallback is closed and every remaining materially distinct representation either needs new player-facing/product-state semantics or an explicit exception to a frozen one-attempt boundary.

Options:

1. **Recommended:** authorise one separately versioned, zero-row forensic audit of the closed ChoiceScreen cross-probe content-identity mismatch. It may classify only exact behaviour/path drift versus a mechanical observation/comparison defect. It must not repair, rerun identity, inspect support, or propose a candidate without a further controlling rule.
2. Select and define one new product contract among a new combat/card representation, a persistent run/relic state, or a new presentation/acquisition surface, including its save/internal-ID and player-choice semantics.
3. Retain the safe unavailable-gate state. #421 remains open and no successor is created.

Until the authority changes, the repository, ledger and protected seeds remain unchanged; no closed family, ML/RL method or adaptive candidate loop will be reopened.

## Primary evidence

- `summaries/post-v30-private-debt-identity-v2-preflight.json`
- `summaries/post-v30-private-debt-identity-v2.json`
- `summaries/post-v30-dusk-exclusive-acquisition-design-v1.json`
- `summaries/post-v30-dusk-exclusive-acquisition-identity-v1.json`
- `summaries/post-v30-post-acquisition-structural-boundary-v1.json`

