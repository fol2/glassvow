## Preregistration — bounded persistent-contract grammar

Option 2 authority is now frozen into one zero-simulation three-contract grammar. This is not a relic-catalogue rebalance and no outcome will add a fourth contract.

The exact hypotheses, in fixed minimum-complexity order, are:

1. `mirror-reliquary-ownership-gate`: a Dusk-only uncommon relic gates the exact Ward → Afterimage → Warden's Edge reflection contract. It advances only if ownership makes it materially distinct from the already-closed payoff/setup/order family.
2. `emberglass-memory-one-carry`: a Dusk-only uncommon relic preserves at most one terminal Ember for the next combat through one namespaced value in the existing durable `questScratch` dictionary, then clears it on use.
3. `shard-compass-reward-feedback`: after a real Dusk Shatter, an owned uncommon relic adds one deterministic current-pool Facet-card reward slot without changing existing offers or RNG.

Every producer, mediator, consumer, payoff, expiry, natural cost, policy exposure, acquisition factor, save/ID/UI consequence, null path and alias risk is frozen in the protocol. Relic ownership/acquisition and the mechanism payoff remain separate causal factors. Fixed ownership must precede natural acquisition in any later experiment.

Three non-overlapping read-only agents will assess current-main implementation compatibility, immutable closure/alias preservation, and evidence-backed decision value plus ordinary policy exposure. They may inspect only the frozen source and evidence inputs and return the exact three-candidate report schema. They cannot construct a new candidate, mutate source/issues/cache/ledger, run Godot or simulator rows, fit endpoints or decide stop/go.

Deterministic consolidation selects the first candidate by frozen rank only when every source, non-breaking, closure, non-alias, evidence, policy, capacity-estimand and product-semantics gate passes. Complete reports with no survivor close this exact grammar. Any report/input fault records inconclusive. There is no correction allowance.

Ceilings: three candidates; three agents; 24 citations, 65,536 bytes, 18,000 model-context tokens and 600 seconds per agent; one consolidation process and five seconds; zero Godot processes, simulator rows, ledger reads/writes, protected identities and product mutations.

- current main: `7943e3c9360ed6920a8f4bba7a4c21d15ef4c1f1`;
- immutable closure head: `c69b2752f8a52c0d50151ba083a02948180a0846`;
- authority intake archive head: `a5725e6`;
- protocol SHA-256: `9748055155e6e484d0486e0d696903a894992ff5b05877ca69c83cec63abca4c`;
- runner SHA-256: `04d80bc3f34120bcd9a87dc6fcd5bc27f9d89bce0e1435ae47a1661a4bf2c970` (`PASS (3 checks)`).

This gate can freeze at most one contract for a separate source/capacity/identity preregistration. It cannot establish capacity, identity, causality, package or detector admission, product scope, protected-seed authority, #421 acceptance or the #108 P9 receipt.
