## Result — bounded persistent-contract grammar

The preregistered deterministic consolidation completed with `SUCCESS`. Exactly one contract survives all nine gates: `emberglass-memory-one-carry`.

The result is not a product or balance decision. It freezes one research-only contract for a separate source/capacity/identity preregistration:

- owned Dusk-only uncommon relic;
- after the canonical victory/post-combat relic consumers, store `min(terminal Embers, 1)` in `run.quest_scratch["relic.emberglassMemory"]`;
- at the end of next-combat start-relic application, after cap changes, add that stored one Ember, emit the existing Ember and `RELIC_PROC` signals, then clear it;
- fixed ownership must be tested before natural acquisition; ownership/acquisition and payoff remain separate causal factors.

Gate results:

- `mirror-reliquary-ownership-gate`: source-compatible, but failed closure preservation, non-alias, ordinary-policy exposure and pre-row capacity-estimand gates. It would reopen the closed Ward → Afterimage → Warden's Edge family.
- `emberglass-memory-one-carry`: passed source reachability, complete/non-breaking contract, closure preservation, non-alias, evidence, policy exposure, pre-row capacity-estimand and product-semantics gates.
- `shard-compass-reward-feedback`: source-compatible, but failed closure preservation, non-alias and evidence-backed decision-value gates. It would re-enter the closed reward/acquisition class.

The three independent reports completed without faults. Deterministic consolidation used the frozen rank and rules once. Safe-state counts remain: zero Godot processes, simulator rows, ledger reads/writes, protected-seed rows and product mutations.

Evidence is uploaded at immutable head [`7dd4716687846760373623dcfd8a9264e260edf3`](https://github.com/fol2/glassvow/commit/7dd4716687846760373623dcfd8a9264e260edf3):

- H1 source/compatibility report SHA-256: `fc3348a6bcad8f17005e514908d2d1f585d587b79ff4c973e1248a0f64f14424`;
- H2 alias/closure report SHA-256: `e6ace34ecb697e6b12a8ada2fa554ad2d46c707fa6380bfc26f123361f232246`;
- H3 decision-value/policy report SHA-256: `15d54a78e2cf04a3641cad0ddcb432d68d379f8a5e3b91d334a6c93378cb0a42`;
- deterministic result SHA-256: `1c0b52ec2d8cab27ea69436a77dd22838b3f1b6ad1a2851246dc7e02fb897e7b`;
- protocol SHA-256: `9748055155e6e484d0486e0d696903a894992ff5b05877ca69c83cec63abca4c`;
- current main: `7943e3c9360ed6920a8f4bba7a4c21d15ef4c1f1`.

No causal, package, detector, acquisition, product, acceptance or P9 claim follows from this zero-row gate. The next relevant step is a new preregistration for this one contract's exact source projection, null identity and cheapest pre-row capacity evidence. A failure there closes this contract or records inconclusive at its frozen cap; it does not trigger adaptive replacement from this grammar.
