## Preregistration — residual combat-local action-trace grammar audit

Under the #421 SSOT and controlling owner authority in comment `5466985701`, the `TARGET_ACTS_LAST_THIS_PHASE` representation remains closed at archive head [`9cc1496d`](https://github.com/fol2/glassvow/commit/9cc1496d7105c99caf274d30cf440021a0ae3375). Every earlier valid family disposition remains immutable.

Current `main` is `5f53020f588145ec5fbf803de32d82c7d29c06d2`. The active current-main instruction identities are:

- `AGENTS.md` SHA-256 `3b22bcd95bf60dc5119799da001edf360f1e0f80c6f38daa201492cc938b50ee`;
- Godot skill SHA-256 `993aa792bfe4c38d06d5c760e6942f262bb33c17f2f580e6e3303df9610b7fd8`;
- AI-SDLC SHA-256 `87effbc352c2b137afddaaf2eb7fd99c62bc1beb8bd099287dd6d6d73383b90a`.

### Frozen question and design

Question: after all valid closures, does either untested within-card or within-one-enemy-action trace seam support one source-backed, alias-free and unchanged-policy-expressible unary structural transform worth a separate zero-row design gate?

This is a mechanism-blocked, zero-row `2 × 7` audit:

- seams: `S1_WITHIN_ONE_PLAYER_CARD_RESOLUTION`, `S4_WITHIN_ONE_ENEMY_ACTION_RESOLUTION`;
- isolated unary operators: permute, drop one, repeat one, shift one across its own action boundary, remap one target, convert one realised result type, or guard one subeffect on another realised result;
- identity anchors preserve each authored trace;
- the between-action, phase-handoff, between-enemy and terminal seams are closure anchors only and cannot expand;
- there are no joint transforms or interactions.

Scalar rescaling, multi-operator composition, cross-seam composition, acquisition/reward mutation, persistent state, internal-ID change, a new policy selector and any `TARGET_ACTS_LAST_THIS_PHASE` rescue are excluded.

Exactly three non-fork, read-only background hypotheses will use immutable, non-overlapping inputs:

1. exact current-main source-seam feasibility only;
2. immutable closure aliasing only;
3. unchanged-policy expressibility and mediator observability only.

They cannot mutate code or issues, construct candidates, read the ledger, run Godot or simulator rows, fit results, or decide stop/go. The owner consolidates once. Frozen deterministic code enumerates all 14 cells, applies the gates and ranks any survivors by direct player agency, minimum operation and semantic disturbance, then lexical ID.

### Frozen ceilings and decision

- three agents, 14 cells and at most 28 citations each;
- at most 32,000 model-context tokens and 900 seconds per agent; 96,000 aggregate context tokens;
- at most 65,536 report bytes per agent;
- one consolidation process and five consolidation seconds;
- zero Godot processes, simulator rows, ledger reads/writes, protected seeds and product candidates;
- one mechanical-correction allowance, which cannot change the question, matrix, ranks, inputs or decision rules.

Success freezes at most one minimum-ranked abstract cell for a later zero-row natural-opportunity and identity-design gate. Complete reports with no eligible cell close only this residual action-trace grammar. A report that cannot decide at cap records an inconclusive result. None of these outcomes is a card, implementation, capacity, causal, package, detector, product, #421 acceptance or #108 P9 claim.

Frozen protocol SHA-256: `9b91ac435cc9e6248bc3acaf372348a30afb883e2d1e622b398ae276792ef6ab`.  
Deterministic runner SHA-256: `e6bd225fbe2627118c374cdf4e6e78e4ff90b5db653e3a524e427ec9637cd936` (`PASS (3 checks)`).  
Task capsule v43 SHA-256: `b93ce42c57b72a7f776b310a97655c3cfe616385283223cae2ea175cc4b9caf7`.
