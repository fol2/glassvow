## Preregistration — autonomous mechanism closure envelope

The residual action-trace family is immutably closed at archive head [`c3762813`](https://github.com/fol2/glassvow/commit/c3762813a38bbc2f5d30cf3e3973ae936a6bbfee). A targeted alias check also found that a generic policy-repertoire decision-value rerun would duplicate the already-closed positive-median `policyPackageOrder` gate, whose immutable result records `measuredDecisionValue=false`. No such rerun or simulator row is authorised.

Under the #421 SSOT and controlling owner authority in comment `5466985701`, this final zero-row envelope asks whether the expanded combat-local design authority still contains one distinct autonomous class or one decision-valued method, or whether the relevant mechanism-selection gate is unavailable.

The exact finite class partition is:

1. existing combat state, zone or authored operator;
2. new fight-local state carrier or transform;
3. combat action scheduling or trace transform;
4. native run acquisition or reward route;
5. new persistent run or presentation contract;
6. external primary-rule topology.

Policy or optimisation method is assessed separately because it selects behaviour rather than defining product semantics. There are no class interactions, candidates or values.

Exactly three non-fork read-only agents will assess non-overlapping questions over immutable inputs:

- class-partition completeness and closure;
- whether the newly observed policy bottleneck has an open decision target and measured value distinct from the closed method gate;
- the exact current authority consequence if neither remains.

Deterministic code alone consolidates once. It freezes at most one minimum-ranked surviving class, freezes one policy-method question only if all four method-value gates pass, or produces the single human-authority package required by an unavailable relevant gate. Any incomplete report or partition makes the result inconclusive.

Ceilings: three agents; six classes; at most 24 citations, 65,536 report bytes, 24,000 context tokens and 600 seconds per agent; one consolidation process and five seconds; zero corrections, Godot processes, simulator rows, ledger reads/writes, protected identities and product mutations.

Protocol SHA-256: `ddddac4c0a4539f0dd10722cbe568c65548e605d333318547a54fbd4fda9f33f`.  
Runner SHA-256: `d8e71984146a7f4e4b4845eb55354a878153c92b74db3172f32c0974566baf3f` (`PASS (3 checks)`).  
Task capsule v46 SHA-256: `07afaed12e90e86f749da24847aae7385d4c78e06b025e6c071d1af3b950206b`.

This gate cannot select product semantics, change save/internal-ID/public contracts, authorise an optimiser without measured value, accept #421 or issue #108 P9.
